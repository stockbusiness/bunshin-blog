# IMPLEMENTATION STATUS

`docs/TASKS.md` のIDに対応。各タスク完了時に更新する。

| ID | 状態 | PR | 完了日 | 残課題 |
|---|---|---|---|---|
| A-1 | 完了 | #1 | 2026-08-06 | テスト基盤の本実装とCIは A-5。`src/modules` は空ディレクトリ（.gitkeep） |
| A-2 | 完了 | #11 | 2026-08-07 | 初期マイグレーションは A-8 でコミット。`targetReader` の命名を修正（Q-006） |
| A-2-R-1 | 完了 | #107 | 2026-08-11 | 人格中心モデルの第一歩。`personas` テーブルと `PersonaType` `PersonaStatus` を**足すだけ**にし、`user_personas` は残した。`blogs.persona_id` は **nullable** で入れる。**いきなり置き換えない理由**は、対象の3モデルが20ファイル（うちテスト9件）から参照されており、スキーマだけ変えると `typecheck` と `build` が落ちて**CIが赤のままマージできない**ため（Q-033）。足す・移す・消すの3段階に分け、各段階でCIを緑に保つ。`identity` は旧 `base_profile`/`tone`/`values`/`ng_expressions` を統合したもの — **分けて持つと分身1つ作るのに4つのjsonbを埋めることになり、どれがどこに効くのか読めない**。**記憶と成長の専用テーブルは作らない**（記憶は `persona_facts` と記事履歴、成長は `metrics_daily` で代替。同じことを2か所に持つとどちらが正か分からなくなる）。`blogs.persona_id` の `onDelete` は **`Restrict`** — 分身を消してもブログを道連れにしない。テーブル数の見張り（`setup.test.ts`）は 28 → 29 に更新し、**A-2-R-3 で 28 に戻る**ことをコメントに残した |
| A-2-R-2-schema | 完了 | #108 | 2026-08-11 | `persona_facts.persona_id` を **nullable** で追加する。**記憶は媒体ではなく人格に溜まる** — 同じ分身が将来べつの媒体へ広がっても引き継げるようにするため。`user_id` と `blog_id` は残す（A-2-R-3 で落とす）。**マイグレーションを機能実装のPRに混ぜない**（DATA_MODEL 9章）ため、コードの移行（A-2-R-2）の前に列だけを単独で入れた。`onDelete` は **`Cascade`** — 分身を消せばその記憶も消える（人格に属するものだから）。`blogs.persona_id` が `Restrict` なのと**あえて揃えていない**：媒体はモニターの資産だが、記憶は分身の一部である |
| A-2-R-2a | 完了 | #109 | 2026-08-11 | 分身のCRUDを**追加**する（`personas` テーブル）。既存の `UserPersona` の経路には触らないので、**この段階でも全て動く**。**`id` だけで引く入口を作らない** — 旧 `user_personas` は `user_id` が unique で条件に入れるだけで越境の余地が無かったが、**1ユーザー複数件になったので `id` と `userId` の両方で絞る**（C-6 の越境テストに5件追加）。**作った直後は `DRAFT`** — 作ることと使い始めることを分け、`ACTIVE` の上限（3件）に当たっても下書きは作れるようにした。**`activated_at` は最初の1回だけ入れる** — 段階解放の起点で、止めて再開するたびに更新すると日数の数え方が変わる。**二度押しは成功させる**（F-6 と同じ）。**専門領域と撤退条件を必須にした** — 専門が空だと何を書く人か決まらず、撤退条件を後から決めると沈んだ費用に引きずられる。**段階解放の起点が未決**のため `maxActivePersonas()` は純粋関数として置き、まだ呼んでいない（Q-034） |
| A-2-R-2b-schema | 完了 | #111 | 2026-08-11 | 段階解放の起点となる `users.activated_at` を足す（Q-034 を (a) で解決）。**列だけを単独で入れる**（機能実装のPRにマイグレーションを混ぜない・DATA_MODEL 9章）。入れるのは H-1 の `ACTIVATE`、読むのは `activatePersonaForUser` で、どちらも A-2-R-2b で配線する。**`created_at` では代用できない** — 登録しただけでは `INVITED` のままでアプリを使えず、**承認待ちが長い人ほど早く枠が開く**。**最初に認めた1回だけ入れる**（二度目の承認でも停止・再開でも動かさない）— ここを起点に段階解放・90日検証の期間・8週間継続率の3つを数えるため、動くと3つとも数え方が変わる。`activatePersonaForUser` で 1〜30日は1体・31〜60日は2体・61〜90日は3体を効かせた。**参加を認められる前は段階解放を効かせない** — 断ると「作れるのに使い始められない」状態になり原因が画面から読めない（参加前に使えないことは `isActiveUser` が別に担保）。**断る文言に経過日数を出す**（「上限です」だけではいつ開くのか分からない） |
| A-2-R-2b | 完了 | #112 | 2026-08-11 | 段階解放を配線する（Q-034）。**入れる側**は H-1 の `ACTIVATE`（`users.activated_at`）、**読む側**は `activatePersonaForUser`（1〜30日は1体・31〜60日は2体・61〜90日は3体）。**最初に認めた1回だけ入れる** — 二度目の承認でも停止・再開でも動かさない。ここを起点に段階解放・90日検証の期間・8週間継続率の3つを数えるため、動くと3つとも数え方が変わる。**参加を認められる前は段階解放を効かせない** — 断ると「作れるのに使い始められない」状態になり、原因が画面から読めない（参加前に使えないことは `isActiveUser` が別に担保）。**断る文言に経過日数を出す**（「上限です」だけでは、いつ開くのか分からない）。全体の上限3件は別に効き続ける |
| A-2-R-2c-schema | 完了 | #115 | 2026-08-12 | ブログと分身が同じモニターのものであることを**DBで強制する**。`blogs.persona_id` の外部キーを `(persona_id, user_id) → personas(id, user_id)` の複合に変え、`personas` に `UNIQUE(id, user_id)` を足した。**アプリ層では塞げない** — 依存の向きが `personas → blogs`（MODULE_RULES）なので、`createBlogForUser` から分身の持ち主を確かめると循環する。「上位へ寄せる」で `src/app/` に確認を置くことはできるが、**確認を呼び出し側の作法に頼ることになり、経路が増えたときに抜けてもレビューでしか気づけない**。C-6・D-11 とまったく同じ形で、答えも同じ — 制約はDBに置く。**既存行の移行は不要** — `persona_id` はまだ全行 NULL で、PostgreSQL の複合外部キーは MATCH SIMPLE のため NULL の行は検査されない（`user_id` は NOT NULL なので、片方だけ NULL にしてすり抜ける余地は無い）。**マイグレーションを単独で入れる**（DATA_MODEL 9章）。`personaId` を受け取る経路と越境テストは A-2-R-2c |
| A-2-R-2c | 完了 | #116 | 2026-08-12 | ブログ作成時に分身を受け取り `blogs.persona_id` を埋める。**`CreateBlogInput.personaId` は必須にした** — 任意にすると A-2-R-3（`NOT NULL`）へ進めず、テストの手直しがマイグレーションのPRへ混ざる。**確かめることを2つに分けた**：他人の分身でないことは**DBの複合外部キー**（A-2-R-2c-schema）、使い始めた分身か（`ACTIVE` か）は **Route Handler**（MODULE_RULES 3「上位へ寄せる」）。前者は越境の防止なのでどの経路からも迂回できない必要があり、後者は運用方針で抜けても他人のデータには届かない。**`DRAFT` ではブログを作らせない** — 下書きのまま媒体を持てると初日に3ブログを立てられ、段階解放が意味を失う。**1分身につきブログ1件**（DATA_MODEL 2章）を `createBlogForUser` で判定し、**`CLOSED` も数える**（閉じれば作り直せるようにすると、同じ分身の媒体が2本に分かれて一次データが繋がらない）。**`personaId` は更新の入力に入れない** — 付け替えると、それまでに書いた記事の書き手が後から変わる。外部キー違反（P2003）は「他人のID」と「存在しないID」を**文言で区別しない**（SPEC 14.1）。統合テストは `blogs-persona.test.ts`（同じ持ち主の中の決まりごと）と C-6 の `tenant-isolation.test.ts`（越境）に分けた |
| A-2-R-2d | 完了 | #117 | 2026-08-12 | 記事生成が使う人格を `Persona` から組み立てる。**どの分身で書くかはブログが持つ**（`blogs.persona_id`）— 利用者に1つしか人格が無かった頃（`user_personas`）は引数に要らなかったが、1ユーザー複数の分身になったので媒体から辿る。**読者像を媒体側から上書きさせない** — `blog_persona_settings.target_reader` の参照を外し、`personas.audience` に一本化した。**同じことを2か所に置くと、どちらが正か分からなくなる**（記憶・成長の専用テーブルを作らなかったのと同じ理由）。読者を変えたいなら別の分身を立てる。**分身の割り当てが無いブログは404** — 推測で既定の分身を当てると、誰が書いた記事なのかが分からなくなる（A-2-R-2c より前に作られた行だけがこの状態になりうる）。`target_reader` 列はまだ NOT NULL なので空で埋める（列の削除は A-2-R-3）。`EffectivePersona` は `baseProfile` を `firstPerson`/`background` へ、`targetReader` を `audience` へ置き換え、`expertise` を足した。記事生成側が使う `tone`・`penName`・`writingRules`・`ngExpressions` は名前を変えていないので、`content-generation` に変更は無い。テストのファクトリ `createBlog` が分身も作るようにした（`persona_id` が無いと書き手が決まらない）。`allowed_experiences` の除去は A-2-R-2e |
| A-2-R-2e | 完了 | #118 | 2026-08-12 | `blog_persona_settings.allowed_experiences` の参照を外し、`setAllowedExperiencesForUser` を削除した。**記憶は分身に溜まり**（A-2-R-2-schema）、**その分身の媒体は1件**（A-2-R-2c）なので、媒体ごとに選び直す対象がそもそも1組しかない。**外す前に、読む側が無いことを確かめた** — 記事生成・構成案・事実チェックはいずれも `listPersonaFactsForUser`（`user_id` +（`blog_id` または `null`）で絞る）を通しており、`allowed_experiences` を読んでいなかった。**D-6 で入口だけを足し、読む側を作らないまま残っていた**（機能の取り下げではなく、繋がっていなかった配線の撤去）。`deletePersonaFactForUser` の掃除処理（トランザクションで参照から外す）も不要になり、単純な削除になった。列の削除は A-2-R-3 |
| A-2-R-2f | 完了 | #119 | 2026-08-12 | 旧 `user_personas` を読み書きするコードを消す。`findUserPersonaForUser` `requireUserPersonaForUser` `saveUserPersonaForUser` `updateUserPersonaForUser`、`normalizeBaseProfile` `normalizeCreateUserPersona` `normalizeUpdateUserPersona`、`AppUserPersona` `BaseProfile` `CreateUserPersonaInput` `UpdateUserPersonaInput` を削除。**テーブルを落とす前にコードを外す**（DATA_MODEL 9章）— 逆順にすると、Prisma クライアントから `userPersona` が消えて typecheck と build が落ち、A-2-R-3 が赤のままマージできない。唯一の外部利用者だったデータ持ち出し（H-4）は `listPersonasForUser` へ切り替え、`persona: unknown` を `personas: [...]` にした（1ユーザー複数件になったため）。**`ARCHIVED` も持ち出す** — 途中でやめた分身があること自体が本人の記録で、抜くと「最初から作らなかった」と区別できない。`tone` と `values` の検証は `blog_persona_settings` と分身の両方から使うので `validate.ts` に残した。テーブルの削除は A-2-R-3 |
| A-2-R-3 | 完了 | #120 | 2026-08-12 | 人格中心モデルへの移行の最終段（Q-033 の「消す」）。`user_personas` テーブルを削除し、`blogs.persona_id` を **NOT NULL** に、`blog_persona_settings` から `target_reader` `allowed_experiences` を落とした。テーブル数の見張りは 29 → 28 に戻った。**`persona_id` が NULL の行が1つでもあれば、このマイグレーションは失敗する** — それが正しい振る舞いで、推測で既定の分身を当てると誰が書いた記事なのかが分からなくなる（落ちた場合は対象を調べて手で割り当ててから再実行する）。列が NOT NULL になったことで `(persona_id, user_id)` の複合外部キーが**常に検査される**ようになった（MATCH SIMPLE は NULL を素通しする）。`AppBlog.personaId` を `string \| null` → `string` に締め、`resolveEffectivePersonaForUser` の「割り当てが無ければ404」の分岐を消した — **DBが保証するようになった判定を、アプリ層に二重に置かない**。**`persona_facts` はまだ触らない** — `user_id` `blog_id` を読むコードが残っており、ここで落とすと typecheck が通らない（A-2-R-4・A-2-R-4-schema へ分けた） |
| A-3 | 完了 | #3 | 2026-08-06 | 検証対象は `DATABASE_URL` `NODE_ENV` のみ。LINE・WordPress・AI・Google連携の変数は各タスクで追加する |
| A-4 | 完了 | #4 | 2026-08-06 | `can()` の呼び出し箇所の追加は各モジュールのタスク。循環参照の自動検出は未導入 |
| A-5 | 完了 | #6 | 2026-08-06 | カバレッジしきい値は80%。統合テスト用の分割は A-9 で実施 |
| A-6 | 完了 | #8 | 2026-08-06 | 仕様の改訂は行っていない。SPEC.md は未変更 |
| A-7 | 完了 | #9 | 2026-08-06 | Search Console の日次データの扱いは未決（Q-005）。G-2 着手前に決める |
| A-8 | 完了 | #11 | 2026-08-07 | — |
| A-9 | 完了 | #14 | 2026-08-07 | 越境テストの中身は C-6。ローカル実行にはPostgreSQLが必要 |
| B-1 | 完了 | #12 | 2026-08-07 | — |
| B-2 | 完了 | #13 | 2026-08-07 | 同意を記録するAPI（`/api/users/consents`）と規約文面は H-2・H-6 |
| B-3 | 完了 | #15 | 2026-08-07 | 3ブログ上限の判定は B-4。2ユーザー×2ブログの越境シナリオ全体は C-6 |
| B-4 | 完了 | #18 | 2026-08-07 | 残枠の表示は B-5。ADMIN によるスロット解放は Phase 0 では SQL（H-6） |
| B-5 | 完了 | #21 | 2026-08-07 | 画面の自動テストは無い（テスト基盤が未整備）。ブログの追加は H-2、ジャンルの変更は E-4 |
| B-6 | 完了 | #23 | 2026-08-07 | **認可のみ。** ログイン手段は Q-012 でメール＋ワンタイムリンクに決定し、実装は B-10・B-11。ADMIN への昇格はSQL |
| B-7 | 完了 | #24 | 2026-08-07 | 一覧のみ。招待・利用停止は操作のため範囲外、サポート依頼は H-3。画面の自動テストは無し（Server Component は B-9 の基盤で扱えない） |
| B-10 | 完了 | #25 | 2026-08-07 | テーブルのみ。発行・検証は B-11。メール送信基盤は未選定（Q-013） |
| B-11 | 完了 | #26 | 2026-08-07 | 実際の送信は未確認（Resend のAPIキーと送信元ドメインの認証が必要）。`APP_BASE_URL` `RESEND_API_KEY` `MAIL_FROM` の設定が要る |
| B-9 | 完了 | #22 | 2026-08-07 | 対象は B-5 の2画面。B-8 の LiffProvider と `/liff` は未着手 |
| B-8 | 完了 | #20 | 2026-08-07 | 実機（LINEアプリ）での確認は未実施。`NEXT_PUBLIC_LIFF_ID` の発行が必要。`/liff/home`（SPEC 6.1）はタスク未設定 |
| C-1 | 完了 | #28 | 2026-08-08 | 保存のみ。**WordPressへの接続確認は C-2**（7項目・SPEC 7.2）。接続画面（`/liff/blogs/[blogId]/wordpress`）と接続状態の取得API（`GET`）も C-2。ADMIN による `site_url` の修正（Q-007・Q-008）は Phase 0 では SQL（H-6） |
| C-7 | 完了 | #29 | 2026-08-08 | 基盤のみ。利用者は C-2（接続テスト）と D-2（LP自動評価）。**プロキシ環境は考慮していない**（`HTTP_PROXY` を見ない）。Phase 0 の実行環境では直接外へ出る前提 |
| D-9 | 完了 | #30 | 2026-08-08 | スキーマのみ。**リンクの組み立ては D-1・D-8。** ASP単位の既定値の設定は Phase 0 では SQL（SPEC 10.3 と同じ扱い）。**サブIDの付け方は未決（Q-014）**で、列が必要になれば D-1 の前に別マイグレーションを挟む |
| C-2 | 完了 | #31 | 2026-08-08 | **実WordPressでの確認は未実施**（サイトとアプリケーションパスワードが必要）。偽WordPressサーバーに対しては通しで確認済み。**メディアは実際にアップロードせず** `Allow` ヘッダーと `capabilities` で判定する。接続画面（`/liff/blogs/[blogId]/wordpress`）は H-2 |
| D-10 | 完了 | #34 | 2026-08-08 | スキーマのみ。**URLの組み立ては D-1。** 値は `<slot>-<contentItemId>`。**実際のパラメータ名は未入力**（利用ASPが確定してから ADMIN が SQL で埋める）。パス埋め込み型のASPはこの列で表現できない |
| C-3 | 完了 | #33 | 2026-08-08 | 投稿と記録のみ。**冪等性キーは C-4**、content hash による更新の抑止と WordPress 側の編集検出は C-5。**呼び出し口はまだ無い**（F-7 のジョブ連携で繋ぐ）。`content_items` は Phase E で作られるため、統合テストでは最小限の行を直接入れている |
| E-1 | 完了 | #36 | 2026-08-08 | 基盤のみ。**登録されたハンドラが1つも無い**ため実際には何も実行されない。**cron は毎分**（Vercel Pro を使う。Q-016 で決定）。**記事生成（E-10）が関数の実行時間に収まるかは未確認**。Vercel への実デプロイでの確認は未実施 |
| C-4 | 完了 | #37 | 2026-08-08 | 冪等性キーの規則（`<種類>:<対象>`）と、**外部呼び出しの途中で関数が殺された場合の手当て**。印は `jobs.output_json` に置く（専用の列を足さない）。**印が残っていたらやり直さずに `JOB_SIDE_EFFECT_UNCERTAIN` で止める** — 二重投稿より安全側。復旧手順は H-6。**使う側はまだ無い**（F-7 で `publishDraftForUser` と繋ぐ） |
| C-5-schema | 完了 | #38 | 2026-08-08 | マイグレーションのみ（DATA_MODEL 9章「機能実装のPRにマイグレーションを混ぜない」）。`wordpress_posts.user_edited_at`（nullable）。**この列を使う判定は C-5。** 今は誰も読み書きしない |
| C-5 | 完了 | #39 | 2026-08-08 | 公開状態の取り込み、content hash による更新の抑止、利用者の編集検出（DATA_MODEL 11章）。**同期しても `last_content_hash` を書き換えない**（書き換えると次回「未編集」に戻る）。編集を検出した記事の上書きには `approvedOverwrite` が要り、**渡す経路は F-6**。`user_edited_at` は初回検出時刻のまま動かさない。**呼び出し口はまだ無い**（F-7 のジョブ連携で繋ぐ） |
| C-6-schema | 完了 | #40 | 2026-08-08 | マイグレーションのみ。**C-6 の越境テストで見つかった穴を塞ぐ** — 他人の未投稿の `content_item_id` を自分のブログの投稿として登録でき、`content_item_id` が unique なため**持ち主がその記事を二度と投稿できなくなる**状態だった。アプリ層では確かめられない（`wordpress` が `content_items` を読むことになり MODULE_RULES 1 に反する）ため複合外部キーで強制する。**既存の違反行があると適用に失敗する**（Phase 0 未launchのため実データは無い） |
| C-6 | 完了 | #41 | 2026-08-08 | 2ユーザー×2ブログ、30ケース。**実際に穴が1件見つかり C-6-schema で塞いだ**（他人の未投稿記事IDの乗っ取り）。拒否だけでなく**攻撃後に相手のデータが変わっていないこと**まで比較する。新しい `...ForUser` が生えたら落ちる網羅チェックを入れてある。**Phase E 以降のモジュールが増えたらこのファイルも増やすこと** |
| D-1 | 完了 | #42 | 2026-08-08 | 案件CRUD（ブログ別）と**リンク組み立ての集約**（`buildAffiliateLink`）。**`link_mode` と `sub_id_param` は入力から設定できない** — ASPの規約に関わる判断で、モニターに判断させない（Q-001・Q-014）。ADMIN が SQL で設定する。**サブIDは方式によらず付く**（リダイレクタはクリック、サブIDは成果）。リダイレクタ本体は D-8、LP自動評価は D-2。**HTTPの入口はまだ無い**（画面は H-2 以降） |
| D-2-schema | 完了 | #43 | 2026-08-08 | マイグレーションのみ。**SPEC 9.2.3 が判定を求める3項目のうち、ページ長だけ保存先が無かった**。スコアにも足切りにも使わないが、消えると「なぜこの案件が落ちたのか」を後から確かめられない。**判定そのものは D-2** |
| D-2 | 完了 | #44 | 2026-08-08 | フォーム項目数・ページ長・viewport を判定。**HTMLを構文解析せず正規表現で必要な2つだけ取る**（外部ライブラリを増やさない）。**`hidden` を項目に数えない** — CSRFトークンで3項目のフォームが0点になる。**失敗時に評価列を消さない**（一時障害で足切りに引っかかる）。SSRF対策は実HTTPサーバーで確認。**スコア計算そのものは F-2** |
| D-3 | 完了 | #45 | 2026-08-08 | 表示位置・対象カテゴリ・有効期間を保存。**画像URLは https 必須** — httpだと混在コンテンツで遮断され、表示されないまま気づかれない。**紐づける案件の所有権を `affiliate` の公開関数で確かめる**（C-6 と同じ形の穴を作らない）。依存は `banners` → `affiliate` の一方向。**HTTPの入口はまだ無い** |
| D-4 | 完了 | #46 | 2026-08-08 | `user_personas` のみ。**jsonb の形を保存直前に固定する**（DBは中身を保証せず、壊れた形は記事生成 E-8 で落ちる）。**項目ごとに丸ごと差し替え**、入れ子の部分更新はしない（部分上書きは D-5 の `tone_override` の役目）。未登録から一部だけ渡して作らせない。**HTTPの入口はまだ無い** |
| D-5 | 完了 | #47 | 2026-08-08 | `blog_persona_settings` と**重ね合わせ**（`resolveEffectivePersona`）。`tone_override` の未指定項目は共通人格を継承する（DATA_MODEL 3章）。**`allowed_experiences` は入力から設定できない** — 参照先の `persona_facts` が D-6 で、所有権を確かめられないIDを受け取ると C-6 と同じ穴になる。**入口は D-6 で足す**。ブログ別設定が無くても人格は組み立てられる |
| D-6 | 完了 | #48 | 2026-08-08 | `persona_facts` と**一人称利用の制限**。`AI_INFERENCE`×`UNVERIFIED` は要望が `true` でも落とす。**`REJECTED` も落とす**（SPEC 5.7 は明記していないが、否定された事実を一人称で書くのは未確認より悪い）。**更新時は現在値と重ねてから判定**（`VERIFIED`→`UNVERIFIED` で通り抜けない）。**D-5 で保留した `allowed_experiences` の入口をここで足した**（所有権を確認）。事実の削除時は参照からも外す |
| D-8 | 完了 | #50 | 2026-08-08 | `/go/:code` と `link_clicks`。**認証が無い入口**なので、コードは22文字の base64url（約128ビット）、飛び先はDBの値のみ、見つからない理由を分けない。**記録に失敗しても飛ばす**（読者の遷移を止めない）。**302**（301はブラウザが覚えてクリックを数えられない）。生のURLとUAは保存しない。**`is_ai_referral` の判別は G-4**（`referrer_host` を残すので数え直せる）。**`content_item_id` が同じブログかは未確認**（`content-planning` が無く、`affiliate_links` に `blog_id` も無い。**塞げなかった** — `affiliate → content-planning` が循環するため。Q-020 で複合外部キー（D-11）に決めた） |
| E-2 | 完了 | #52 | 2026-08-08 | `prompt_versions` の版管理。**有効化とロールバックは同じ関数**（過去の版を指定して呼べば戻る。どの版へ戻すかは人が一覧を見て選ぶ）。**1つの `key` に有効な版は1つまで**を1文のUPDATE（`is_active = (version = $2)`）で保つ — 2文に分けると同時実行で2つ有効になりうる。**同じ版を上書きしない**（過去の記事の生成条件が分からなくなる）。**利用者に紐づかない**ため `...ForAdmin`。呼び出し側で `requireAdmin` を通すこと。**管理画面は H-x** |
| E-3 | 完了 | #53 | 2026-08-08 | 用途（`AiOperation`）→段→モデル名の順で決める。**呼び出し側はモデル名を知らない**（SPEC「直書きしない」）。切替は環境変数（設定テーブルを足すとスキーマ変更が要り、モデル乗り換えはデプロイを伴う判断）。**単価は入力と出力の両方が揃わなければ費用を `null`**（片方だけだと実際より小さく出て E-15 が鳴らない）。**Anthropic のみ実装**、OpenAI は明示的に拒否。**実APIキーでの疎通は未確認**（偽サーバーに対しては通しで確認済み） |
| E-14 | 完了 | #54 | 2026-08-08 | ユーザー別・ブログ別・記事別・モデル別・用途別に集計。**単価が未設定でも記録は残す**（0で入る）。集計は「費用を計算できなかった呼び出し数」を返す — 0円の合計を見て安く済んでいると誤解しないため。費用は `decimal(10,6)` のまま丸めない。**`totalCostForBlog` を `totalBlogCostForUser` へ改名**（C-6 の網羅チェックが `...ForUser` で拾うため、旧名だと越境テストから静かに漏れる）。**呼び出し側との接続は E-10** |
| E-15 | 完了 | #55 | 2026-08-08 | 80/100/150% を**跨いだときだけ**通知する（超えている間ずっと鳴らすと呼び出しのたびにメールが飛ぶ）。状態を持たず、記録の前後の合計で判定する。**停止と低価格モデルへの切替は仕組みだけ用意し既定を無効**（SPEC 12.2）。**宛先未設定でも送信失敗でも記録は残す**（通知は運用の助けで、記事の生成とは別）。予算は環境変数（Phase 0 は10名で上限も一律の想定）。通知文に「停止しません」と明記 |
| H-7-schema | 完了 | #59 | 2026-08-09 | マイグレーションのみ（DATA_MODEL 9章）。**秘密が平文の列に入らないことをDBで強制する**（`app_settings_secret_column`）。設定を解除するときは**行ごと消す** — 値を空にできるようにすると、秘密なのに暗号文が無い行が生まれる。**読み書きする実装はまだ無い**（H-7）。`key` は unique だが主キーは uuid（DATA_MODEL 1章） |
| H-7 | 完了 | #60 | 2026-08-09 | 設定の解決（DB→環境変数→コード既定）と保存。**保存済みの秘密を読み返す入口が無い**（末尾4文字と更新日時だけ）。**一覧に無い名前は保存も削除もしない** — 受け取れると管理画面が「環境変数を何でも書き換えられる入口」になる。**AADに設定名を入れる**（暗号文を別の設定名の行へ移し替えられない）。**キャッシュを持たない**（「画面で変えたのに効かない時間」を作らない）。復号できない行は `UNREADABLE` として見せる。**読む側の付け替えはまだ**（H-10）。LINE のトークンは読む処理が無いため項目に出していない（F-2 で足す） |
| H-8 | 完了 | #61 | 2026-08-09 | AIとメールの接続テスト。**保存前の値で試せる**（誤った鍵を保存してから気づく順序にしない）。**副作用のない呼び出しだけを使う** — Anthropic は `GET /v1/models`（トークンを消費しない）、Resend は `GET /domains`（送らない）。**設定したモデル名が実在するかまで見る**（鍵が通ってもモデル名が違えば生成できず、実際に生成するまで分からない）。**送信元が認証済みドメインかまで見る**（B-11 の残課題）。応答本文を画面へ出さない。**一覧にない名前を上書きに使えない**。呼び出し口（画面）は H-9 |
| H-9 | 完了 | #62 | 2026-08-09 | `/admin/settings` と `PUT/DELETE /api/admin/settings/:key`・`POST /api/admin/settings/connection-test`。**平文は画面に来ない**（サーバーが伏せ字にしたものだけを渡す）。**入力欄は常に空から始まり、空のまま保存できない** — 伏せ字を送り返して鍵を壊さないため。**保存した値を返す `GET` を置かない**。**画面に置けない設定とその理由も画面に出す**（探して見つからずに止まらないよう）。CSRF はセッションCookieの `SameSite=Lax` で防ぐ（B-2）。**実ブラウザでの確認は未実施** |
| H-10 | 完了 | #63 | 2026-08-09 | メール送信・宛先・予算が**DBの設定を見る**。**`getMailer` のキャッシュを外した** — 覚えたままだと管理画面で鍵を差し替えても古い設定で送り続ける（サーバーレスでは入れ替わる時期が読めない）。`ADMIN_ALERT_EMAIL` を設定できる項目に足した。**`createConfiguredAiProvider` を用意した** — `createAiProvider()` を引数なしで呼ぶと `process.env` を見るため、覚えていなくてよい入口を置く。`APP_BASE_URL` は環境変数のまま（画面へ入る前に要る） |
| E-4 | 完了 | #65 | 2026-08-09 | STEP 1 の判定・記録・説明文。**判定にAIの出力を一切使わない**（`judgeGenre` はAIの引数を持たない。CONTENT_PLANNING 1.1）。**AIが落ちても判定と記録は残る** — 逆にすると「止まったから通す」が始まる。**検索上位が空なら判定せずに落とす**（取得できないことを理由に停止条件を飛ばさない）。差し戻し回数はDBの履歴から数える（申告を信じない）。**終了した案件を数えない**。`OVERRIDDEN` は差し戻しに数えない。**`audit_logs` への記録は H-11**（Q-018）。`GENRE_REVIEW` `GENRE_SUGGESTION` を場面へ追加。**ジョブへの登録はまだ**（STEP 1〜4 が揃う E-9 で繋ぐ） |
| E-5-schema | 完了 | #66 | 2026-08-09 | マイグレーションのみ（DATA_MODEL 9章）。`affiliate_offers.blog_posting_prohibited`。**`deny_conditions` の文言から機械判定しない**（表記が揺れ、黙って通すのも黙って落とすのも起きる）。ADMIN が設定する。**既定は `false`** — `true` にすると設定されるまで全案件が足切りされ、STEP 2 が常に0件になる。**使う判定は E-5** |
| E-5 | 完了 | #67 | 2026-08-09 | 足切り6項目・100点満点スコア・上位3件採用。**AIに聞くのは検索需要の3値だけ**で、`HIGH/MEDIUM/NONE → 15/8/0` の写像はコード側の定数（AIが `score` を返しても使わない）。**未評価の案件は0点ではなく足切り** — 0点にすると落選の理由が「測っていない」から「質が低い」に化ける。**足切りされた案件も内訳を残す**（なぜ採用されなかったのかに答えられる）。**足切りされる案件にAIを聞かない**（採用しないので費用が無駄）。同点はIDで並びを固定（再実行で構成表が変わらない）。**差し戻しの実行は E-9**（ここでは `needsGenreReview` を返すまで） |
| E-6 | 完了 | #68 | 2026-08-09 | 記事数は「案件数×2＋1」（上限10）。**種類と本数はコードが決め、AIは文言だけ付ける**。**枠との一致（件数・`slotId`）を確かめてから保存する** — 確かめないとAIが増減させた構成表がそのまま通る。**キーワードの重複を落とす**（自分の記事どうしで食い合う）。**上限に掛かったら案件ごとの記事から削り、比較記事は必ず残す**。**AIが呼べなければ失敗させる**（空のタイトルで構成表を作らない）。**実行のたびに新しい版**（前の構成表を書き換えない）。1トランザクションで入れる（半端な構成表を残さない）。`publish_priority` は暫定で、付け直すのは E-9 |
| E-7 | 完了 | #69 | 2026-08-09 | 検索意図 → キーワード → 重複解消 → リンク割り当て。**リンク先が `AFFILIATE` だけであることを保存の直前に検査する**（完了条件。手作業では30本中9本で誤った）。**比較記事もリンク先にしない**（種別が `AFFILIATE` でない。例外を作ると各所で判断することになる）。**キーワードの重複はコードで正規化して突合**（NFKC＋空白畳み＋小文字）。**差し替えても直らない候補は落とす**。**AIが作った収益記事IDを使わない**。**重複した項目だけ差し替えさせる**（全体を作り直させない）。被リンク3本未満は返すだけで、判定は E-8。**D-8 の穴は塞げなかった**（`affiliate → content-planning` が循環する。Q-020 → D-11） |
| D-11 | 完了 | #70 | 2026-08-09 | `affiliate_links.blog_id` と複合外部キー（Q-020）。**D-8 で残した穴を塞ぐ** — 他人の記事IDを紐づけたリンクが作れなくなる。**アプリ層では塞げなかった**（`affiliate → content-planning` が循環する）ので、C-6 と同じく制約をDBへ置いた。**マイグレーションと実装を分けられない**（`blog_id` は NOT NULL）。記事側の `ON DELETE` は `SET NULL` → `CASCADE`（複合外部キーの `SET NULL` は NOT NULL 列と両立しない。**実際の挙動は変わらない**） |
| E-8 | 完了 | #71 | 2026-08-09 | SPEC 9.2.6 の全項目＋DATA_MODEL 4章の2・SPEC 2.2 を判定（8項目）。**1つでも欠ければ不合格**（「だいたい通っている」を返さない）。**3回で収束しなければ例外** — 暫定的な構成表を承認依頼へ送らない。**試行のたびに `planning_runs` に残す**（再生成の回数がプロンプト改善の指標）。**やり直しは版を増やして作る**（前の構成表を書き換えない）。判定は**保存された値**で行う（組み立ての途中の値を見ない）。比較記事は流入3本の対象外。公開週が未割り当てなら週の判定をしない（付けるのは E-9）。**ジョブへの登録は E-9** |
| E-9 | 完了 | #72 | 2026-08-09 | 公開順序と週の割り当て。**収益記事が先行**（比較記事も収益扱い）、**集客記事はリンク先の収益記事が出る順**（流入が早く効く）。**週の上限はブログの `weeklyPublishCap`**（SPEC 2.2 の4本は絶対の上限）。**上限が低いと収益記事が2週に収まらない** — SPEC 9.2.7 の「1〜2週目」は週4本前提の書き方で、守るのは「先行する」ほう（上限を破って詰め込まない）。同じリンク先なら構成表の順で並びを固定。**`PLAN_GENERATION` ジョブを登録した**（E-1 以来はじめてのハンドラ）。**`user_id`/`blog_id` はジョブの列から取り、入力からは取らない**（入力を信じると他人のブログの構成表を作れる） |
| E-10 | 完了 | #73 | 2026-08-09 | 記事本文の生成。**入口は `contentItemId` だけ** — タイトルを渡して1本作る関数を置かない（単体生成モードを作らない。完了条件）。**内部リンクは `outbound_link_item_ids` から渡す**（AIに選ばせない）。**プロンプトに書いた制約を受信後に検査する**（CONTENT_PLANNING 7.2）— 本文から `href` を実際に抜き出して照合、PR表記の有無、事実IDの照合。**版は上書きしない**。**`READY_FOR_REVIEW` にしない**（E-12 の事実チェックと E-13 の禁止表現を通っていない）。JSON-LD はこの時点では空（E-11 で組み立てる）。`ARTICLE_GENERATION` ジョブを登録。**実APIキーでの生成品質は未確認** |
| E-11 | 完了 | #74 | 2026-08-09 | アンサーカプセル・FAQ・JSON-LD。**結論を置く位置をAIに任せない** — `<p class="answer-capsule">` を本文の先頭に挿し、本文の `<h1>` を禁じる（タイトルがH1なので本文の先頭＝H1直後。完了条件）。**80〜120字はコード字数で検査し、範囲外なら作り直す**（CONTENT_PLANNING 7.2）。字数は `String.length` ではなくコードポイントで数える（サロゲートペアを2字と数えない）。FAQ は3〜5件かつ疑問形。**JSON-LD はコードで組み立てる**（`AFFILIATE` → FAQPage + Review、それ以外 → FAQPage）— AIに作らせず、組み立て後に `JSON.stringify`/`JSON.parse` で構文を検証し、失敗したら記事を保存しない。**`Review` に `reviewRating` を入れていない**（評点の出どころが無い。Q-021 で未解決＝リッチリザルトの対象外）。**作り直した試行の費用も記録する**（CONTENT_PLANNING 9。E-10 では最終試行しか記録していなかった）。**案件の `name`/`facts` を生成の入力へ渡すようにした**（E-10 では空だった。照合は E-12）。**実APIキーでの生成品質は未確認** |
| E-12 | 完了 | #75 | 2026-08-09 | 事実チェック。**AIには「何を主張しているか」だけを訊き、照合はコードで行う**（CONTENT_PLANNING 8.1）— 「この主張は正しいですか」と訊けば「正しいです」と返るだけで、確かめたことにならない。照合は2条件（facts の記述が主張文に現れる／主張の数値がすべて facts にある）で、後者が完了条件の「**facts外の数値を検出**」。**照合できなかったものを「確認済み」に倒さない** — 言い換えを文字列一致で追えないため、未確認として承認者に見せる側へ倒す。`EXPERIENCE` の照合先は **`usableFirstPerson = true` の `persona_facts` だけ**（D-6 の制限を無効化しない）。**`GENERAL` に照合先は無く常に未確認** — SPEC 9.7 の `WARNING`（「GENERAL のみ未確認」）が想定している状態そのもので、**実運用ではほとんどの記事が `WARNING` になる**。**生成すると必ず事実チェックまで走る**（飛ばす経路を作らない。呼び忘れた記事が `NOT_CHECKED` のまま承認画面で「問題なし」に見えるのを防ぐ）。`FAILED` は `isApprovable` が `false` を返す。**`facts.updatedAt` が無ければ古い扱い**にしたため、現状すべての収益記事が `WARNING`（Q-022）。**E-2 のキー形式がアンダースコアを弾いていた**のを直した — CONTENT_PLANNING 1.4 が固定した9個のうち7個が登録できなかった。**実APIキーでの抽出精度は未確認** |
| E-13 | 完了 | #77 | 2026-08-09 | 禁止表現・リスクフラグ・PR表記。SPEC 9.6 が挙げた禁止事項を**受信後にコードで探す**。フラグの形は DATA_MODEL 132 のとおり（`code` / `severity` / `message` / `excerpt`）。**`error` だけが承認を止める** — `warning` で止めると「ランキング」の一語で記事が進まなくなり、**判断は承認者に残す**（SPEC 1.1「LINE承認型」）。`error` は PR表記欠落・NG表現・高リスク助言（薬機法/金商法）の3つ。**利用者が挙げた `ngExpressions` は `error`** — 本人が明示した語に「文脈によっては良い」の余地は無い。**タグの中は検査しない**（`<a href="...ranking...">` を本文として拾わない）。**同じ `code` は1件にまとめる**（同じ指摘が並ぶと他が埋もれる）。**生成すると必ず検査まで走り**、修正依頼（F-6）のあとに単独でも掛け直せる。承認へ送ってよいかの判定は **`canSendToApproval` 一本**（事実チェックとフラグの両方を見る。片方だけの経路を作らないため、E-12 の `isApprovable` は `factCheckAllowsApproval` へ改名して「半分」であることを明示した）。**語彙で拾える範囲しか拾えない** — 言い換えは追えず、取りこぼしを前提にした仕組み |
| F-1-schema | 完了 | #78 | 2026-08-10 | `approvals.article_version_id` に一意制約。**同じ記事の版を二度提案できない。** 提案はLINE通知を伴うため、二重に作ると同じ記事の確認依頼が2通届く。SPEC 8.3 の「同一提案を連続通知しない」を**通知の側で数えるより、作れないようにする**（C-6 と同じ筋）。**状態を問わない全体の一意**にしたのは、見送られた版を作り直さないため — 修正依頼のときは E-10 が `version_no` を増やした新しい版を作るので、再提案は常に別の版になる。部分インデックス（`WHERE status IN (...)`）にしないのは、Prisma が `WHERE` 付きインデックスを表現できず差分検査に引っかかるため |
| F-1 | 完了 | #79 | 2026-08-10 | 提案の優先順位算出（3ブログ横断）。**点の付け方は仕様に無いため判断で置いた**（Q-024）— 定数は名前付きで1箇所にまとめてあり差し替えられる。中心は **E-9 の `publish_priority` をそのまま使う**こと（収益記事の先行も週の割り当ても既に入っており、同じ判断を二度しない）。そこへ**待たせているブログを上げる**（未提案 +30、経過日数×2）— **1日1件しか送れないため**（SPEC 8.3）、これが無いと3ブログのうち1つだけが進み続ける。**未回答の提案があるブログは下げる**（-25×件数）。リスクの減点は公開順序1つ分（5点）より小さく抑えた（検査結果で構成表の順序が崩れないため）。**提案理由はコードで組み立てる** — AIに書かせると「点は低いのに理由は強い」記事が生まれる。候補の判定は **`canSendToApproval` 一本**（E-12・E-13）で、SQLに条件を書き直していない。**「作る」と「送る」を分けた** — 送信は F-2、件数制御は F-3。送信に失敗した提案が消えないため。記事の状態を `READY_FOR_REVIEW` へ進めるのは**提案ができてから**（先に進めると、作成に失敗した記事が「承認待ち」に見える）|
| F-2 | 完了 | #80 | 2026-08-10 | LINE通知送信。**二重通知を2段で止める** — (1) 送る前に `sent_at` を立てる（`updateMany` の条件に `sent_at IS NULL` を入れるので、同時に2回走っても片方しか押さえられない）、(2) `x-line-retry-key` に承認IDを渡して LINE 側でも落とす。**「立てたが送れなかった」は起こりうるが、提案は承認一覧（F-4）に残る** — SPEC 8.3 が禁じているのは重複通知のほう。**宛先は押さえる前に確かめる**（押さえてから宛先が無いと分かると、送っていない提案に印が残る）。**`ACTIVE` 以外には送らない**（停止した利用者に提案が届くと、止めたはずのものが動いて見える）。`line_user_id` は `findNotificationTargetForUser` が唯一の経路で、`AppUser` には載せない（SPEC 14.2）。**文面はコードで組み立てる**（SPEC 8.2）— 毎回同じ形で届くこと自体が「確認時間：約3分」を支えている。**1日の件数はここで決めない**（F-3）。既定は1件で、無制限にすると初回に30件まとめて届く。`LINE_CHANNEL_ACCESS_TOKEN`（秘密）と `LIFF_BASE_URL` を管理画面の設定に追加（H-7 の仕組みに乗せた）。**実機（LINEアプリ）での受信は未確認** |
| F-3 | 完了 | #81 | 2026-08-10 | 通知数制御。**「3ブログ合計で制限」は、数える単位が利用者だということ** — ブログごとに1日1件にすると、3ブログ持つ人には1日3件届く。数えるのは `approvals.sent_at` を利用者で絞ったもの。**JSTの暦日で数える**（UTCで数えると日本の1日が2日にまたがり、夜に届いた1件が翌日の枠を1つ消す）。上限は `monitor_profiles.max_daily_proposals`。**読むときに1〜2へ丸める** — DBは3以上も入る整数の列で、設定画面の検証だけに頼ると直接書き換えられた値がそのまま効く。**0を許さない**（0にすると提案が永久に届かない。止めたいなら利用者を `PAUSED` にする）。**緊急通知は別枠** — `sendEmergencyNotificationForUser` は `approvals` の行を作らないので、数えようがない（作らないことがそのまま別枠になる）。中身は H-3。**呼び出し側の指定は上限を超えられない**（「それ以下に絞る」ためだけに効く）。**`notification_days` / `notification_time` は誰も読んでいない**（Q-025） |
| F-4 | 完了 | #82 | 2026-08-10 | LIFF承認一覧。完了条件「他ユーザーの承認を開けない」は、**画面から他人のIDを指定する経路を作らない**ことで満たす — `GET /api/approvals` はセッションの利用者だけを返し、絞り込みのクエリを受け付けない（受け付けてから弾く形にすると、弾き忘れが穴になる）。**`EXPIRED` は SPEC 6.1 の4タブに無い** — どこにも置かないと画面から消えるため「見送り」に入れ、**行の表示は「期限切れ」のまま分けた**（「自分で見送った」と「期限が切れた」を同じに見せると判断の記録として使えない）。**`VIEWED` は承認待ちに残す**（開いただけで一覧から消えると「あとで答えよう」と閉じた提案を見つけられない）。**確認が要ることを開く前に行へ出す** — 事実チェックの `WARNING` と表現の指摘の件数（E-12・E-13）。一覧は全件返して画面で分ける（3ブログでも数十件で、タブを切り替えるたびに問い合わせるより速い）。**詳細画面への導線は F-5**（飛び先が無い状態でリンクだけ置くと、押せて何も出ない）。**タブの並べ分けは `src/app/liff/_lib/` に置く** — `approvals` モジュールの `index.ts` は `repository.ts` 経由で Prisma を引き込むため、クライアントコンポーネントから読むと ブラウザ向けの束に `node:dns` が混ざる（MODULE_RULES 4） |
| F-5 | 完了 | #83 | 2026-08-10 | LIFF承認詳細。完了条件「未確認事実とリスク警告が表示される」。**記事本文をページのDOMへ入れない** — 本文はAIが書いたHTMLで、E-13 の検査は表現を見るものでスクリプトを落とさない。`dangerouslySetInnerHTML` だと `<img onerror=...>` ひとつでセッションを触られる。**`sandbox` を空にした `iframe`** に入れ、スクリプトも遷移もブラウザに止めさせる。確かめるべきアフィリエイトURLは SPEC 6.1 が別項目として並べているので、**本文中のリンクを踏ませる必要が無い**。**他人のものは404で「無い」と区別しない**（区別するとIDを変えながら叩くだけで存在が分かる。SPEC 14.1）。開くと `PENDING → VIEWED`。**`viewed_at` は最初に開いた時刻のまま**（「いつ気づいたか」の記録で、開くたびに更新すると意味が変わる）。**見た記録は最後に付ける**（途中で落ちたら「開いた」ことにしない）。`params` はページ（Server Component）で解き、クライアント側は文字列のIDだけを受け取る — Promise をクライアントで `use()` すると描画がサスペンドの扱いに左右されて試験しにくい。**操作（承認・修正依頼・見送り）は F-6**。`unverified_claims` は現状の形のまま読んでいる（Q-023 未解決） |
| F-6 | 完了 | #84 | 2026-08-10 | 承認・修正依頼・見送りAPI。完了条件「トランザクションと冪等性を持つ」。**同じ答えを二度送っても成功する** — LINEの通信は落ちるしモニターは二度押す。二度目にエラーを返すと「押せていない」と思って三度押す。**違う答えは受け付けない**（409）— 承認した提案を見送りへ変えられると、何を承認したのか分からなくなる。**承認の記録・`revision_requests` の行・記事の状態を1つのトランザクションで決める**（片方だけ残ると承認済みなのに `PLANNED` の記事が生まれる）。`content_items` は content-planning の `setItemStatusInTx` 経由（MODULE_RULES 1）。状態を `where` に入れているので、同時に2回来ても片方しか通らない。**F-5 の「GETで既読にする」を取り消した** — SPEC 13.6 は `POST /view` を別に定めており、読み取りで状態が変わると一覧の先読みで「開いた」ことになる。承認→`APPROVED`、見送り→`REJECTED`、修正依頼→`PLANNED`（作り直すため。`article_versions` は上書きしない）。**`FREE_TEXT` は本文必須**（何を直すか分からない依頼を残さない）。**「後で確認」のボタンは置かない** — 何もせず閉じることがそれ。**答えたあとはボタンを消す**（押せるままだと409を見ることになる）。投稿は F-7 |
| F-7 | 完了 | #85 | 2026-08-10 | 承認からWordPress投稿ジョブ連携。完了条件「承認→下書き投稿がE2Eで成功」。**承認とジョブを同じトランザクションで決める**（`enqueueJobInTx`）— 承認したのにジョブが無いと記事が永久に投稿されず、承認が巻き戻ったのにジョブだけ残ると承認していない記事が投稿される。**WordPress を呼ぶのはジョブのハンドラ**（承認の応答を外部サービスの応答時間に縛らない）。**どの版を投稿するかを入力から取る** — 最新の版を拾うと、承認後に作り直された版が承認なしで公開されうる。**下書きのまま置く**（公開はモニターが WordPress 側で行う。SPEC 7）。承認を経ているので `approvedOverwrite`（C-5）。**投稿できてから `POSTED` にする**（先に進めると失敗した記事が投稿済みに見える）。冪等キーは記事ID。二度承認してもジョブは1件、ジョブを再実行しても内容が同じなら WordPress を呼ばない（C-5 の抑止）。**ハンドラの登録を `createJobHandlers(deps)` に変えた** — 差し替えられるのは外部への呼び出しだけで、種類と処理の対応は変えられない。**実WordPressでの投稿は未確認** |
| H-1 | 完了 | #86 | 2026-08-10 | 招待フロー（参加の承認と利用停止）。**実験への参加は「登録できた」ではなく「ADMIN が認めた」で決まる** — `users.status` の既定は `INVITED` で、LIFF から登録した時点でその状態になり、`INVITED` のままではアプリを使えない（`isActiveUser`）。管理画面から `ACTIVATE` / `PAUSE` / `RESUME` を行う。**遷移は表にして1箇所に置いた** — 条件分岐に散らすと「停止中を承認できてしまう」ような穴が後から入る。**`INVITED` を `RESUME` で `ACTIVE` にしない**（認めるのは `ACTIVATE` だけ）。**`WITHDRAWN` は動かせない**（退会は H-4）。**退会のボタンを停止と同じ並びに置かない** — 戻せない操作を混ぜると、停止のつもりで退会させる事故が起きる。**`ADMIN` は対象にしない**（ADMIN 同士で停止し合えると誰も管理画面に入れなくなる）。二度押しは成功（冪等）。**「招待」そのもの（声をかける側）はアプリの機能にしていない**（Q-026）。**ADMINの介入は `audit_logs` に残していない**（H-11 が未着手。Q-018） |
| H-11 | 完了 | #87 | 2026-08-10 | 監査ログ（`audit_logs`）。Q-018 で登録したタスク。**記録するのは「普通ではないことが起きた」場面だけ** — SPEC 9.2.2 の「承知で進める」（E-4）と ADMIN の介入（H-1）。正常系を全部残すと、後から見たときに異常が埋もれる。**`action` を自由入力にしない**（列は `text` だが、書く側が好きな語を入れると集計できない）。**ADMIN の介入は同じトランザクションで記録する** — 記録だけ残って状態が戻る、あるいはその逆を作らない。**単独の記録（`recordAudit`）は失敗しても投げない** — 監査ログは後から辿るためのもので、書けないことを理由にモニターの操作を失敗させると本末転倒。書けなかったことはログに残す。**`metadata` に氏名や `line_user_id` を入れない**（SPEC 14.2）。`updateMonitorStatusForAdmin` に `actorUserId` を必須で足したので、**誰が介入したかを書かない呼び出しは型で通らない**。Q-008 の `site_url` 変更用に `BLOG_SITE_URL_CHANGED` を用意したが、**その救済手順自体は未実装** |
| H-4 | 完了 | #89 | 2026-08-10 | 退会・停止処理。完了条件「物理削除せずCLOSED。データエクスポートができる」。**行を消さない**（SPEC 13.2）— Phase 0 は10名がどこまで続いたかを見る実験で、抜けた人の記録を消すと「10名中何名が続いたか」が数えられなくなる。ブログは `CLOSED` にし、**スロット番号は残す**（Q-008）。**利用者の状態・ブログ・監査ログを1つのトランザクションで決める**（「退会したのにブログが動いている」を作らない）。**退会は戻せない**（H-1 の遷移表が `WITHDRAWN` を動かさない）。**`/status` とは別の入口**にした — 戻せない操作を状態変更の引数の一種にすると、値を間違えたときの結果が重すぎる。**エクスポートに秘密を入れない**（SPEC 14.2）— WordPress の認証情報・`line_user_id` は対象外で、実際に入らないことを試験で確認。**`CLOSED` のブログも含める**（外すと退会後に空のファイルが出てくる）。記事は**本文まで**入れる。**案件は退会後に空になる**（`listOffersForUser` が `CLOSED` を弾くため）— 持ち出す価値があるのは記事のほう |
| H-5 | 完了 | #90 | 2026-08-10 | バックアップと復旧手順（`docs/BACKUP.md`）。**先頭に「鍵が無いと戻らないものがある」を置いた** — DBだけ戻しても `ENCRYPTION_KEY` を失っていれば WordPress の認証情報・Google の refresh token・APIキーは二度と読めない。**鍵はDBのバックアップとは別の場所に、別の手段で保管する**（同じ場所だと両方まとめて失う）。**障害の最中に読む手順書として書いた** — 判断（どこまで戻すか）→ 手順 → 繋ぎ替え → 確認の順。`pg_dump --no-owner --no-privileges`（復旧先のロール名が違ってもそのまま入る）、**ダンプは暗号化して保管**（`line_user_id` と記事本文が入る。SPEC 14.2）。**`prisma migrate reset` を使わないと明記**（全データが消える）。**復旧後は `RUNNING` のジョブを `QUEUED` へ戻す**（ジョブは冪等なのでやり直しても二重に起きない）。**「一度も戻したことのないバックアップはバックアップではない」** — 月1回の復旧確認と、その結果を HISTORY に残すことを手順に含めた。**運用先のDBが未確定**なので、自動バックアップの保持期間・PITRの可否・取得時刻は書けていない（Q-028） |
| H-3 | 完了 | #91 | 2026-08-10 | エラー通知（接続切れ・リンク切れ・案件終了）。**同じことを毎日言わない** — 通知はジョブとして積み、**冪等キーに「ブログ・種類・JSTの日付」を入れる**（C-4）。同じ日の同じ指摘は1回だけ。**直っていなければ翌日また届く**（直すまで思い出させるのは正しい）。**落ちたことと切れたことを分ける** — 404/410 だけを「消えた」とし、401/403/5xx は一時的な失敗として通知しない（ASPのメンテナンスのたびに緊急通知が飛ぶのを避ける）。**一度も接続を試していないブログは対象外**（準備中のブログに毎日「接続が切れています」と送らない）。**確かめるのは `landing_page_url`** — `affiliate_url` は踏むとクリックとして計上されうる。**検出はその場で送らずジョブにする**（ブログごとにHTTPを叩くため、途中で落ちると「一部だけ送った」状態になる）。`LINK_CHECK` と `LINE_NOTIFY` のハンドラを登録。**リンク切れの状態は保存していない**（Q-029） |
| G-5-schema | 完了 | #92 | 2026-08-10 | `metrics_daily` の一意インデックスを `NULLS NOT DISTINCT` にする。**PostgreSQL は既定で NULL 同士を「違う値」として扱う** — 記事に紐づかない行（`content_item_id IS NULL`）は、同じブログの同じ日でも何行でも入っていた。手動の収益入力（G-5）はブログ単位で記録するため、ここがそのまま効く。**重複した行は集計（G-6）で二重に数えられ、しかも気づけない。**PostgreSQL 15 以降の `NULLS NOT DISTINCT` で塞いだ（C-6 と同じ筋で、アプリ側の書き方に頼らない）。**インデックスとして張り直した** — 制約として作り直すと Prisma のスキーマとの差分検査に引っかかる |
| G-5 | 完了 | #93 | 2026-08-10 | 手動収益入力（週次）。完了条件の「**0件を1操作で記録できる**」が要点 — 0件の週に数字を2つ入れるのは面倒で放っておかれ、**記録に穴が空くと「成果が無かった」のか「報告されなかった」のかが分からなくなる**。ボタン1つで0を記録でき、入力欄より前に置いた。**行が無い＝未報告、行があって0＝0件と報告済み**として読めるようにし、画面でも区別して見せる。週は**JSTの月曜始まり**でその週の月曜の行に入れる（週の途中の日付だと同じ週で別の行になる）。**ブログ単位で記録**（記事ごとの分解はモニターにさせない）。同じ週に入れ直すと上書き。**0件なのに報酬があれば落とす**（打ち間違い）が、**成果があって0円は通す**（承認待ちの案件がありうる）。`upsert` は使えない（Prisma が複合一意の `where` に `null` を受け付けない）ため引いてから書くが、**同時に2回来ても重複しないのは G-5-schema の制約のおかげ**。SPEC 6.1 の集計項目（検索表示・AI費用など）は**空の枠を並べない** — 自動収集（G-2・G-6）が未実装で、並べると「取れているのに0」と読める |
| G-1-schema | 完了 | #97 | 2026-08-11 | `search_console_connections.refresh_token_encrypted` を落とす。Q-030 で連携方式を**サービスアカウント**に決めたため、**ブログごとの資格情報が存在しなくなった**。`webmasters.readonly` は Google の機密スコープで、審査を通さない場合の公開状態は「テスト」になり、**その状態のリフレッシュトークンは7日で失効する** — 3か月の実験では日次の取得ジョブ（G-2）が週明けに落ちてデータに穴が空く。加えて **OAuth のスコープはプロパティ単位で絞れない**ため、モニターのGoogleアカウントにある全プロパティが読める状態になる。サービスアカウントなら実験対象のブログだけを指定できる。残った行が持つのは `property_url` と接続状態だけ。**NOT NULL の列を使わないまま残さない** — 埋める値が無く、後から読む人に「入っているはず」と読ませる。DATA_MODEL 7章（暗号化対象）と BACKUP.md 0章の該当行も直した |
| G-1 | 完了 | #98 | 2026-08-11 | Search Console 連携（**サービスアカウント**・Q-030）。完了条件「ブログ単位で連携でき、トークンが暗号化される」のうち、暗号化されるのは**ブログごとのトークンではなく鍵1つ**（`app_settings.value_encrypted`）。ブログごとに決まるのは**どのプロパティを見るか**で、`search_console_connections` はその対応を持つ。**保存できたで終わりにしない** — モニターが Search Console 側でアドレスを追加していなければ何も取れないため、保存と同時に問い合わせて `connection_status` に残す（取れないまま「連携済み」に見えると G-2 が動くまで誰も気づけない）。**確かめられなくても行は残す**（先にURLを入れ、あとから権限を渡す順序がありうる）。**プロパティのURLは2つの形を両方受ける** — `sc-domain:` とURLプレフィックスがあり、**モニターがどちらを作ったかはこちらで決められない**。片方しか受けないと「合っているのに弾かれる」。URLプレフィックスは末尾の `/` まで含めて一致するため補う。**404 と 403 を同じに倒す**（どちらもモニターがすることは同じ）が、**所有確認前（`siteUnverifiedUser`）は別扱い**（頼むことが違う）。**Google側の一時的な失敗を `FAILED` にしない**（H-3 と同じ筋）。鍵は**1つのJSONとして保存**する（`client_email` と `private_key` を分けると片方だけ差し替えられて食い違う）。**要求するのは読み取りスコープだけ**（鍵が漏れてもプロパティ設定を変えられない）。外すときは**行ごと消す** — WordPress（Q-007）と違い、繋ぎ直しは誤りではない |
| G-2 | 完了 | #99 | 2026-08-11 | Search Analytics 取得ジョブ。**昨日ぶんだけ取らない** — Search Console のデータは遅れて確定するため、昨日ぶんを取って保存で終わりにすると**取りこぼしたまま二度と取り直さない**。毎回**直近5日を取り直して上書き**する（C-4 のとおり何度動かしても同じ結果）。**ブログ全体をページの合計で代用しない** — 1回の検索で複数ページが出れば表示は重複して数えられ、平均掲載順位は**加重平均なので足せない**。`date` と `date`+`page` で**2回問い合わせ**、全体はGoogleに集計させた値をそのまま入れる。**触るのは自分の3列だけ**（同じ行に手入力の成果が入っている・G-5）。「**API上限を考慮**」で効くのは行数で、1回25,000行・`startRow` で続きを取り、**満たなければそこで終わり**（空の応答を待たない）。無限に回さない歯止めを置き、達したら**黙って打ち切らず記録する**。失敗は**やり直して直るかを持つ** — 429・5xx は再試行、403・404 は何度やっても同じで、再試行はモニターに何も知らせないまま回数を消費する。ジョブは**ブログごとに1件**（1件で3ブログ回すと2つ目の失敗で1つ目までやり直す）、冪等キーはブログ＋JSTの日付。**未連携・読めない状態・閉じたブログは叩かない**。ページのURLは**末尾の `/` と大文字小文字を揃えて**突き合わせる（揃えないと記事があるのに0件として並ぶ）。日付は Q-005 の決定どおり**返ってきたものをそのまま**入れる |
| G-3 | 完了 | #100 | 2026-08-11 | インデックス状況の取得（URL Inspection）。**別ジョブにする理由は上限の枠が違うこと** — Search Analytics は1ブログ1日数回だが、こちらは**記事の本数だけ呼ぶ**（1プロパティ1日2,000回）。同じジョブに入れると、**上限に当たったときに取れていたはずの検索データまで巻き戻る**。**「分からない」を `false` に倒さない** — `PASS`→`true`、`FAIL`→`false`、`PARTIAL`/`NEUTRAL`/読めない応答は**書かない**。倒すと「調べたが載っていない」と区別できず、インデックス率（SPEC 11.2）が実際より低く出る。**今日ぶんを既に記録した記事は呼ばない**（枠を二重に使わない）。1回の実行の本数に上限を置き、枠に張り付かせない。`indexed` 以外の列に触らない（同じ行に G-2 の検索データと G-5 の手入力がある）。**日付はJSTの今日** — 索引の有無は「いま尋ねた結果」で、過去の日に遡って当てはまらない。**この作業中に `metric_date` が意図した暦日より1日前で保存されることを実測で見つけた（Q-031）。原因は G-5 から入っており、G-3 では直していない**（直すと2つの基準が同時に存在するため） |
| Q-031修正 | 完了 | #101 | 2026-08-11 | `metrics_daily.metric_date` が意図した暦日より1日前で保存されていた不具合を直す。`date` 型の列に `startOfJstDay`（JSTの00:00を表すUTCの瞬間）を渡していたため、**UTCの日付部分が取られて前日になっていた**。`jstDateColumn`（`Date.UTC(y,m,d)`）を足し、書き手3か所（G-5・G-2・G-3）を付け替えた。**`startOfJstDay` は残す** — `timestamptz` の範囲検索ではそちらが正しく、消すと今度は範囲検索が9時間ずれる。**PostgreSQL が実際に何を持っているかを試験で固定**した（`select metric_date::text`）— JSの `Date` を見るだけではまた通り抜ける。**本番にまだ1行も入っていない今が最も安い**（実験開始後は古い行と新しい行で基準が変わる）。G-6・G-7 はこの列を読むため、ずれたまま集計を積むと直す範囲が広がっていた |
| G-6 | 完了（一部未着手） | #102 | 2026-08-11 | `metrics_daily` の日次集計。SPEC 10.2 の19項目を1つずつ当たり、**17項目は保存先がある**ことを確かめたうえで、**生イベントから数える必要があるもの**（アフィリエイトクリック・AI検索経由）を実装した。条件（ジャンル・競争レベル・戦略など）は**日ごとに写し取らない** — ブログの属性として既にあり、SPEC 10.3 が「条件の記録は行い、集計はSQLで実施する」と定めている。**生イベント（`link_clicks`）を残したまま数え直す**ので何度動かしても同じ結果になり、G-4 で対象ドメインを足したあとに数え直せば過去の日も正しくなる。**記事に紐づかないクリックも全体には数える**（落とすと「記事の合計＝ブログ全体」に見えて実際より少なく読める）。**触るのは `affiliate_clicks` と `ai_referrals` の2列だけ**（同じ行に G-2・G-3・G-5 の値がある）。**クリックが無いブログに空行を作らない**が、既にある行は0で更新する（「数えた結果0」を残す）。ジョブは**外部に依存しない**ので検索データの取得（G-2）と分けた — Google が落ちていても数えられる。**残る2項目（広告クリック・PV）は測る手段そのものが無く、Q-032 に記録した**（広告クリックは SPEC 20.2 の未決項目6そのもの、PV は GA4 のタスクが TASKS に無い） |
| G-7 | 完了 | #103 | 2026-08-11 | 管理ダッシュボード（実験の集計）。**ジャンル別・戦略別・ブログ別をSQLで取る**（完了条件）。**集計を `src/app/admin/` に置いた** — `metrics_daily`・`blogs`/`genres`・`experiment_groups`・`ai_usage_logs`・`wordpress_posts` を同時に結合するため、どれか1つのモジュールに置くと MODULE_RULES 1 に反する。MODULE_RULES 3 の「上位へ寄せる」に当たり、TASKS の主な変更先も `src/app/admin/`。**`metrics_daily` はブログ全体の行だけを足す**（`content_item_id IS NULL`）— 記事ごとの行も足すと**同じクリックを二重に数える**。**指標とAI費用を1つのクエリで join しない** — 行数の掛け算で数が膨らむため、CTEで別々に集めてから結合する。**未設定を「その他」に混ぜない** — ジャンルや戦略が付いていないこと自体が運営の直すべき状態。**閉じたブログも数える**（途中でやめたことも実験の結果・H-4）。**測っていない列を出さない**（広告クリック・PV。Q-032）— 0として並べると「測ったが0だった」と読めるため、列そのものを出さず、注記を画面に置いた。SPEC 10.3 のとおり**実験グループの作成・割当・比較のUIは作らない**（読むだけ） |
| G-4 | 完了 | #94 | 2026-08-10 | AI検索サービス経由の流入の判別。**対象ドメインは配列（`ai-referral.ts`）に並べ、環境変数 `AI_REFERRAL_EXTRA_DOMAINS` で足せる**（完了条件）。**足すだけで、消せない** — 既定を上書きできる形にすると、誤って空にしたときに判別が全て `false` になり「AI経由の流入が無い」と読めてしまう。判定は**末尾一致だが区切りを確かめる** — 単純な `endsWith` だと `notchatgpt.com` が `chatgpt.com` に一致する。`Referer` が無い場合は `false`（異常ではなく「判別できなかった」）で、SPEC 11.4 のとおり**完全値として扱わない**（表記は「判別可能なAIサービス経由流入数」）。判別は書き込み時に `link_clicks.is_ai_referral` へ持つが、**元の値は `referrer_host`** なので、ドメインを足したあとに `recountAiReferrals()` で**過去のクリックを数え直せる**（`referrer_host` は書き換えない） |

以降のタスクは着手時に行を追加する。
