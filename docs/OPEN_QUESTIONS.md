# OPEN QUESTIONS

判断できない事項を記録する。**推測で実装しない。**

## 形式

```
### Q-001 タイトル
- 発生タスク：
- 状況：
- 選択肢：
- 影響範囲：
- 状態：未解決 / 解決（決定内容と日付）
```

---

### Q-001 アフィリエイトリンクのリダイレクト方式

- 発生タスク：D-8（TASKS.md）
- 状況：自前リダイレクタを後から導入すると、公開済み記事のリンクを全て貼り替えることになる（SPEC 20.1）

  **前提として押さえておくべき事実が2つある。**

  1. **`prisma/schema.prisma` は既に自前リダイレクタを前提にしている。** `affiliate_links` に `code` と `destination_url` があり、`link_clicks` が生イベントを持つ。TASKS D-8 の主な変更先も `src/app/go/`
  2. **ブログはモニター自身のドメインで動く**（SPEC 1.2）。リダイレクタは我々のアプリのドメインに置かれる。つまり `monitor-blog.example.com` の記事から `app.example/go/<code>` へ**別ドメインを経由**することになる

  ここから、技術的な難しさではなく**ASPの規約と成果計測の扱い**が判断の中心になる。

- 選択肢：

  | | 方式 | クリック計測 | 主なリスク |
  |---|---|---|---|
  | (a) | **自前リダイレクタ**（我々のドメイン。スキーマの想定どおり） | 正確 | ASP規約・リファラー・可用性 |
  | (b) | **各ブログのドメイン上でリダイレクト**（WordPress側に仕組みを置く） | 正確 | 30サイトへの導入と維持。プラグインの開発が必要 |
  | (c) | **直リンクのみ。クリックは計測しない** | 無し | `affiliate_links` `link_clicks` が未使用になる |
  | (d) | **直リンク＋ブログ側でクリックイベントを送る** | 概算 | 広告ブロッカーで漏れる |

- 影響範囲：D-8、E-10（記事本文へのリンク埋め込み）、G-4（AI検索流入の判別）、`affiliate_links`・`link_clicks`、**公開済み記事の全て**
- 状態：**解決（2026-08-07）→ 再決定（2026-08-11）。リダイレクタを各ブログの
  ドメイン配下へ移す。** 案件ごとの切り替え（2026-08-07 の決定）はそのまま残す。
  詳細は本文書末尾「Q-001 の再決定（2026-08-11）」

#### (a) を選ぶ前に確認が必要だったこと

**この判断は、こちらで調べられない事実に依存していた。** 確認をお願いした点は次のとおり。

1. **利用予定のASPが、別ドメインのリダイレクタ経由の掲載を許しているか。** 「登録したサイトに直接リンクを設置すること」を条件にしているASPは珍しくない。違反すると**成果が無効になる**
2. **リファラーが我々のドメインになっても成果が計上されるか。** ASPによっては送客元の判定に使う
3. アプリが停止したときに**30ブログ全ての収益リンクが死ぬ**ことを許容するか

**1つ目が「許していない」なら (a) は選べない。** 実験の目的は「AIが運営するブログが稼げるか」を測ること（SPEC 1.2）であり、**成果が無効になると実験そのものが成立しない**。クリックデータの価値より優先される。

#### 決定内容（2026-08-07）

**回答は「ASPによって異なる。許可しているところもあれば、していないところもある」だった。** この前提では、方式を1つに決めること自体が誤りになる。以下を採用する。

##### 1. 案件ごとに方式を切り替える

`affiliate_offers` に**リンクの出し方**を持たせ、記事本文へ埋めるURLをその値で決める。

| 値 | 本文に埋めるURL | クリック計測 |
|---|---|---|
| `REDIRECT` | `/go/<code>` | 正確（`link_clicks`） |
| `DIRECT` | `affiliate_url` をそのまま | サブID・ビーコン（下記） |

**既定は `DIRECT`（安全側）とし、規約を確認できたASPだけ `REDIRECT` に上げる。** 判断がつかないものを許可側へ倒すと、成果が無効になったときに取り返しがつかない。

**モニターに規約の判断をさせない。** ASP単位の既定値を ADMIN が設定し、案件単位で上書きできるようにする。モニターは技術者でも法務でもない。

##### 2. これなら「後から貼り替え」が起きない

SPEC 20.1 が Q-001 を Phase A 着手前としたのは、**リダイレクタを後から入れると公開済み記事のリンクを全て貼り替えることになる**ためだった。

**切り替えの仕組みを最初から入れておけば、この手戻りは起きない。** 記事生成（E-10）が呼ぶリンク組み立てを `src/modules/affiliate/` の1関数に集約し、`REDIRECT` / `DIRECT` の判断をそこだけに閉じる。後から ASP の規約が変わっても、**変わるのは以後に生成される記事だけ**で、既存記事はそのまま動く。

##### 3. `DIRECT` の案件でも計測を諦めない

**(1) サブID（最優先。全案件で入れる）**

ほとんどのASPは、アフィリエイトURLに識別子を付けられる（サブID・sub affiliate id など、名称はASPによる）。

```
https://asp.example/click?a=xxxx&sub=<slot>-<contentItemId>
```

- **ASPが用意した機能なので規約上の問題が無い**
- **成果（＝収益）を記事単位で分解できる。** Phase 0 の目的は「AIが運営するブログが稼げるか」（SPEC 1.2）であり、**クリック数より成果の紐付けのほうが価値が高い**
- 数値はASPの管理画面から取る。Phase 0 は手入力が許可されている（SPEC 11.5）

**`REDIRECT` の案件にもサブIDは付ける。** リダイレクタはクリックを数え、サブIDは成果を紐づける。役割が違う。

**(2) 発リンクのクリックイベント**

`DIRECT` の案件でクリック数も欲しい場合、リンクは直リンクのまま、クリック時にブログ側から計測用のリクエストを送る。**HTML上のリンクは直リンクなので「リンクの設置」は規約に触れにくい。** 広告ブロッカーで漏れるため概算。

**(3) GA4 の外部リンククリック**

SPEC 11.4 が GA4 に言及している。導入済みなら追加コストが小さい。

##### 4. 混ぜて集計しない

`REDIRECT` と `DIRECT` ではクリック数の意味も精度も違う。**`metrics_daily` に出所の分からないクリック数が混ざると、集計が読めなくなる。** 計測方式を区別できるようにするか、少なくとも案件ごとの方式が後から辿れるようにする。

##### 5. 必要になる作業

| 内容 | 位置づけ |
|---|---|
| `affiliate_offers` にリンク方式の列を追加 | **スキーマ変更（単独タスク・DATA_MODEL 9章）** |
| ASP単位の既定値をADMINが設定する仕組み | Phase 0 は設定ファイルまたはSQLで足りる（SPEC 10.3 と同じ扱い） |
| リンク組み立ての集約 | D-1 または D-8 |
| サブIDの付与 | D-1（案件登録時にURLの組み立て規則を決める） |

D-8 の完了条件「記事内リンクが**自前URL経由**になり、クリックが記録される」は、この決定に合わせて**「リンク方式に従って組み立てられ、`REDIRECT` の案件でクリックが記録される」**へ改めた。

**タスクは D-9（スキーマ変更）を追加し、D-1 の依存に加えた。** 案件登録時にリンクの組み立て規則が決まるため、D-1 の前に列が要る。タスク総数 59 → 60。

#### 補足

- SPEC 11.5 は **Phase 0 の収益を手入力で許可**している。つまり (c)(d) でも収益の記録は成立する。失われるのは「クリック数」であり「収益」ではない
- `affiliate_links` と `link_clicks` は **`REDIRECT` の案件でのみ使う**。`DIRECT` の案件では行が作られない。DATA_MODEL 6章の「方式が確定するまでこの2テーブルは確定版ではない」という但し書きは、この決定をもって外せる
- **SPEC 20.1 は本来「Phase A着手前」に決めるとしていた項目**である。Phase A・B を終えてからの決定になったが、**切り替えの仕組みを D-1 の前に入れるため手戻りは生じない**

### Q-002 ドメイン・サーバー費用の負担者

- 発生タスク：H-1
- 状況：モニター募集条件そのもの（SPEC 20.1）。**実装上の選択肢は無く、費用をどちらが持つかの決定のみ**
- 影響範囲：H-1、H-2、募集要項
- 状態：**解決（2026-08-07）。モニター（利用者）の負担とする。
  2026-08-11 に「各自が自分名義で契約する」ことを追記**

決定内容：

- **ドメイン・サーバーの費用はモニターが負担する。** 運営は負担しない
- 10名×最大3ブログで、ドメインは最大30件。**モニター1人あたり最大3ドメイン＋サーバー1契約**が想定される
- **募集要項（H-1）に明記する。** 参加してから費用が発生することを知る、という状態にしない
- **同意（SPEC 6.1 のオンボーディング 2・3）より前に提示する。** 費用の話は参加可否そのものであり、規約同意の後に出すのは順序が逆

実装への影響：

- **無い。** 費用に関する項目をDBへ持たない（Phase 0 で請求も精算もしない）
- ただし Q-007（接続先の変更禁止）との関係で、**ドメインとサーバーはモニターの資産**である点は変わらない。退会時にブログはモニターの手元に残り、こちらのDBには実測データだけが残る

実験結果を読むときの注意：

- **参加者の属性が偏る。** 「独自ドメインとサーバーを自費で用意できる人」に限られるため、SPEC 1.2 の結果を一般化する際はこの条件を併記する必要がある
- **費用は離脱の理由になりうる。** H-6 の運用で離脱理由を記録するなら、費用を選択肢に含めておくとよい

### Q-003 ソースディレクトリに `src/` を付けるか

- 発生タスク：A-1
- 状況：`SPEC.md` 4.2 のツリーは `src/` 配下に `modules/` `app/` `lib/` `tests/` を置いている。一方 `TASKS.md` の「主な変更先」欄は `lib/env.ts` `modules/auth/` `app/liff/` `tests/integration/` と `src/` を省いた表記になっている
- 選択肢：(a) `src/app` `src/modules` `src/lib` `src/tests`（SPEC 4.2 の表記どおり）／(b) リポジトリ直下に `app/` `modules/` `lib/` `tests/`
- 影響範囲：以降の全タスクのファイルパス、`tsconfig.json` の `@/*` エイリアス
- 状態：**解決（2026-08-06）。(a) を採用。`src/` 配下が正であり、SPEC 4.2 を唯一の正とする。ディレクトリ移動は不要**

決定内容：

- ソースコードは全て `src/` 配下に置く。`@/*` → `./src/*`
- `TASKS.md` の「主な変更先」欄が `src/` を省いているのは記載ミス。以下のとおり読み替える

  | TASKS.md の表記 | 実際のパス |
  |---|---|
  | `lib/env.ts` | `src/lib/env.ts` |
  | `modules/auth/` | `src/modules/auth/` |
  | `app/liff/` | `src/app/liff/` |
  | `tests/` | `src/tests/` |

- **例外：`prisma/` はリポジトリ直下のまま。`src/` 配下へ移動しない**
- `TASKS.md` 本体の表記修正は A-1 のPRに含めず、別PR（`docs/fix-tasks-paths`）で行う

### Q-004 Prisma のメジャーバージョン

- 発生タスク：A-5
- 状況：A-2 の `prisma/schema.prisma` は `datasource db { url = env("DATABASE_URL") }` を持つ。**Prisma 7 はこの記法を廃止**しており、`prisma validate` が `P1012` で失敗する

  ```
  The datasource property `url` is no longer supported in schema files.
  Move connection URLs for Migrate to `prisma.config.ts`
  ```

  Prisma 6.19.3 では検証が通り、26テーブル・30 enum の初期SQLも生成できることを確認した
- 選択肢：
  - (a) **Prisma 6 系に固定する**（A-5 で採用）。`schema.prisma` は無変更で済む
  - (b) Prisma 7 へ上げる。`schema.prisma` から `url` を削除し、`prisma.config.ts` を追加する。A-2 の成果物に手を入れるため、DATA_MODEL 9章に従い単独タスク・単独PRが必要
- 影響範囲：`prisma/schema.prisma`、DB接続の実装（B-1以降）、CI
- 状態：**解決（2026-08-06）。(a) を採用。Phase 0 は Prisma 6 系で進める**

決定内容：

- `prisma@^6.19.3` に固定する。`prisma/schema.prisma` は無変更のまま
- **Phase 0 の期間中は 7系へ上げない。** 7系で得られるもののうち Phase 0 で使う予定のものが無い一方、26テーブルのスキーマがCIで実DBへ適用できている現状を崩すリスクがある
- 移行を検討するのは Phase 0 終了後。実施する場合は `schema.prisma` の変更と `prisma.config.ts` の追加を伴うため、DATA_MODEL 9章に従い単独タスク・単独PRで行う
- 依存の更新でうっかり 7系に上がらないよう、`package.json` の指定は `^6` の範囲に留める

### Q-005 Search Console の日次データをJSTの暦日へどう対応づけるか

- 発生タスク：A-7（判断が必要になるのは G-2）
- 状況：`DATA_MODEL.md` 10章に「Search Console は日付がUTC基準で返るため、JSTの日付へ変換してから `metrics_daily` に保存する」とある。しかし Search Console が返すのは**時刻を持たない日付文字列**であり、日単位で集計済みの値である

  UTCの1日 `[00:00Z, 24:00Z)` は JST の `[09:00, 翌09:00)` にあたる。**1つのUTC暦日は2つのJST暦日にまたがる**ため、時間単位の内訳が無い以上、JSTの暦日へ割り直すことは原理的にできない。「変換」は実際には**どの暦日に載せるかの取り決め**になる
- 選択肢：
  - (a) UTCの暦日をそのままJSTの暦日として保存する（9時間のずれを許容する）
  - (b) 1日ずらして保存する
  - (c) `metrics_daily` にデータ源のタイムゾーンを持たせ、Search Console 由来の行は「UTC基準の日」として区別する
- 影響範囲：G-2（取得ジョブ）、G-6（`metrics_daily` 集計）、G-7（管理ダッシュボード）。手動収益入力（G-5）はJST基準のため、混在すると同じ `metric_date` の行で基準が食い違う
- 状態：**解決（2026-08-08）。(a) を採用。返ってきた日付をそのまま `metric_date` に入れる**

決定内容：

- **Search Console が返した日付を変換せず `metric_date` に入れる。** 日付をずらさない
- **`metrics_daily` にタイムゾーンの列を足さない**（(c) は採らない）
- **`metric_date` の意味を DATA_MODEL 10章に明記する。** 「Search Console 由来の列はSearch Consoleの暦日であり、JST基準の収益（G-5）と同じ日付で突き合わせると最大1日ずれる」
- G-7 の画面で両者を並べる場合は、この注記を画面にも出す

理由：

- **原理的に割り直せない。** Search Console が返すのは時刻を持たない日付文字列で、日単位に集計済み。時間単位の内訳が無い以上、JSTの暦日へ再配分することはできない。どの方式も「取り決め」にすぎない
- **ずれは全ブログに等しくかかる。** Phase 0 の目的は3ブログ×10名の比較（SPEC 1.2）であり、**系統的なずれは比較を歪めない**。8週間のトレンドを見るのに1日の位相差は影響しない
- (b) の「1日ずらす」は、ずれを別のずれに置き換えるだけで根拠が無い
- (c) の「タイムゾーン列を持つ」はマイグレーションと分岐が増えるわりに、上記のとおり分析上の利得が無い

**残っている確認事項：**

`DATA_MODEL.md` 10章は「Search Console は日付がUTC基準で返る」としているが、**この前提は検証していない**（Google の集計は太平洋時間だという説明も見られる）。**G-1 で実際に接続したときに確かめ、違っていれば記述を直す。** 決定の中身（そのまま入れる）は前提がどちらでも変わらない。

- A-7 が提供した `toJstDate(instant)` は**瞬間**の変換であり、この決定とは別物。日付文字列の割り直しには使わない

### Q-006 `blogs.targetReader` のカラム名が snake_case になっていない

- 発生タスク：A-8（初期マイグレーションの生成中に判明）
- 状況：`DATA_MODEL.md` 1章は「テーブル名・カラム名は snake_case」と定めているが、`prisma/schema.prisma:292` の `Blog.targetReader` に `@map` が無く、**DBのカラム名が `"targetReader"` になる**

  ```prisma
  // prisma/schema.prisma:292（Blog）
  targetReader      String              // @map が無い → "targetReader"

  // prisma/schema.prisma:388（BlogPersonaSetting）
  targetReader      Json  @map("target_reader")   // こちらは正しい
  ```

  実カラム26テーブル分を走査した結果、**snake_case でないのはこの1件のみ**。同じ概念が `blogs` と `blog_persona_settings` で別名になっている
- 選択肢：
  - (a) **`@map("target_reader")` を追加して揃える。** 初期マイグレーション作成前の今なら1行の変更で済む
  - (b) そのままにする。以降 `blogs` だけ SQL で `"targetReader"` とクォートが必要になる
  - (c) 後で直す。初期マイグレーション適用後は `ALTER TABLE ... RENAME COLUMN` の追加マイグレーションが必要
- 影響範囲：`prisma/schema.prisma`、初期マイグレーション、B-3・B-5（ブログCRUDと設定画面）、G-7（ダッシュボードの生SQL）
- 状態：**解決（2026-08-07）。(a) を採用。`@map("target_reader")` を追加した**

決定内容：

- `prisma/schema.prisma` の `Blog.targetReader` に `@map("target_reader")` を追加
- 初期マイグレーションを再生成し、**26テーブルの全実カラムが snake_case であることを確認**
- `blogs.target_reader` と `blog_persona_settings.target_reader` で名前が揃った
- 本件は初期マイグレーション適用前だったため、追加のマイグレーションは不要

### Q-007 ブログのWordPress接続先を途中で別サイトに変更できるか

- 発生タスク：C-1（判断が必要になるのは C-1・C-5）
- 状況：`wordpress_connections.blog_id` は `unique` で「1ブログ＝1接続」だが、**`site_url` を後から書き換えられるか**が `SPEC.md` にも `DATA_MODEL.md` にも定義されていない。SPEC 13.3 には `connect` と `disconnect` の両方があり、切断後に別サイトへ繋ぎ直す経路が読める
- 影響範囲：C-1（接続情報の保存）、C-5（WP同期）、G-2・G-6（計測）、実験データの解釈
- 状態：**解決（2026-08-07）。別サイトへの変更は許可しない**

決定内容：

- **接続後に `site_url` を別のサイトへ変更できない。** 変更しようとした場合は拒否する
- **同一 `site_url` のままの再接続は許可する。** アプリケーションパスワードの入れ替え・権限の付け直し・接続エラーからの復旧はこれにあたり、運用上必ず発生する
- `disconnect` で `connection_status` を `REVOKED` にしても **`site_url` は保持する。** 再接続時は保持している値との一致を確認し、異なれば拒否する
- **モニター自身は変更できない。ただし ADMIN は設定ミスの救済として変更できる**（Q-008 で追記）
- ADMIN の介入でも直せない場合は、そのブログを `CLOSED` にして別のブログとして作り直す

理由：

- 接続先が変わると、そのブログに紐づく `wordpress_posts`・`metrics_daily`・Search Console のデータが別サイトのものと混ざり、**実験データとして読めなくなる**
- Phase 0 の目的は3ブログ×10名の比較（SPEC 1.2）であり、途中で土台が変わった行は分析に使えない
- 一方で認証情報の入れ替えを塞ぐと、パスワード漏洩時や権限変更時に復旧できなくなる。**「サイトの変更」と「認証情報の更新」を分けて扱う**


### Q-008 `CLOSED` にしたブログのスロットを再利用できるか

- 発生タスク：B-4
- 状況：SPEC 2.5 は「1ユーザー当たりブログ：最大3件」、SPEC 13.2 は「削除は物理削除せず `CLOSED`」と定めるが、**`CLOSED` にしたスロットを再利用できるかが書かれていない**

  技術的な制約として `UNIQUE(user_id, slot_number)` があり、`slot_number` は NOT NULL で 1〜3 に限られる。`CLOSED` の行もスロットを保持し続けるため、**再利用を許すには部分一意索引（`CLOSED` を除く）への差し替えが必要**になる
- 選択肢：
  - (a) **再利用しない。** スキーマ変更が不要。`(user_id, slot_number)` が期間を通じて安定した識別子になる
  - (b) 再利用する。マイグレーションが必要。集計時に必ず期間で切り分けることになる
- 影響範囲：B-4、B-5、H-2（オンボーディング）、`metrics_daily` の分析
- 状態：**解決（2026-08-07）。(a) を採用。ただし設定ミスは ADMIN の介入で救済する**

決定内容：

- **モニターは `CLOSED` にしたスロットを再利用できない。** 3スロットは期間を通じて固定
- スキーマは変更しない。`UNIQUE(user_id, slot_number)` をそのまま使う
- **設定ミスは「作り直し」ではなく「ADMIN による修正」で救済する**（下記）

### ADMIN による設定ミスの救済（Q-007・Q-008 共通）

Q-007 で WordPress 接続先の変更を禁止し、Q-008 でスロットの再利用を禁止した。この2つが重なると、**オンボーディングでの接続ミスが 1/3 の枠を恒久的に潰す**。モニターは技術者ではなく、H-2 のオンボーディングは10ステップあるため、ミスは起きる前提で考える。

そのため **ADMIN の手動介入を正規の救済手段とする**（SPEC 3.1「手動介入」）。

- **ADMIN は設定ミスのブログについて、`wordpress_connections.site_url` を変更できる**
- **ADMIN はスロットを解放できる。** 手段はそのブログの物理削除。`Blog → 配下全て` が `Cascade` のため関連行も消える
- **いずれも「そのブログでまだ記事を投稿していない」場合に限る**（`wordpress_posts` が0件）。投稿済みのブログを消すと実測データが失われ、Phase 0 の目的（SPEC 1.2）に反する
- **介入は `audit_logs` に残す**
- **Phase 0 では専用の管理UIとAPIを作らない。** SQL または管理画面の既存機能で行う（SPEC 10.3 の `experiment_groups` と同じ扱い）
- 手順は H-6（操作マニュアル）に記載する

「何回介入が必要になったか」は、オンボーディング設計の評価データとしても使う。

### Q-009 ブログ設定画面で「ジャンル」を編集できるか

- 発生タスク：B-5
- 状況：`SPEC.md` 6.1 は `/liff/blogs/[blogId]/settings` の項目に**ジャンル**を挙げている。しかし `genres` は独立したマスタテーブルで、`status` は `CANDIDATE` / `APPROVED` / `REJECTED` を取る

  SPEC 9.2.2（STEP 1 ジャンル審査）によれば、ジャンルは**審査を経て決まる**。YMYL該当・案件0件・上位10件の寡占という停止条件があり、差し戻しは2回まで。つまりジャンルは利用者が設定画面で自由に選ぶ値ではなく、**E-4 の審査結果**である

  また `genres` を作る・埋めるタスクが `TASKS.md` に存在しない。B-5 の時点で選択肢として出せる行が1件も無い
- 選択肢：
  - (a) **設定画面ではジャンルを表示のみとし、変更は E-4 の審査経由にする。** 停止条件を迂回できなくなる
  - (b) 設定画面で自由に選べるようにする。審査を通っていないジャンルが設定できてしまう
  - (c) ジャンルマスタを事前に用意する別タスクを立て、承認済みのものから選ばせる
- 影響範囲：B-5、E-4、`blogs.genre_id`
- 状態：**解決（2026-08-07）。(a) を採用。設定画面では表示のみとする**

決定内容：

- **`/liff/blogs/[blogId]/settings` からジャンルを変更できない。** 表示のみ
- ジャンルの決定と変更は **E-4（STEP 1 ジャンル審査）** を経由する
- `blogs.genre_id` が未設定のブログでは「未設定」と表示し、審査への導線を出す（導線の実装は E-4）
- `genres` を埋めるタスクは新設しない。行は E-4 の審査で生まれる

理由：停止条件（YMYL該当・案件0件・上位10件の寡占）はジャンル選定の中核（SPEC 9.2.2）であり、設定画面から素通りできる経路を作ると審査の意味が無くなる。

### Q-010 ブログ設定画面の「通知設定」をどこに置くか

- 発生タスク：B-5
- 状況：`SPEC.md` 6.1 は `/liff/blogs/[blogId]/settings`（**ブログ別**）の項目に**通知設定**を挙げている。しかし通知設定は `monitor_profiles`（**ユーザー別**）にある

  ```prisma
  // prisma/schema.prisma（MonitorProfile）
  notificationDays  Int[]     @map("notification_days")
  notificationTime  DateTime  @map("notification_time") @db.Time(6)
  maxDailyProposals Int       @default(1) @map("max_daily_proposals")
  ```

  `TASKS.md` の F-3「通知数制御（既定1日1件・最大2件）」の完了条件も「**3ブログ合計で**制限される」であり、ユーザー単位での制御を前提としている。ブログ別に通知曜日・時刻を持たせる場所がスキーマに無い
- 選択肢：
  - (a) **通知設定はブログ設定画面から外し、ユーザー設定として扱う**（オンボーディング STEP 9・H-2 の担当）。スキーマ変更が不要
  - (b) `blogs` にブログ別の通知設定を追加する。マイグレーションが必要で、F-3 の「3ブログ合計」と整合を取り直すことになる
- 影響範囲：B-5、F-2・F-3（通知送信と通知数制御）、H-2（オンボーディング STEP 9）
- 状態：**解決（2026-08-07）。(a) を採用。通知設定はユーザー単位のまま**

決定内容：

- **通知設定を `/liff/blogs/[blogId]/settings` に置かない。** ブログ設定画面の項目から外す
- 通知曜日・時刻・1日の上限は `monitor_profiles` のまま。**ユーザー単位で1組**
- 編集の入口は**オンボーディング STEP 9（H-2）**とユーザー設定画面。B-5 の範囲外
- スキーマは変更しない

理由：1日1件という制限は「モニターに届く通知の総量」の話（F-3「3ブログ合計で制限される」）。ブログごとに曜日と時刻を分けると3ブログで最大3倍の通知が出る設計になり、SPEC 2.x の意図と食い違う。

### Q-011 「新規記事・改善提案比率」の保存先が無い

- 発生タスク：B-5
- 状況：`SPEC.md` 6.1 は設定画面の項目に**新規記事・改善提案比率**を挙げているが、これを保存するカラムが `blogs` に無い

  近いものとして `blogs.article_ratio` があるが、`DATA_MODEL.md` 3章の定義は別の軸である

  ```ts
  {
    revenue: number;          // 収益記事の本数（9.2.4の算出値）
    traffic: number;          // 集客記事の本数
    weeklyPublishCap: number; // 既定 4（SPEC 2.2）
  }
  ```

  `revenue` / `traffic` は**収益記事と集客記事**の軸で、しかも「9.2.4の算出値」＝**コードが算出する値**である（SPEC 9.2「判定は必ずコード側で行う」）。一方「新規記事・改善提案」は**既存記事を書き直すか新しく書くか**という別の軸で、利用者が決める値と読める
- 選択肢：
  - (a) `article_ratio` に第3の軸（例 `improvementRatio`）を足す。jsonb なのでマイグレーション不要
  - (b) 項目自体を B-5 から外し、比率が実際に使われる E-9（公開順序）で必要性を判断する
  - (c) SPEC 6.1 の記載を「収益記事・集客記事比率」の誤記とみなし、表示のみにする
- 影響範囲：B-5、E-9（公開順序の付与）、`blogs.article_ratio`
- 状態：**解決（2026-08-07）。(b) を採用。B-5 では扱わない**

決定内容：

- **「新規記事・改善提案比率」を B-5 の対象項目から外す。** 保存先を先に決めない
- 必要性と保存先は **E-9（公開順序の付与）** で判断する。そこが比率を実際に読む最初の場所になる
- **`article_ratio.revenue` / `traffic` は利用者に編集させない。** SPEC 9.2.4 の算出値であり、SPEC 9.2「判定は必ずコード側で行う」に反する
- **設定画面で編集できるのは `article_ratio.weeklyPublishCap` のみ**（投稿頻度）。上限は4（SPEC 2.2「週4本を超えて公開する処理を実装してはならない」）

理由：この比率を読む処理がまだ無く、先に入力欄だけ作ると E-9 の実装時に意味が確定して作り直しになる。

### B-5 で編集できる項目（Q-009〜Q-011 の決定を反映）

| 項目 | 保存先 | 扱い |
|---|---|---|
| ブログ名 | `blogs.name` | 編集可 |
| ペンネーム | `blogs.pen_name` | 編集可 |
| 想定読者 | `blogs.target_reader` | 編集可 |
| 収益方針 | `blogs.purpose` | 編集可 |
| 投稿頻度 | `blogs.article_ratio.weeklyPublishCap` | 編集可（1〜4） |
| ジャンル | `blogs.genre_id` | **表示のみ**（Q-009） |
| 収益記事・集客記事の本数 | `blogs.article_ratio.revenue` / `traffic` | **表示のみ**（算出値・Q-011） |
| 通知設定 | `monitor_profiles` | **B-5 の範囲外**（Q-010。H-2 が担当） |
| 新規記事・改善提案比率 | — | **B-5 の範囲外**（Q-011。E-9 で判断） |

`slug` と `slot_number` は設定画面に出さない。`slug` は WordPress 側の識別に関わり、`slot_number` は B-4 の決定によりサーバーが決める。

### Q-012 管理者のログイン手段に Supabase Auth を採用するか

- 発生タスク：B-6
- 状況：`SPEC.md` 3.2「管理者側」は次のように定めている

  > Phase 0では以下のいずれかを採用する。
  > 推奨：Supabase Auth／メール＋ワンタイムリンク／ADMINロールによる制御
  > Basic認証のみの運用は廃止する。

  **「いずれか」であり、Supabase Auth は必須ではない。**

  一方、このリポジトリの現状は次のとおり。

  | 項目 | 現状 |
  |---|---|
  | DB | 素の PostgreSQL + Prisma。**Supabase は未導入** |
  | モニターの認証 | LINE Login（B-1）＋ HMAC署名付きCookie（B-2） |
  | ロール | `users.role` に `ADMIN` / `MONITOR`（A-2） |

  B-6 で実装したのは**認可**（`requireAdmin`）であり、**誰がどうログインするか**は別の判断である
- 選択肢：
  - (a) **現状の仕組みをそのまま使う。** ADMIN も LINE でログインし、`users.role = 'ADMIN'` で制御する。追加の依存なし。ADMIN への昇格は SQL で行う
  - (b) **Supabase Auth を導入する。** Supabase プロジェクトの作成、`@supabase/ssr` の導入、環境変数（URL・キー）が必要。管理者の識別情報が Supabase 側と `users` 側の2箇所に分かれる
  - (c) **メール＋ワンタイムリンクを自前で実装する。** トークンを保存するテーブルが要るため**スキーマ変更**を伴い、DATA_MODEL 9章により単独タスク・単独PRになる。加えてメール送信基盤（Resend / SES 等）の選定が未着手
- 影響範囲：B-6、B-7、G-7（管理ダッシュボード）、H-6（操作マニュアル）、運用コスト
- 状態：**解決（2026-08-07）。(c) を採用。メール＋ワンタイムリンクを自前で実装する**

決定内容：

- **管理者は「メールアドレス＋ワンタイムリンク」でログインする**（SPEC 3.2 の2つ目）
- **Supabase は導入しない。** DBは素の PostgreSQL のまま
- **モニター側は変更しない。** LINE Login（B-1）＋署名付きCookie（B-2）のまま。認証の入口が2つになるが、対象者も画面も分かれている（`/liff` と `/admin`）
- 認証が成功したあとは**既存のセッションCookieを発行する**（B-2 の仕組みを共用）。`requireAdmin` は変更不要
- 照合は `users.email`。**`users.role = 'ADMIN'` の行にのみリンクを送る。** MONITOR のメールアドレスにリンクを送らない

必要になる作業（B-6 の完了条件の外なので別タスクにする）：

| タスク | 内容 |
|---|---|
| **B-10** | ワンタイムトークンのテーブル追加とマイグレーション（DATA_MODEL 9章によりスキーマ変更は単独タスク） |
| **B-11** | ログイン画面・リンク発行・検証・セッション発行 |

トークンの扱いで守ること（B-10・B-11 の前提）：

- **DBにはトークンのハッシュだけを保存する。** DBが漏れてもリンクが使えないようにする
- **1回だけ使える。** 使用時刻を記録し、2回目を拒否する。メールは残るため、期限内の再利用を許すと転送・端末紛失でそのまま入られる
- **期限は短くする**（15分程度）
- **存在しないメールアドレスでも同じ応答を返す。** どのアドレスが登録済みかを教えない
- 発行の回数を制限する（同一アドレスへの連続送信を防ぐ）

**なお、この決定は Q-013（メール送信基盤の選定）が前提になる。**

### Q-013 メール送信基盤をどれにするか

- 発生タスク：B-11（Q-012 の決定に伴い発生）
- 状況：Q-012 で管理者のログインを「メール＋ワンタイムリンク」と決めたが、**メールを送る手段がまだ無い**。`SPEC.md` にも `TASKS.md` にも送信基盤の記載がなく、環境変数も定義されていない

  Phase 0 の送信量は少ない。管理者1〜2名のログインリンクのみで、モニターへの連絡は LINE（F-2）である
- 選択肢：
  - (a) **Resend。** APIが単純で、ドメイン認証だけで送れる。無料枠が小さめだが Phase 0 の量なら収まる
  - (b) AWS SES。単価は最安。ただしドメイン検証に加え、**サンドボックス解除の申請**が要る
  - (c) SMTP（Gmail・さくら等の既存アカウント）。新しい契約が不要。送信の到達率と資格情報の管理が課題
- 影響範囲：B-11、H-3（エラー通知）が将来メールを使う場合、環境変数、運用コスト
- 状態：**解決（2026-08-07）。(a) Resend を採用**

決定内容：

- **Resend を使う。** HTTP API を `fetch` で叩く。SDKは入れない（依存を1つ増やす価値が無い）
- 環境変数は `RESEND_API_KEY` `MAIL_FROM` `APP_BASE_URL` の3つ
- **3つとも `src/lib/env.ts` では必須にしない。** 未設定でも LIFF 側（モニターの利用）は動く必要があるため、起動を止めない。管理者ログインを試みたときに設定不足として扱う
- **`APP_BASE_URL` は必ず環境変数から取る。** リクエストの `Host` ヘッダーから組み立てると、偽の `Host` を送られたときにリンクの向き先を攻撃者のドメインへ差し替えられる
- **送信先はログインリンクのみ。** モニターへの通知は LINE（SPEC 2.x・F-2）であり、メールへ広げない
- 送信処理は差し替え可能なインターフェースにする。テストは偽の実装で行い、実際の送信は行わない

理由：Phase 0 で必要なのは「管理者1〜2名へリンクを送る」だけで、(b) の申請と (c) の到達率の面倒を負う理由が無い。送信量が増える段階で見直す。

---

### Q-014 サブIDをアフィリエイトURLへどう付けるか

- 発生タスク：D-9（判断が必要になるのは D-1）
- 状況：Q-001 の決定で「**サブIDは全案件に付ける**」と定めた。ASPが用意した機能なので規約上の問題が無く、`DIRECT` の案件でも**成果を記事単位で紐づけられる**。Phase 0 の目的は収益の実測（SPEC 1.2）であり、クリック数より価値が高い

  ところが**付け方はASPごとに違う**。

  | 形 | 例 |
  |---|---|
  | クエリパラメータ | `...&sub=<値>` `...&s1=<値>` `...&argument=<値>` |
  | パスの一部 | `.../click/<値>` |
  | 付けられない | サブID機能そのものが無い |

  パラメータ名も、使える文字種も、長さの上限もASPが決める。**こちらでは調べられない。** 利用するASPが確定して初めて決まる
- 選択肢：
  - (a) **`affiliate_offers` にパラメータ名の列を持つ**（例：`sub_id_param text nullable`）。`NULL` なら付けない。ASPごとの違いをデータで吸収できる
  - (b) ASP名から実装側のテーブルで引く。列は増えないが、ASPが増えるたびにコード変更が要る
  - (c) 案件登録時に完成形のURLテンプレートを入れてもらう。最も柔軟だが、登録が難しくなる
- 影響範囲：D-1（案件登録時にURLの組み立て規則が決まる）、D-8、G-6（収益の記事別集計）
- 状態：**解決（2026-08-08）。(a) を採用。`affiliate_offers` にパラメータ名の列を持つ**

決定内容：

- **`affiliate_offers.sub_id_param`（text・NULL可）を追加する。** クエリパラメータの名前を入れる（`sub` `s1` `argument` など）
- **`NULL` ならサブIDを付けない。** 案件は普通に登録でき、リンクは `affiliate_url` のまま
- 値は `<slot>-<contentItemId>`（Q-001 の決定どおり）
- **設定するのは ADMIN。** Phase 0 は SQL で入れる（SPEC 10.3 の `experiment_groups` と同じ扱い）。モニターに触らせない
- **`REDIRECT` の案件にも付ける。** リダイレクタはクリックを数え、サブIDは成果を紐づける。役割が違う（Q-001）

**この決定で D-1 のブロックが外れる。** ASPの情報がゼロでも案件は登録でき、サブIDが付かないだけになる。分かったASPから順に `sub_id_param` を埋めれば有効になる。**「着手を止める問題」から「あとから入れる運用データ」へ変わった。**

理由：

- (b)（ASP名から実装側のテーブルで引く）は、ASPが増えるたびにコード変更とデプロイが要る。Phase 0 の運用に対して重い
- (c)（完成形のURLテンプレートを登録してもらう）は柔軟だが、案件登録が難しくなる。登録するのは運営だが、10名×複数案件を手で入れる作業で誤りが増える
- (a) は列1つで済み、**知らないASPは `NULL` のまま動く**

**まだ分からないこと（急がない）：**

実際に使うASPのパラメータ名。**サブIDを使いたくなった時点で分かればよい。** モニター募集（H-1）より後でも間に合う。

**パス埋め込み型のASP**（`.../click/<値>`）が出てきた場合はこの列で表現できない。そのときに改めて論点として起票する。**先回りして設計しない**（形が決まらないうちに作ると作り直しになる）。

**スキーマ変更は単独タスク D-10 とする**（DATA_MODEL 9章）。D-1 は D-10 に依存する。

---

### Q-015 D-7「LINE返信からのfacts保存」の半分が着手できない

- 発生タスク：D-7（TASKS.md）
- 状況：

  D-7 の完了条件は「返信が **`persona_facts` または `revision_requests`** に保存される」。このうち **`revision_requests` 側が現時点では実装できない。**

  理由は2つあり、どちらも単独で決定的である。

  1. **`revision_requests.approval_id` は NOT NULL。** 修正依頼は承認に紐づくもので、`approvals` の行が無ければ作れない。`approvals` を作るのは F-1（提案の優先順位算出）で、**まだ存在しない**
  2. **`revision_requests` は `approvals` モジュールの所有**（MODULE_RULES 1 のテーブル所有表）。`line` モジュールが直接書くことはできず、経由すべき公開関数は F-6（承認・修正依頼・見送りAPI）で作られる

  TASKS の D-7 の依存欄は `D-6` のみで、この関係が反映されていない。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **分割する** | D-7 は分類と `persona_facts` への保存まで。修正依頼の保存は F-6 に含める（依存欄を直す） |
  | (b) | **D-7 ごと後ろへ倒す** | F-6 の完了まで D-7 に着手しない |
  | (c) | **`approvals` を前倒しで作る** | D-7 のために最小の `approvals` モジュールを先に作る |

- 影響範囲：D-7、F-6、`src/modules/line/`、`docs/TASKS.md` の依存欄
- 状態：**解決（2026-08-11）。(a) を採用。D-7 は分類と `persona_facts` への保存まで**

**修正依頼の保存は F-6 が担う**（実装・マージ済み）。D-7 は分類だけを書けば完成する。
分類そのものは修正依頼側でも要るため、作る場所はここでよい。

推奨は **(a)**。

- **分類そのものは修正依頼側でも要る。** 「感想」「助言」「修正希望」を見分ける処理は D-7 で作り、行き先だけ F-6 で繋ぐのが自然
- (b) は、**Phase D の他タスクと無関係に D-7 だけが Phase F まで止まる**。`line` モジュールが空のまま F-2（LINE通知送信）へ入ることになり、通知と返信の実装が離れる
- (c) は**タスクの順序を崩す**。`approvals` は F-1 の成果物で、先に作ると F-1 側で作り直しになる

**この決定は着手前に要る**（D-7 の完了条件の読み方が変わるため）。決まるまで D-7 には着手せず、**D-8 を先に進める**（依存は D-1 のみで、影響を受けない）。


---

### Q-016 Vercel のプランを Pro にするか

- 発生タスク：E-1（ジョブ基盤）
- 状況：

  `vercel.json` の cron を**毎分**にしてあるが、**毎分の cron は Vercel Pro でしか動かない**（Hobby は1日1回まで）。Hobby のままだと、承認された記事が**最大24時間投稿されない**。

  承認から投稿までの待ち時間は Phase 0 の体験そのもの（SPEC 1）で、「承認したのに何も起きない」が24時間続くと、モニターは仕組みが壊れていると受け取る。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **Pro を使う** | 毎分 cron。承認から数分で投稿される |
  | (b) | Hobby のまま | cron は1日1回。承認から最大24時間 |
  | (c) | 外部から叩く | 別のスケジューラから `/api/jobs/run` を呼ぶ。プランは Hobby のまま |

- 影響範囲：`vercel.json`、`docs/ARCHITECTURE.md` 10章、E-1 の残課題
- 状態：**解決（2026-08-09）。(a) を採用。プランは Pro**

毎分 cron を前提にしてよい。`ARCHITECTURE.md` と `IMPLEMENTATION_STATUS.md` の E-1 の但し書きを更新した。

---

### Q-017 APIキーを管理画面から設定できるようにするか

- 発生タスク：新規（H-7〜H-9）
- 状況：

  現状、`ANTHROPIC_API_KEY`・`RESEND_API_KEY`・LINE のトークンはすべて**環境変数**にある。E-3 では「モデルの乗り換えはデプロイを伴う判断だから環境変数でよい」と決めた（SPEC 9.8 はどちらでもよいとしている）。

  だが**運用の実態が違った**。Phase 0 の ADMIN は、鍵の発行・差し替え・疎通確認を**デプロイと切り離して**行いたい。環境変数だと、鍵を1つ入れるたびに再デプロイが要る。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **設定テーブルを足し、管理画面から設定する** | `app_settings` に暗号化して保存。解決順は DB → 環境変数 → コード既定 |
  | (b) | 環境変数のまま | Vercel の画面で設定する。再デプロイが要る |
  | (c) | 併用せず DB のみ | 環境変数を読まない |

- 影響範囲：`src/modules/settings/`（新規）、`src/lib/ai/config.ts`、`src/lib/mailer/`、`src/app/admin/`、`prisma/`
- 状態：**解決（2026-08-09）。(a) を採用**

利用者の指示による。(c) を採らないのは、**設定を読む前に落ちる経路が要る**ため（下記）。

#### 画面に置けないものがある

**すべてを画面へ出すことはできない。** 設定はDBに入っており、DBを読むには設定が要る。

| 環境変数のまま | 理由 |
|---|---|
| `DATABASE_URL` | これが無いと設定そのものを読めない |
| `ENCRYPTION_KEY` | 保存された秘密を復号する鍵。DBに置いたら暗号化の意味が無い |
| `SESSION_SECRET` | 管理画面へ入るための認証に要る |
| `LINE_LOGIN_CHANNEL_ID` | LIFF の認証に要る。誤ると誰もログインできない |
| `APP_BASE_URL` | 管理者ログインのリンクを組み立てる。**画面へ入る前**に要る |
| `CRON_SECRET` | cron の呼び出し検証。アプリの外から使う |
| `NEXT_PUBLIC_*` | ブラウザ向けは**ビルド時に埋め込まれる**。DBに置いても効かない |

画面から設定できるのは、**アプリが動いた後で初めて要るもの**に限る。

| 画面から設定 | 秘密 |
|---|---|
| `AI_PROVIDER` `AI_MODEL_*` `AI_PRICE_*` `AI_BUDGET_*` | いいえ |
| `ANTHROPIC_API_KEY` | **はい** |
| `RESEND_API_KEY` | **はい** |
| `MAIL_FROM` | いいえ |
| LINE Messaging API のチャネルアクセストークン・シークレット（F-2） | **はい** |

#### 決めたこと

1. **秘密を復号して返す入口を作らない。** 画面に出すのは**末尾4文字と更新日時**だけ。「表示」ボタンを作らない — 一度入れた鍵を読み返す必要は運用上無く、経路があるだけで漏洩面が増える
2. **解決順は DB → 環境変数 → コード既定。** DBを先にするのは、「画面で設定したのに効かない」が最も原因の分かりにくい失敗だから
3. **接続テストは保存前の値でも試せる。** 誤った鍵を保存してから気づく順序にしない
4. **`audit_logs` へは書かない。** `audit` モジュールは未実装で、他モジュールのテーブルを触れない（MODULE_RULES 1）。代わりに設定行そのものに `updated_by_user_id` と `updated_at` を持つ

---

### Q-018 `audit_logs` を書くタスクが無い

- 発生タスク：E-4（ジャンル審査）
- 状況：

  SPEC は `audit_logs` への記録を**2か所で求めている**。

  | 箇所 | 内容 |
  |---|---|
  | SPEC 9.2.2 | 停止条件を承知で進めた選択を記録する（E-4） |
  | Q-008 の決定 | ADMIN による `site_url` 変更の介入を記録する |

  ところが **`audit` モジュールを作るタスクが TASKS.md に無い。** `audit_logs` は
  `audit` モジュールの所有テーブルで（MODULE_RULES 1）、`content-planning` から
  直接書くことはできない。

  E-4 の完了条件（TASKS）は「停止条件を満たすジャンルが通過しない。差し戻し2回で
  選択肢が出る」で、記録先は含まれていない。**選択そのものは
  `planning_runs.overridden_at` に残している**ため、E-4 は成立している。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **タスクを立てる** | `audit` モジュールを H-11 として作り、E-4 と Q-008 の記録をそこから行う |
  | (b) | 各モジュールが直接書く | MODULE_RULES 1 に反する |
  | (c) | `audit_logs` を使わない | SPEC の指示に反する |

- 影響範囲：`src/modules/audit/`、E-4、Q-008 の救済手順、`docs/TASKS.md`
- 状態：**解決（2026-08-09）。(a) を採用。H-11 として登録した**

**E-4 は止めない。** 選択は `planning_runs.overridden_at` に残っており、
`audit_logs` は「誰が・いつ・何を」を横断で追うための別の記録。H-11 で足す。

### `AiOperation` に無い場面（E-4 で判明）

CONTENT_PLANNING 1.3 が「ジャンル審査の所見」に `MODEL_HIGH` を指定しているが、
**SPEC 9.8 の場面の一覧に対応する項目が無かった。** 既存の場面（`MONTHLY_STRATEGY`
など）へ寄せると `ai_usage_logs` の内訳が用途とずれ、費用の分析ができなくなる。

`GENRE_REVIEW`（HIGH）と `GENRE_SUGGESTION`（STANDARD）を `src/lib/ai/tiers.ts` へ
足した。**SPEC.md は変更していない。**

---

### Q-019 「ブログ掲載禁止」を判定する列が無い

- 発生タスク：E-5（案件スコアリング）
- 状況：

  SPEC 9.2.3 は案件の足切り条件に「**ブログ掲載禁止**」を挙げているが、
  `affiliate_offers` に判定できる列が無い。近いのは `deny_conditions`
  （文字列の配列）だけで、これは否認条件の自由記述である。

  文言から機械判定すると、**黙って通す**（禁止なのに採用する）のも
  **黙って落とす**（禁止でないのに除外する）のも起きる。前者は規約違反、
  後者は成果の機会損失で、どちらも気づきにくい。「ブログ掲載不可」
  「ブログNG」「自社メディアのみ」など表記が揺れる。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **列を足す** | `blog_posting_prohibited`（既定 `false`）。ADMIN が設定する |
  | (b) | `deny_conditions` の文言で判定 | 表記の揺れで誤判定する |
  | (c) | 足切りから外す | SPEC の指示に反する |

- 影響範囲：`prisma/schema.prisma`、E-5、`src/modules/affiliate/`
- 状態：**解決（2026-08-09）。(a) を採用。E-5-schema として単独のマイグレーションにした**

`link_mode`（Q-001）・`sub_id_param`（Q-014）と同じ形。**ASPの規約の判断は
モニターにさせず、ADMIN が設定する。**

**既定は `false`。ここだけは安全側に倒さない。** `true` にすると、設定される
まで全案件が足切りされ、STEP 2 が常に0件になって STEP 1 へ差し戻され続ける。

---

### Q-020 `affiliate_links.content_item_id` が同じブログかを確かめられない

- 発生タスク：D-8（記録）→ E-7（塞げるか確かめた）
- 状況：

  D-8 の時点で「`affiliate_links.content_item_id` が案件と同じブログの記事か
  確かめられない」と記録し、**`content-planning` ができる E-4 の時点で塞ぐ**
  としていた。E-7 で `content_items` を引く経路（`listContentItemsForUser`）が
  できたので確かめたところ、**アプリ層では塞げない**ことが分かった。

  `affiliate.ensureRedirectLinkForUser` から記事の持ち主を確かめるには
  `affiliate → content-planning` の import が要る。ところが依存の向きは
  **`content-planning → affiliate`**（MODULE_RULES）で、**循環する。**

  C-6 とまったく同じ形の問題で、そのときの答えは「**制約はDBに置く**」だった。
  ただし `affiliate_links` には `blog_id` が無いため、複合外部キーを張れない。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **`affiliate_links` に `blog_id` を足し、複合外部キーで強制する** | C-6 と同じ形。どのモジュールから書いても迂回できない |
  | (b) | 呼び出し側の規律に任せる | `content-generation`（E-10）が確認済みのIDしか渡さない前提。**迂回できる** |
  | (c) | 依存の向きを変える | `affiliate → content-planning` にすると、既存の4本の依存が逆流する |

- 影響範囲：`prisma/schema.prisma`、`src/modules/affiliate/`、D-8
- 状態：**解決（2026-08-09）。(a) を採用。D-11 として実装した（#70）**

**E-7 では塞いでいない。** 実装の中で分かった制約なので、単独のタスクにした。

**マイグレーションと実装を分けられなかった。** `blog_id` は NOT NULL で、
書く側（`ensureRedirectLinkForUser`）が同じ変更で埋めないと、リンクを1本も
作れなくなる。DATA_MODEL 9章の「機能実装のPRにマイグレーションを混ぜない」は
**混ぜる必然性が無い場合の決まり**であり、ここは当てはまらない。

**記事側の `ON DELETE` を `SET NULL` から `CASCADE` へ変えた。** 複合外部キーの
`SET NULL` は参照している列を全て NULL にするため、NOT NULL の `blog_id` と
両立しない（PostgreSQL 15 以降の列指定は Prisma が表現できず、スキーマとの
乖離になる）。**実際の挙動は変わらない** — `content_items` が単独で消えることは
無く、消えるのはブログごと削除されるときだけで、そのとき `affiliate_links` も
案件経由で CASCADE で消える。

影響は**成果の帰属**に限られる（他人の記事IDを紐づけても、飛び先は自分の
案件のURL）。E-10 が記事にリンクを埋める前に塞ぐ。

---

### Q-021 `Review` 構造化データの `reviewRating` をどうするか

- 発生タスク：E-11（TASKS.md）
- 状況：CONTENT_PLANNING 7.3 は収益記事に `FAQPage` と `Review` を出すよう定めている。
  実装したが、**`Review` に `reviewRating` を入れていない**。

  **評点の出どころが無い。** 分身は案件の `facts` の範囲でしか書けず（SPEC 9.6）、
  「5段階で4.5」といった数字はどこにも存在しない。ここで適当な数字を入れることは
  SPEC 9.6 が禁じる「根拠のないランキング」そのものになる。

  一方で、**Google の Review 構造化データは `reviewRating` を必須としている。**
  無い状態では構文としては妥当でもリッチリザルトの対象にならず、
  「`Review` を出す」という指示の目的は果たせていない。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **承認画面（F-5）でモニターが評点を入力する** | 人間が付けた評点なら根拠がある。承認フローに1項目増える |
  | (b) | `Review` を出さない | `FAQPage` だけにする。CONTENT_PLANNING 7.3 の記述から外れる |
  | (c) | 評点なしのまま出す（現状） | 構文は妥当。リッチリザルトの対象外。**何も決めていないのと同じ** |

- 影響範囲：`src/modules/content-generation/structured-data.ts`、F-5、`docs/CONTENT_PLANNING.md` 7.3
- 状態：**解決（2026-08-11）。(b) を採用。`Review` を出さない（`FAQPage` だけにする）**

評点の出どころが無く、作り出すことは SPEC 9.6 が禁じる「根拠のないランキング」になる。
承認画面で入力させる案 (a) は**承認1回あたりの負担を増やす** —
短期KPIが「承認率」と「1記事当たり確認時間」（SPEC 11.1）である以上、そこを重くしない。
**E-16 として起票**し、`docs/CONTENT_PLANNING.md` 7.3 の記述もあわせて直す。

**(c) のまま実装した。** 評点を作り出すことだけは選択肢に入らないため、
決まるまでは「無い」を保つ。決まり次第 (a) か (b) へ寄せる。

---

### Q-022 `offer.facts.updatedAt` を書く経路が無い

- 発生タスク：E-12（TASKS.md）
- 状況：CONTENT_PLANNING 8.2 が「`offer.facts.updatedAt` が90日より古い場合は、
  照合が一致しても `WARNING` とする」と定めている。実装したが、
  **この値を書く経路がどこにも無い。**

  `affiliate_offers.facts` は jsonb で、D-1 の登録も D-2 のLP評価も
  `updatedAt` を入れない。列としての `affiliate_offers.updated_at` は
  あるが、**これは行のどの列を変えても更新される** — 状態を `ACTIVE` に
  しただけで「事実を確かめ直した」ことになってしまう。

  E-12 は**「`updatedAt` が無ければ古い扱い」**にした。
  「いつ確かめたか分からない」を「新しい」に倒すと、
  **測っていないことが「問題なし」に化ける**ため。
  結果として、**現状すべての収益記事が `WARNING` になる。**

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **`affiliate_offers.facts_updated_at` を列として足す** | `facts` を更新した経路だけが触る。行の `updated_at` と混ざらない |
  | (b) | `facts` の中に `updatedAt` を書く規約にする | 仕様の記述どおり。**書き忘れを防げない**（jsonb に制約を置けない） |
  | (c) | 90日の判定をやめる | 古い価格が「確認済み」として公開される |

- 影響範囲：`prisma/schema.prisma`、`src/modules/affiliate/`、D-1、D-2、E-12
- 状態：**解決（2026-08-11）。(a) を採用。`affiliate_offers.facts_updated_at` を列で足す**

jsonb の中に書く規約（b）は**書き忘れを防げない**（制約を置けない）。
列なら「事実を確かめ直した経路」だけが触り、行の `updated_at` とも混ざらない。
**D-13-schema（列）と D-13（更新経路）として起票**する。
いま全収益記事が `WARNING` になっている状態を解消する。

**`WARNING` は承認依頼を止めない**（SPEC 9.7）ので、実害は「すべての収益記事に
リスク表示が付く」こと。**黙って新しい扱いにするよりは、鳴りっぱなしのほうが安全側**
と判断してこの形で実装した。

---

### Q-023 `unverified_claims` の形が DATA_MODEL と違う

- 発生タスク：E-12（実装後に判明。E-13 の着手時に気づいた）
- 状況：DATA_MODEL 132 は次の形を定めている。

  ```ts
  unverified_claims = { claim: string; expectedSource: "offer_facts"|"persona_facts"; matched: boolean }[]
  ```

  E-12 が保存しているのは次の形。

  ```ts
  { text: string; type: ClaimType; excerpt: string; reason: UnverifiedReason }[]
  ```

  **私の確認漏れ。** E-12 の `参照` は CONTENT_PLANNING 8 と SPEC 9.7 で、
  DATA_MODEL の当該行を読んでいなかった。

  そのうえで、**定められた形では表せないものが2つある。**

  1. **`type`。** SPEC 9.7 の `WARNING`（`GENERAL` のみ未確認）と `FAILED`
     （`PRICE`/`CONDITION`/`FEATURE`/`EXPERIENCE` に未確認あり）の区別は
     主張の種別で決まる。承認画面（F-5）で「どれが重いのか」を出せない
  2. **`reason`。** 「照合先が空だった」と「facts に無い数値が入っている」では、
     人が確かめるときに見るところが違う

  また `matched: boolean` は、**未確認の一覧に入っている時点で常に `false`** になり、
  情報を持たない。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **DATA_MODEL を実装に合わせる** | 判定に必要な情報が仕様の形に無い。ドキュメントを直す |
  | (b) | 実装を DATA_MODEL に合わせる | `type` と `reason` を捨てる。F-5 でリスクの軽重を出せない |
  | (c) | 両方の項目を持つ | 同じことを2通りで持ち、書き分けを間違える |

- 影響範囲：`src/modules/content-generation/fact-check.ts`、F-5、`docs/DATA_MODEL.md`
- 状態：**解決（2026-08-11）。(a) を採用。`docs/DATA_MODEL.md` を実装に合わせる**

仕様の形の `matched: boolean` は、**未確認の一覧では常に `false`** で情報を持たない。
実装の `type` / `reason` は、F-5 で「どれが重いか」「どこを見ればよいか」を出すのに要る。
**まだ誰も読んでいない**うちに、正しいほうへ揃える。

**まだ誰も読んでいない**（F-5 が最初の読み手）ため、今なら (b) も費用ゼロで選べる。
`docs/` の書き換えは判断を仰いでから行う決まりなので、実装は現状のまま置いた。

---

### Q-024 提案の優先度の式が仕様に無い

- 発生タスク：F-1（TASKS.md）
- 状況：SPEC が定めているのは `priority_score integer`（5.14）と
  「3ブログ横断で優先順位を付ける」（9.1）だけで、**点の付け方は書かれていない。**

  完了条件が「優先度と提案理由が保存される」なので、置かないと実装できない。
  **推測で置いた**ため記録する。置いた式は次のとおり。

  | 要素 | 点 | 根拠 |
  |---|---|---|
  | 公開順序 | `100 - (publish_priority - 1) × 5`（下限0） | **順番は E-9 が既に決めている。** 収益記事の先行も週の割り当てもそこに入っており、同じ判断を二度しない |
  | 未提案のブログ | `+30` | **1日1件しか送れない**（SPEC 8.3）。3ブログのうち1つが勝ち続けると、残り2つは永久に進まない |
  | 前回提案からの日数 | `+日数 × 2`（上限14日） | 同上 |
  | 未回答の提案 | `-25 × 件数`（上限2件） | 返事が来ていないのに次を積むと、承認一覧が溜まってどれから見ればよいか分からなくなる |
  | 事実チェックが `WARNING` | `-5` | 同点なら手のかからないほうを先に見せる |
  | `warning` のリスクフラグ | `-3 × 件数`（上限3件） | 同上 |

  **リスクの減点は公開順序1つ分（5点）より大きくならないよう抑えた。**
  検査の結果で順番が入れ替わると、E-9 が組んだ公開順序が崩れる。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **この式のまま実験を始め、実データで直す** | Phase 0 は実験。10名の反応（SPEC 「LINE提案反応率：70%以上」）を見て調整する |
  | (b) | 式を仕様として固める | 決めた根拠が無いまま固定することになる |
  | (c) | 重みを管理画面から設定できるようにする | H-7 の仕組みに乗せられる。**実験中に変えられる**が、10名の間で条件が揃わなくなる |

- 影響範囲：`src/modules/approvals/priority.ts`、F-2、F-3、G-7
- 状態：**解決（2026-08-11）。(a) を採用。この式で実験を始め、実データで直す**

決める根拠が今は無い。定数は名前付きで1箇所（`priority.ts`）にまとまっており、
**10名の反応（SPEC 11.1「LINE提案反応率70%以上」）を見てから差し替えられる。**
(c) の「管理画面から重みを変える」は、**実験中に条件が揃わなくなる**ため採らない。

**(a) のつもりで実装した。** 定数は全て名前付きで1箇所にまとまっているので、
決まり次第すぐ差し替えられる。

---

### Q-025 `notification_days` / `notification_time` を読む処理が無い

- 発生タスク：F-3（TASKS.md）
- 状況：SPEC 6.1 のオンボーディング9番目が「通知曜日・時刻設定」で、
  `monitor_profiles` に `notification_days`（`integer[]`）と
  `notification_time`（`time`）がある。**H-2 が集めるが、読むタスクが無い。**

  F-3 が実装したのは SPEC 8.3 の**件数**の制御だけで、
  「いつ送るか」は含まれていない。TASKS を通しで見ても、この2列を読む
  タスクは見当たらない（`grep` で確認）。

  このままだと、**モニターが「火・金の朝9時」と設定しても、
  ジョブが動いた時刻にそのまま届く。** 設定できるのに効かない項目は、
  「設定したのに直らない」という形で信頼を落とす。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **F-3 に「送ってよい時間帯か」の判定を足す** | 送信の直前で弾く。実装は小さいが、弾かれた提案が翌日に持ち越されるだけか、溜まり続けるかを決める必要がある |
  | (b) | 新しいタスク（F-3b）として立てる | Q-018（`audit_logs`）と同じ扱い。1タスク＝1PRを守れる |
  | (c) | Phase 0 では読まない | 列を残したまま効かせない。**その旨をオンボーディングの画面に書く必要がある**（設定できるように見せない） |

- 影響範囲：`src/modules/line/`、H-2、`monitor_profiles`
- 状態：**解決（2026-08-11）。(b) を採用。F-3b として起票する**

F-3 の完了条件は「3ブログ合計で制限される。緊急通知は別枠」で、時刻を含んでいない。
**完了したタスクの条件を後から広げると、何が済んでいるのか読めなくなる**
（W-6 で D-8 を触らず D-12 を立てたのと同じ）。

**送れなかった提案は翌日へ持ち越す。** 溜め続けると、曜日を絞った人ほど
承認待ちが積み上がり、**通知が届いた瞬間に3件溜まっている**状態になる。

**H-2 の前に要る。** 読む処理が無いまま設定画面を作ると、
「設定させておいて効かない」状態でモニターに渡すことになる。

**F-3 では件数だけを実装した。** 完了条件が「3ブログ合計で制限される。
緊急通知は別枠」で、時刻の話を含んでいないため。
(b) を採るなら、H-2 の着手前に決める必要がある。

---

### Q-026 「招待」をアプリの機能にするか

- 発生タスク：H-1（TASKS.md）
- 状況：SPEC 6.2 の `/admin/users` に「招待」が並んでおり、
  SPEC 15.3 の E2E も「管理者がモニター招待 → モニターがLIFF登録」で始まる。
  **招待そのものを表すテーブルも列も無い。**

  H-1 が実装したのは「**ADMIN が参加を承認する**」ところまで。
  `users.status` の既定は `INVITED` で、LIFF から登録した時点でその状態になり、
  `INVITED` のままではアプリを使えない（`isActiveUser`）。
  つまり実験への参加は「登録できた」ではなく「ADMIN が認めた」で決まる。

  **残っているのは「声をかける」側**である。10名の実験なので、
  LIFFのURLを個別に渡せば足りるが、そうだと決めた記録が無い。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **アプリの外で招待する（現状）** | LIFFのURLを個別に渡す。実装ゼロ。**誰でもURLを知れば登録画面に入れる**が、`INVITED` のままなので使えない |
  | (b) | 招待コードを発行する | `invitations` テーブルを足し、登録時に消費する。**登録できる人を絞れる**。マイグレーションが要る |
  | (c) | ADMINが先に利用者を作る | 氏名だけの行を先に作り、LIFF登録時に結びつける。**結びつける鍵が無い**（`line_user_id` は登録して初めて分かる）ため、重複した行が生まれる |

- 影響範囲：`src/modules/users/`、H-2、`prisma/schema.prisma`
- 状態：**解決（2026-08-11）。(a) を採用。アプリの外で招待する**

LIFFのURLを個別に渡す。実装ゼロ。

**参加の可否は ADMIN の承認（H-1・実装済み）が既に握っている。**
URLを知られても `INVITED` のままでは何もできない。招待コードのテーブルを足しても
守れるものが増えず、10名の実験に見合わない。

**(a) の前提で H-1 を実装した。** (b) を採るなら H-2（オンボーディング）の
着手前に決める必要がある — 登録の最初の一歩が変わるため。

---

### Q-027 SPEC 14.4 が求める監査ログのうち、H-11 は2つしか書いていない

- 発生タスク：H-11 の直後（H-4 の着手時に SPEC 14.4 を読んで判明）
- 状況：**私の確認漏れ。** H-11 は Q-018 が挙げた2か所（SPEC 9.2.2 の
  「承知で進める」と ADMIN の介入）だけを実装した。Q-018 を書いたとき、
  **SPEC 14.4 を読んでいなかった。**

  SPEC 14.4「監査ログ」は8種類を挙げている。

  | SPEC 14.4 の項目 | H-11 | 書くとしたら |
  |---|---|---|
  | ログイン | ✗ | B-2 / B-6 の経路 |
  | WordPress接続変更 | ✗ | C-1 の `connectWordpressForUser` |
  | 案件URL変更 | ✗ | D-1 の案件更新 |
  | 承認 | ✗ | F-6 の `approveForUser` |
  | 公開 | ✗ | F-7 の投稿ジョブ |
  | 管理者介入 | **✓** | H-1（実装済み） |
  | ジョブ再実行 | ✗ | H-9 相当（管理画面の再実行） |
  | AIプロンプト変更 | ✗ | E-2 の `createPromptVersionForAdmin` |

  加えて SPEC 9.2.2 の「承知で進める」も実装済み（14.4 の一覧には無い）。

  **H-11 の完了条件（TASKS）は満たしている** — 「ADMINの介入と「承知で進める」の
  選択が記録される（Q-018）」。TASKS の完了条件が SPEC より狭かった。

  問題は**このまま忘れられること**。承認と公開の記録が無いと、
  「誰がいつ何を公開したか」を後から辿れない。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **H-12 として立てる** | 8種類の残りを1タスクでまとめて足す。呼び出し側は7モジュールに散る |
  | (b) | 各タスクの積み残しとして個別に足す | 該当タスクは全て完了済みで、戻る理由が要る |
  | (c) | Phase 0 では2つだけにする | **SPEC 14.4 に反する。** 選ぶなら SPEC を直す必要がある |
  | (d) | 優先度で分ける | 承認・公開・WordPress接続変更を先に足し、残りは後 |

- 影響範囲：`src/modules/audit/`、B-2、C-1、D-1、E-2、F-6、F-7、`docs/TASKS.md`
- 状態：**解決（2026-08-11）。(d) を採用。優先度で分け、重い3つを先に足す**

**承認・公開・WordPress接続変更**を先に足す（**H-12 として起票**）。
残り（ログイン・案件URL変更・ジョブ再実行・AIプロンプト変更）は後回し。

**承認と公開が最も重い。** 実験の結果を振り返るとき、
誰がいつ何を通したかが分からないと検証できない。
仕組みは H-11 で揃っており、足すのは各所からの1行の呼び出しと
`AUDIT_ACTIONS` への追加だけ。

**記録の仕組み（`audit` モジュール）は H-11 で揃っている**ので、
足すのは各所からの1行の呼び出しと、`AUDIT_ACTIONS` への追加だけ。
(a) か (d) を推す。**「承認」と「公開」が最も重い** — 実験の結果を
振り返るときに、誰がいつ何を通したかが分からないと検証できない。

---

### Q-028 本番のデータベースの提供元が決まっていない

- 発生タスク：H-5（TASKS.md）
- 状況：`docs/ARCHITECTURE.md` はアプリの実行先を Vercel と定めているが
  （Q-016 で Pro を決定）、**データベースをどこで動かすかは書かれていない。**

  H-5 のバックアップ手順は、提供元に依存しない部分（`pg_dump` / `pg_restore`、
  鍵の保管、復旧の確認）を書いた。**書けないのは次の3つ。**

  - 自動バックアップの保持期間の設定値
  - ポイントインタイムリカバリの可否と、遡れる範囲
  - スナップショットの取得時刻（JSTで何時か）

  **これらは「戻せるかどうか」を直接決める。** 例えば PITR が無い提供元なら、
  誤った `UPDATE` を1回流しただけで直前へ戻せず、日次スナップショットまで
  巻き戻すことになる（その日の承認が全部消える）。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **PITR を持つマネージドを選ぶ** | 承認の記録を失わずに戻せる。Phase 0 の規模なら費用は小さい |
  | (b) | 日次スナップショットのみ | 安い。**戻すと最大1日分の承認が消える** |
  | (c) | 自前で運用する | 手順が増え、10名の実験に見合わない |

- 影響範囲：`docs/BACKUP.md` 2.1、`docs/ARCHITECTURE.md`、`DATABASE_URL`、運用費
- 状態：**解決（2026-08-11）。(a) を採用。Google Cloud SQL for PostgreSQL（東京）**

決定内容：

- **Google Cloud SQL for PostgreSQL（東京リージョン）**
- **アプリの実行リージョンも東京（`hnd1`）に揃える**
- 自動バックアップ **7日以上**、**PITR を有効にし7日以上遡れること**、
  取得時刻は **JST 03:00〜04:00**（最も動きが少ない時間帯）
- 設定値は `docs/BACKUP.md` 2.1 に記載した

理由：

- **PITR が標準で含まれる。** 別料金のアドオンにすると、規模が変わるたびに
  費用の判断が要る。**誤った `UPDATE` を1回流しただけでその日の承認が
  全部消えるのを防ぐ**（承認の記録は実験の一次データ・SPEC 1.2）
- **アプリとDBを同じリージョンに置く。** ジョブは1件ずつ順番にクエリを
  投げるため（G-2・G-6）、離れていると1往復ぶんの遅延が何十回も積み上がる
- **東京にしたのは保管場所のため。** DBには `line_user_id` と記事本文が入る
  （SPEC 14.2）。**プロバイダの乗り換えは `pg_dump` で済むが、保管場所を
  国外から国内へ移すのは同意の取り直しになりうる**（SPEC 6.1 の
  オンボーディング3）。**後から変えると高くつく選択がこれだけ**なので、
  最初から国内にする
- **同じ製品のまま大きくできる。** 拡大しても製品を変えずにCPU・メモリ・
  ストレージを上げられ、リードレプリカも足せる

**検討して採らなかったもの：**

| | 採らなかった理由 |
|---|---|
| Neon / Render（シンガポール） | 費用は安いが**保管場所が国外**。後から国内へ移すのが高くつく |
| Supabase（東京）+ PITR アドオン | **PITR が別料金**で、この規模には割高。Auth も Storage も使わない（Q-012 で管理者ログインは自前と決定済み） |
| AWS RDS（東京） | 要件は満たすが、運用の手間が10名の実験に見合わない |

**残る確認事項：** 実際の月額（インスタンスサイズとストレージで決まる）と、
契約時に上の3つの設定が実際にその値になっていること。

**接続プールは拡大したときに足す。** Vercel は関数が同時に何本も立ち上がるため、
利用者が増えると接続数を使い切る。30ブログでは起きない。**これは「足す」変更で
あって「移す」変更ではない**ので、いま用意しない。

---

### Q-029 リンク切れの状態を保存するか

- 発生タスク：H-3（TASKS.md）
- 状況：H-3 はリンクを確かめて**その場で通知を積む**。
  「切れている」を保存する列は無い。

  **通知の重複はジョブの冪等キー（ブログ・種類・JSTの日付）で防いでいる**ので、
  同じ日に何度走らせても1回しか届かない。日が変われば、直っていない限り
  また届く。**運用としてはこれで足りる。**

  保存していないことで**できないことが2つある。**

  1. **管理画面に「いま切れているリンク」の一覧を出せない。** SPEC 6.2 の
     `/admin` は「リンク切れ」を挙げていないが、`/liff/blogs/[blogId]` は
     「エラー」を挙げている（SPEC 6.1）
  2. **いつから切れているかが分からない。** 3日前からか今朝からかで、
     モニターの対応の優先度が変わる

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **保存しない（現状）** | 通知だけ。実装ゼロ。画面には出せない |
  | (b) | `affiliate_offers` に `link_checked_at` / `link_broken_at` を足す | 画面に出せる。**いつからかが分かる**。マイグレーションが要る |
  | (c) | 確認の結果を別テーブルに履歴として残す | 変化の履歴が追える。Phase 0 には重い |

- 影響範囲：`prisma/schema.prisma`、`src/modules/affiliate/link-check.ts`、H-2 の画面
- 状態：**解決（2026-08-11）。(b) を採用。`link_checked_at` / `link_broken_at` を足す**

H-2 の画面（SPEC 6.1「エラー」）に出すのに要る。
**いつから切れているか**が分からないと、モニターは直す優先度を決められない。
**H-3b-schema（列）と H-3b（保存と表示）として起票**する。

**H-3 の完了条件（「緊急通知される」）は (a) で満たしている。**
画面に出す必要が生じたら (b)。

---

### Q-030 Search Console OAuth の管理方式

- 発生タスク：G-1（TASKS.md）
- 状況：**SPEC 20.2 が「該当Phase開始前に決定する項目」の4番目に挙げている
  そのものである。** Phase G の入口が G-1 なので、ここで止まる。

  SPEC 11.3 は「**ブログ単位でOAuth連携**」とだけ書いている。
  `search_console_connections` も**ブログごとに1行**で、
  `refresh_token_encrypted` を持つ（DATA_MODEL）。
  だが「誰のGoogleアカウントで連携するのか」は、どちらにも書かれていない。

  **いま実装できない具体的な理由：**

  1. **Googleの資格情報を置く場所が無い。** `src/lib/env.ts` にも
     `app_settings` の目録（H-7）にも、クライアントIDと
     クライアントシークレットの項目が無い。どちらに置くかは Q-017 の
     基準（「アプリが動いた後で初めて要るもの」は `app_settings`）で
     決められるが、**そもそも必要かどうかが方式によって変わる**
  2. **`webmasters.readonly` はGoogleの「機密スコープ」である。**
     未審査のアプリは「テスト」状態のまま最大100名までしか使えず、
     モニターには「**このアプリは確認されていません**」という警告が出る。
     Phase 0 は10名（SPEC 1.2）なので枠は足りるが、
     **警告を出したままオンボーディングさせるかは運営の判断**であり、
     審査を通すなら着手前に日数が要る
  3. **オンボーディングの手順数が変わる。** H-2 は10ステップと決まっている
     （SPEC 6.1）。方式によってSearch Console連携が
     「OAuthのボタン1つ」なのか「Search Console の画面で権限を足す作業」なのかが
     変わり、**モニターにやってもらう内容が別物になる**

- 選択肢：

  | | 案 | 連携のしかた | 影響 |
  |---|---|---|---|
  | (a) | **モニター自身のGoogleアカウントでOAuth** | LIFFから同意画面へ。ブログごとに `refresh_token` を保存 | スキーマのとおり。**Googleの審査か「未確認」警告のどちらかを飲む**。モニターがGoogleのパスワードを扱う場面が増える |
  | (b) | **運営のGoogleアカウント1つに、モニターが閲覧権限を付与** | モニターは Search Console の画面で運営のアドレスを「ユーザー追加」する。OAuthは運営が1回だけ | 審査が要らない。**`refresh_token` がブログごとに要らなくなる**（スキーマの前提が変わる）。運営が全プロパティを見られる |
  | (c) | **サービスアカウント** | (b) と同じだがサービスアカウントのアドレスを追加してもらう | OAuthの流れ自体が無くなる。`refresh_token_encrypted` を使わない |

- 影響範囲：`src/modules/analytics/`（G-1）、G-2・G-3・G-6・G-7（**G-1 を待つ**）、
  H-2 のオンボーディング手順、`prisma/schema.prisma`（(b)・(c) なら
  `search_console_connections` の前提が変わる）、`src/lib/env.ts` または
  `app_settings` の目録
- 状態：**解決（2026-08-11）。(c) サービスアカウントを採用**

決定内容：

- **サービスアカウント1つで全ブログの Search Console を読む。** OAuth の同意画面を使わない
- **モニターは Search Console の「設定 → ユーザーと権限」で、こちらが渡すアドレスを追加する。** H-2 の1ステップに収める
- **`search_console_connections.refresh_token_encrypted` を落とす**（G-1-schema）。ブログごとの行が持つのは `property_url` と接続状態だけ
- **サービスアカウントの秘密鍵は `app_settings` の暗号化列に置く**（H-7、Q-017 の基準どおり「アプリが動いた後で初めて要るもの」）

理由：

- **(a) は Phase 0 の期間と噛み合わない。** `webmasters.readonly` は Google の機密スコープで、
  審査を通さない場合の公開状態は「テスト」になる。**この状態のリフレッシュトークンは7日で失効する。**
  3か月の実験で、10名がそれぞれ3ブログ分を毎週つなぎ直すことになり、
  **日次の取得ジョブ（G-2）が週明けに落ちてデータに穴が空く**
- 審査を通す道もあるが、プライバシーポリシー・ドメイン所有権確認・デモ動画が要り、
  **着手前に日数がかかる**
- **OAuth のスコープはプロパティ単位で絞れない。** (a) はモニターのGoogleアカウントにある
  **全プロパティ**が読める状態になる。(c) はモニターが実験対象のブログだけを指定できる
- **(b) も7日失効を免れない。** 利用者が運営1人でも公開状態の話は変わらない。
  加えて個人アカウントに紐づくため、担当者の交代や2段階認証の再設定で全ブログの取得が止まる
- **モニターがGoogleのパスワードを扱う場面が無い。** オンボーディングの事故が減る（H-2）
- 実装が小さい。OAuthのコールバック経路もトークン更新も暗号化した `refresh_token` も要らない

**引き受ける不利：**

- **SPEC 11.3 の「ブログ単位でOAuth連携」という文言と食い違う。** `docs/SPEC.md` は唯一の正であり、
  **こちらでは書き換えていない。** 文言を直すかどうかは別途の判断が要る。
  なお「ブログ単位で連携する」という趣旨自体は満たしている
  （`search_console_connections` はブログごとに1行で、プロパティもブログごとに指定する）
- **秘密鍵1つが30ブログ分の入口になる。** `ENCRYPTION_KEY` と同じ扱いで保管する（H-5 の0章）
- **運営が全モニターのプロパティを見られる。** (b) と同じ。データ利用同意文（H-6）に含める

**G-1 の完了条件の読み方：** 「ブログ単位で連携でき、トークンが暗号化される」のうち、
暗号化の対象は**ブログごとのトークンではなくサービスアカウントの秘密鍵**になる。
`docs/TASKS.md` は書き換えていない。

**着手時に実測すること（G-1 で確かめる）：**

1. **URL Inspection API（G-3）が要求する権限の水準。** 「制限付きユーザー」で足りるか、
   「フル」または委任オーナーが要るか。**30ブログに後から権限を上げ直してもらうのは避けたい**ので、
   高いほうが要るなら最初からそれを頼む
2. **Search Console が返す日付の基準**（Q-005 に残した未検証事項）

**この2つは G-1 の実装時点でも未実測である。** 実際のサービスアカウントの鍵と、
所有確認の済んだプロパティが要り、開発環境では確かめられない。
**運用の準備時（H-2 の着手前）に、実際の鍵で1プロパティ試す。**

G-1 は権限の水準を**保存していない**。`sites.get` が返す `permissionLevel` は
接続テストの戻り値に含めてあるので、**1件つないだ時点で分かる。**
そこで「制限付きでは足りない」と分かった場合、モニターに頼む権限が変わるため、
**H-2 のオンボーディングの文面を書く前に確かめる必要がある。**

---

### Q-031 `metrics_daily.metric_date` が意図した暦日より1日前で保存される

- 発生タスク：G-3（G-2 の統合テストを書いていて気づいた。**原因は G-5 から入っている**）
- 状況：**実測で確かめた不具合。**

  `metric_date` は `date` 型の列だが、書き込む値を
  `startOfJstDay(...)` で作っている。この関数が返すのは
  **JSTの暦日の 00:00 を表すUTCの瞬間**である。

  ```
  startOfJstDay('2026-08-11')  →  2026-08-10T15:00:00Z
  ```

  これを `date` 型の列へ渡すと、**UTCの日付部分が入る。**
  実際にPostgreSQLへ書いて確かめた結果は次のとおり。

  ```
  送った値      : 2026-08-10T15:00:00.000Z
  PostgreSQL側  : 2026-08-10
  ```

  つまり**JSTの 2026-08-11 の行が `2026-08-10` として保存される。**

  影響する書き手は3つで、**どれも同じ関数を使っている。**

  | タスク | 何の日付のつもりか | 実際に入る値 |
  |---|---|---|
  | G-5（手入力） | その週の**JSTの月曜** | 前日（日曜） |
  | G-2（検索データ） | Search Console が返した暦日（Q-005 の決定「**そのまま入れる**」） | 前日 |
  | G-3（インデックス） | 調べた日（JSTの今日） | 前日 |

  **いま壊れていることは無い。** 書き手も読み手も同じ関数を通るため、
  同じ行に集まること自体は保たれており、統合テストも通っている。

  **困るのは外と突き合わせるとき。** G-7 の管理画面に出す日付、
  Search Console の画面と並べたときの日付、モニターに見せる「◯月◯日の週」が
  **すべて1日ずれる。** Q-005 は「Search Console が返した日付を変換せず
  `metric_date` に入れる」と決めているので、**決定と実装が食い違っている。**

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **`date` 列用の変換を用意して3か所を直す** | `Date.UTC(y, m, d)`（UTCの真夜中）を渡す。`startOfJstDay` は `timestamptz` の範囲検索用として残す（`jstDayRange` はこのままで正しい） |
  | (b) | 実験開始後に直す | **既存の行と新しい行で1日ずれる。** 突き合わせが余計に難しくなる |
  | (c) | 直さず、ずれている前提で読む | 画面と文書のすべてに注記が要る。**忘れられる** |

- 影響範囲：`src/lib/datetime.ts`、`src/modules/analytics/weekly-result.ts`（G-5）、
  `search-metrics.ts`（G-2）、`index-status.ts`（G-3）、G-6・G-7
- 状態：**解決（2026-08-11）。(a) を採用し、3か所まとめて直した**

決定内容：

- **`jstDateColumn(date)` を `src/lib/datetime.ts` に追加した。** `Date.UTC(y, m, d)` を返す。
  `date` 型の列にはこれを渡す
- **`startOfJstDay` は残す。** `timestamptz` の範囲検索（`jstDayRange`・`atJstTime`）では
  こちらが正しい。**消すと今度は範囲検索が9時間ずれる**
- 書き手3か所（G-5・G-2・G-3）を `jstDateColumn` に付け替えた
- **PostgreSQL が実際に何を持っているかを試験で固定した**
  （`select metric_date::text`）。JSの `Date` を見るだけでは、
  この不具合はまた通り抜ける

理由：

- **いま直すのが最も安い。** 本番にはまだ1行も入っていない（Phase 0 未開始）。
  実験が始まると**古い行と新しい行で基準が変わり**、どちらの日付なのかを
  行ごとに判断することになる
- G-6（集計）と G-7（画面）はこの列を読む。**ずれたまま集計を積むと直す範囲が広がる**
- Q-005 の決定（「Search Console が返した日付をそのまま入れる」）と
  実装が食い違っていた。**決定のほうが正しい**

**再発を防ぐために：** `jstDateColumn` の doc に、どちらをどこで使うかの表を置いた。
`date` 型の列を新しく足すときは必ずこちらを使う。

---

### Q-032 SPEC 10.2 のうち「広告クリック」と「PV」に記録する経路が無い

- 発生タスク：G-6（TASKS.md）
- 状況：G-6 の完了条件は「**SPEC 10.2の記録条件が全て保存される**」。
  19項目を1つずつ当たった結果、**17項目は保存先がある**が、
  **2項目は測る手段そのものが無い。**

  | SPEC 10.2 の項目 | 保存先 | 状態 |
  |---|---|---|
  | ジャンル | `blogs.genre_id` → `genres.name` | ✅ |
  | 競争レベル | `genres.competition_level` | ✅ |
  | YMYLリスク | `genres.ymyl_risk` | ✅ |
  | 運営戦略 | `experiment_groups.strategy_type` | ✅ |
  | 更新頻度 | `experiment_groups.settings`（jsonb） | ✅（形は運用で決める） |
  | 記事数 | `content_items` を数える | ✅ |
  | 記事平均文字数 | `article_versions.body_html` から数える | ✅ |
  | 収益記事比率 | `content_items.content_type` | ✅ |
  | リライト数 | `article_versions.version_no` | ✅ |
  | 内部リンク数 | `content_items.inbound/outbound_link_item_ids` | ✅ |
  | バナー位置 | `banners.slot` | ✅ |
  | 承認率 | `approvals.status` | ✅ |
  | 修正率 | `revision_requests` | ✅ |
  | AI費用 | `ai_usage_logs.cost_usd` | ✅ |
  | 検索表示・検索クリック | `metrics_daily`（G-2） | ✅ |
  | アフィリエイトクリック | `metrics_daily`（**G-6 で実装**） | ✅ |
  | 成果・収益 | `metrics_daily`（G-5） | ✅ |
  | **広告クリック** | `metrics_daily.banner_clicks` | ❌ **数える経路が無い** |
  | （PV・SPEC 11.2） | `metrics_daily.page_views` | ❌ **GA4 が未実装** |

  **1. 広告クリック。** 列（`banner_clicks` / `banner_impressions`）はあるが、
  **バナーの表示もクリックも記録するテーブルが無い。** アフィリエイトリンクは
  自前のリダイレクタ（D-8）を通るので数えられるが、**バナーは
  `destination_url` へ直接飛ぶ**（B-8）。

  これは **SPEC 20.2 が「該当Phase開始前に決定する項目」の6番目に挙げている
  「広告クリックの計測方式」そのもの**である。Q-030 と同じ形。

  **2. PV。** `GA4_FETCH` というジョブの種類は SPEC 4.3 にあるが、
  **TASKS に GA4 のタスクが1つも無い**（`grep` で確認）。
  SPEC 20.2 の5番目「GA4必須化の有無」も未決。

- 選択肢（広告クリック）：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **バナーもリダイレクタを通す** | D-8 の仕組みをそのまま使える。**公開済み記事のバナーを貼り替えることになる**（Q-001 が同じ理由でリダイレクタを先に決めた） |
  | (b) | 表示・クリックを記録する経路を別に作る | テーブルとエンドポイントが要る。バナーは記事本文に埋まるため、**WordPress 側にJSを置く必要がある** |
  | (c) | Phase 0 では測らない | `banner_clicks` は0のまま。**SPEC 10.2 の項目が1つ欠ける** |

- 影響範囲：`prisma/schema.prisma`、`src/modules/banners/`、B-8、G-6、G-7、
  `docs/SPEC.md` 10.2
- 状態：**解決（2026-08-11）。広告クリックは D-12 に含める。PV は Phase 0 では測らない**

**広告クリック：バナーも `/go/{code}` を通す。** W-6 でリダイレクタを各ブログの
ドメインへ移す（D-12）ので、**同時にやれば追加コストがほぼゼロ**。
D-12 の後に単独でやると、公開済み記事のバナーを貼り替えることになる
（Q-001 が「後から入れると貼り替えになる」としたのと同じ構図）。
**D-12 の完了条件に含める。**

**PV：測らない。** GA4 のタスクが TASKS に1つも無く、SPEC 20.2 の
「GA4必須化の有無」も未決。**検索表示・クリック（G-2）とアフィリエイトクリック（G-6）が
あれば、ROADMAP 4章の先行指標は判定できる。**
`metrics_daily.page_views` は `NULL` のまま残す（0を入れない — 測ったと読める）。

**G-6 は17項目まで実装した。** 残る2つは**測る手段の決定**であって、
集計の実装で埋まるものではない。**推測で `banner_clicks` に何かを入れない** —
0のままなら「測っていない」と読めるが、それらしい数字を入れると
「測ったが少ない」と読めてしまう。

**(a) は早いほど安い。** Q-001 が「リダイレクタを後から入れると公開済み記事の
リンクを全て貼り替えることになる」としてPhase A着手前に決めたのと同じ理由が、
バナーにもそのまま当てはまる。

---

### Q-033 A-2-R を「スキーマと文書のみ」で出せない

- 発生タスク：A-2-R（作業指示書 W-5）
- 状況：W-5 の禁止事項は「実装コードを含めない。**スキーマと文書のみ**」だが、
  **対象の3モデルはすでに広く実装されている。**

  | 指示された変更 | 壊れるもの |
  |---|---|
  | `UserPersona` → `Persona`（`@unique` 除去） | `personas` モジュール全体 |
  | `Blog.persona_id` を**必須**で追加 | ブログ作成の全経路と全テストのフィクスチャ |
  | `BlogPersonaSetting` から `target_reader` / `allowed_experiences` を削除 | `content-generation` の事実チェック、記事生成 |
  | `PersonaFact.user_id` → `persona_id`、`blog_id` 削除 | `personas/facts.ts`、`users/withdrawal.ts`、E-12 |

  参照しているファイルは**20件**（うちテスト9件）。`target_reader` は32ファイルに現れる。

  **スキーマだけ変えると `typecheck` と `build` が落ち、CIが赤のままマージできない。**
  「スキーマと文書のみ」と「CIが緑であること」は同時に満たせない。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | 1PRでスキーマ＋既存コードの追随まで | W-5 の禁止事項に反する。差分が20ファイル超でレビューが難しい |
  | (b) | スキーマのみで出す | **CIが赤。マージできない。** 後続の W-7・W-8 も止まる |
  | (c) | **3タスクに分ける** | ①`Persona` を追加し `UserPersona` を残す ②コードを移す ③旧モデルを落とす。**各段階でCIが緑** |

- 影響範囲：`prisma/schema.prisma`、`src/modules/personas/`、
  `src/modules/content-generation/`、`src/modules/content-planning/`、
  `src/modules/users/`、`docs/DATA_MODEL.md`、W-7・W-8
- 状態：**解決（2026-08-11）。(c) を採用し、A-2-R-1 / A-2-R-2 / A-2-R-3 に分割**

理由：

- **途中で止めても壊れない。** ①だけ入った状態でも既存の機能はそのまま動く
- **1PRの差分が読める大きさに収まる。** 20ファイルの破壊的変更を一度に見るより、
  「足す」「移す」「消す」が分かれているほうが誤りに気づける
- ①のあいだ `Persona` と `UserPersona` が並存するが、**③で必ず消える**。
  並存を残さないために③まで含めて1つのタスク群として起票した
- ROADMAP 8章の「A-2-R のPRが通ってから文書修正」は、**③の完了**をもって満たす

---

### Q-034 段階解放の「参加開始日」をどこから取るか

- 発生タスク：A-2-R-2（ROADMAP 5章）
- 状況：ROADMAP 5章が「各モニターについて、**参加開始日を起点に**適用する」
  （1〜30日は1体、31〜60日は2体、61〜90日は3体）と定めているが、
  **参加開始日にあたる列が無い。**

  候補は2つあり、**最大で数週間ずれる。**

  | 候補 | 意味 | 問題 |
  |---|---|---|
  | `users.created_at` | LIFFから登録した時刻 | **登録しただけでは参加していない。** `INVITED` のままではアプリを使えず（H-1）、ADMINが承認するまで何も始まらない |
  | ADMINが `ACTIVE` にした時刻 | 実際に使い始められるようになった時刻 | **保存していない。** H-1 は `users.status` を更新するだけで、時刻を残していない |

  ROADMAP 5章の「第1段階の2名が90日を終える頃、第3段階の5名はまだ1分身」という
  記述は、**モニターごとに参加時期がずれる**ことを前提にしている。
  登録時刻を起点にすると、承認待ちの期間まで日数に含まれ、
  **使い始める前に2体目が開く**ことが起きうる。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **`users.activated_at` を足す** | H-1 の承認時に入れる。マイグレーションが1本要る。**意味が正確** |
  | (b) | `users.created_at` を使う | 実装ゼロ。**承認待ちが長い人ほど早く枠が開く** |
  | (c) | 分身ごとの `activated_at` だけで運用する | 段階解放をやめる。ROADMAP 5章と食い違う |

- 影響範囲：`prisma/schema.prisma`、`src/modules/users/`（H-1）、
  `src/modules/personas/`、H-2 のオンボーディング
- 状態：**解決（2026-08-11）。(a) を採用し `users.activated_at` を足した**

決定内容：

- **`users.activated_at` を追加**（nullable）。H-1 の `ACTIVATE` で入れる
- **最初に認めた1回だけ入れる。** 二度目の承認でも、停止・再開でも動かさない
- 段階解放は `activatePersonaForUser` で効かせる。
  **参加を認められる前（`activated_at` が `NULL`）は段階解放を効かせない** —
  ここで断ると「作れるのに使い始められない」状態になり、原因が画面から読めない。
  参加前にアプリを使えないことは `isActiveUser`（H-1）が別に担保している
- **断る文言に経過日数を出す。** 「上限です」だけでは、いつ開くのか分からない

理由：

- 「いつ参加したか」は段階解放だけでなく、90日検証の期間（ROADMAP 4章）と
  継続率（SPEC 11.1「8週間継続率」）の分母にも要る。
  **登録時刻で代用すると、3か所すべてが同じだけずれる**
- 承認待ちの期間まで日数に含めると、**使い始める前に2体目が開く**

---

### Q-035 オンボーディングに分身を作る段が無い

- 発生タスク：A-2-R-4-schema（H-2 の前提）
- 状況：人格中心モデルへの移行が完了し、**ブログは `ACTIVE` の分身が無いと作れない**
  （`blogs.persona_id` は `NOT NULL`、`POST /api/blogs` は `DRAFT` を断る）。

  一方 **SPEC 6.1 のオンボーディング10段には、分身を作る段が無い。**

  | 段 | 内容 | いまの実装との関係 |
  |---|---|---|
  | 1 | LINE連携 | — |
  | 2 | 利用規約同意 | — |
  | 3 | データ利用同意 | — |
  | 4 | 目標登録 | **分身の `business`（収益方針・目標・KPI・撤退条件）と重なる** |
  | 5 | **1〜3ブログ枠作成** | **分身が無いと通らない** |
  | 6〜10 | WordPress接続・ジャンル設定・案件登録・通知設定・完了 | — |

  SPEC 6.1 は人格中心モデル（ROADMAP 2章）より前に書かれたもので、
  **矛盾ではなく、まだ揃えていない**（ROADMAP 8章の文書整合、W-7 の範囲）。

  **`docs/SPEC.md` は実装タスクで書き換えない**（TASKS 0章）ので、
  ここに記録して判断を仰ぐ。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **段4を「分身を作る」に置き換える** | 目標（`business`）は分身の一部なので、いま2つに分かれているものが1つになる。段数は10のまま |
  | (b) | 段を1つ足して11段にする | 「目標登録」を残し、その前に「分身を作る」を置く。**同じことを2回聞くことになる** |
  | (c) | ブログ作成時に分身も同時に作る | 段は増えないが、**分身とブログが1操作になり、「分身が主、媒体が従」が画面から読めなくなる** |

- 影響範囲：`docs/SPEC.md` 6.1、H-2、`src/app/liff/onboarding/`
- 状態：**解決（2026-08-12）。(a) を採用する**

決定内容：

- **オンボーディングの段4「目標登録」を「分身を作る」に置き換える。** 段数は10のまま
- 段4で作った分身を **`ACTIVE` にするところまで**を段4に含める。
  段5「1〜3ブログ枠作成」がその分身を選ぶ
- **段階解放（ROADMAP 5章）により、初日に作って使い始められるのは1体。**
  段5で作れるブログも1件になる。「1〜3ブログ枠作成」の文言は
  **「まず1つ」に読み替える**（W-7 で SPEC を揃える）

理由：

- **目標（収益方針・月間目標額・KPI・撤退条件）は分身の `business` そのもの。**
  いま2つに分かれているものが1つになる
- (b) は同じことを2回聞くことになる
- (c) は分身とブログが1操作になり、**「分身が主、媒体が従」が画面から読めなくなる**

**`docs/SPEC.md` 6.1 の書き換えは W-7（ROADMAP 8章の文書整合）で行う** —
実装タスクで仕様書を書き換えない（TASKS 0章）。

補足（実装側の現状）：

- 分身のCRUDは `src/modules/personas/`（A-2-R-2a・2b）にあるが、
  **HTTPの入口も画面も無い。** `POST /api/personas` 相当は未実装
- そのため、**いまはブログを1件も作れない**（分身を作る経路が無いため）。
  H-2 の前に、分身の入口を作るタスクが要る

---

### Q-036 週の公開上限を 3〜5 にするか（列と範囲の食い違い）

- 発生タスク：C-9-schema（作業指示書 W-8）
- 状況：W-8 は `blogs` に **`weekly_post_cap Int`（3〜5）** を足すよう指示している。
  しかし**同じものが既にある。**

  | | いまあるもの | W-8 の指示 |
  |---|---|---|
  | 置き場所 | `blogs.article_ratio.weeklyPublishCap`（jsonb） | `blogs.weekly_post_cap`（列） |
  | 範囲 | **1〜4** | **3〜5** |
  | 根拠 | SPEC 2.2「週4本を超えて公開する処理を実装してはならない」 | W-8「G-8 により実測で調整する」 |

  **列を足すと、週の上限が2か所になる。** どちらが正か分からなくなる。

  範囲を 3〜5 へ広げるには **SPEC 2.2 の書き換えが要る**が、
  **実装タスクで仕様書を書き換えない**（TASKS 0章）。

- 選択肢：

  | | 案 | 内容 |
  |---|---|---|
  | (a) | **既存の `article_ratio.weeklyPublishCap` を使い、範囲の変更は SPEC 修正と G-8 に委ねる** | C-9 は列を足さない。G-8 の着手前に SPEC 2.2 と 9.2.7 を直す（W-8 の「あわせて行うドキュメント修正」） |
  | (b) | `weekly_post_cap` 列を足す | W-8 の字面どおり。**同じものが2か所になる** |
  | (c) | C-9 で SPEC 2.2 も直す | 1タスクで仕様と実装を同時に変える。**TASKS 0章に反する** |

- 影響範囲：`prisma/schema.prisma`、`src/modules/blogs/article-ratio.ts`、
  `docs/SPEC.md` 2.2・9.2.7、`docs/ROADMAP.md` 6章、G-8
- 状態：**C-9-schema では (a) で進めた。** 範囲を 3〜5 にするかは未決

補足：

- **C-9 は列を足していない。** 週の上限は `article_ratio.weeklyPublishCap`
  のまま（1〜4）
- **G-8 に着手する前に決める必要がある。** G-8 の完了条件が
  「インデックス率 80%以上 → `weekly_post_cap` を +1（上限5）」なので、
  範囲が 1〜4 のままだと 5 へ上げられない
- あわせて **W-8 が挙げているドキュメント修正**（SPEC 2.2・9.2.7、
  ROADMAP 6章の記事数を「28〜34本」へ）も、この判断とまとめて行う

---

### Q-001 の再決定（2026-08-11）：リダイレクタを各ブログのドメイン配下へ

2026-08-07 の決定（自前リダイレクタ・案件ごとに `REDIRECT` / `DIRECT` を切り替える）
のうち、**リダイレクタをどのドメインに置くか**を変更する。

#### 変更点

| | 旧（2026-08-07） | 新（2026-08-11） |
|---|---|---|
| リダイレクトの場所 | **Bunshin の共通ドメイン** `/go/{code}` | **各ブログのドメイン** `https://example.com/go/{code}` |
| 実装 | Next.js のルート（D-8・実装済み） | 各WordPressのスニペット ＋ Bunshin の**受信API** |
| クリックの記録 | リダイレクトの中で直接 `link_clicks` へ | WordPress から送信 → 受信APIが `link_clicks` へ |

**案件ごとの切り替え（`link_mode`）・サブID（`sub_id_param`）・302・
`rel="nofollow sponsored"`・IPを保存しないことは、いずれもそのまま。**

#### 理由

**30ブログが同一の外部ドメインへリンクすると、運営者の同一性を示す痕跡になる。**
計測の一元化より footprint の回避を優先する。

Phase 0 は「1人が3ブログを別名で運営する」形（SPEC 1.2）であり、
**共通ドメインへのリンクは、その3つが同じ運営者であることを外から辿れるようにする。**
30ブログ分が集まれば、なおさら分かりやすくなる。

#### 仕組み

1. Bunshin が `{code}` と遷移先の対応を持つ（`affiliate_links`。変更なし）
2. 各WordPressに軽量なスニペット（mu-plugin）を入れ、`/go/{code}` を処理する
3. WordPress側が記録して Bunshin の受信APIへ送る
   - `code`、アクセス日時、リファラの**ホスト名**、AI検索経由か
4. Bunshin は受信したものを `link_clicks` に保存し、日次で `metrics_daily` へ集計する（G-6）
5. **送信に失敗したらWordPress側で保持し、次回まとめて送る**

#### 引き継ぐ必須要件

- `/go/` を `robots.txt` で `Disallow`
- リンクには `rel="nofollow sponsored"`
- リダイレクトは **302**（301はブラウザが覚えてクリックを数えられない）
- **受信APIはブログ単位のトークンで認証する。他ブログのイベントを投入できないこと**
- **個人情報を保存しない。** IPアドレスは保存せず、User-Agent はハッシュ化する

#### 記事生成への影響

E-10 で本文へ入れるアフィリエイトリンクは、**必ず自ブログの `/go/{code}` 形式**とする。
**ASPから取得した生のアフィリエイトURLを本文に書かない**（`REDIRECT` の案件の場合）。

#### 実装済みのものをどうするか

**D-8 は Bunshin 側のルートとして実装・マージ済み**（`src/app/go/[code]/`）。
この再決定により、**そのルートは受信APIへ置き換わる。** 置き換えは D-8 の
やり直しではなく**別タスク（D-12）**として起票する — 完了したタスクの
完了条件を後から変えると、何が済んでいて何が済んでいないのかが読めなくなる。

**`link_clicks` と `affiliate_links` はそのまま使える。** 変わるのは
「誰がその行を作るか」だけで、テーブルの形も集計（G-4・G-6）も影響を受けない。
