# IMPLEMENTATION HISTORY

決定と変更の履歴。**何を作ったか**ではなく、**何をどう決めたか**を残す。作業内容は `docs/IMPLEMENTATION_STATUS.md` と各PRを参照。

## 書き方

- 判断が伴った変更のみ記録する。定型作業は書かない
- 決定は「何を選んだか」と「なぜ他を選ばなかったか」をセットで書く
- OPEN_QUESTIONS の解決は必ず記録する

---

| 日付 | 内容 |
|---|---|
| 2026-08-06 | 仕様書 v2.0 → v2.1 へ改訂（構成表ジェネレーター新設ほか8件） |
| 2026-08-06 | タスク分解表を作成（52タスク） |
| 2026-08-06 | `prisma/schema.prisma` を設計（A-2）。SPEC 5章の疑似記法から26テーブル・30 enum を確定 |
| 2026-08-06 | 構成表生成のAI仕様を作成 |
| 2026-08-06 | **Q-003 解決。** ソースの配置を `src/` 配下とする。SPEC 4.2 を唯一の正とし、TASKS.md の「主な変更先」欄の表記を修正した（#2）。`prisma/` は直下のまま |
| 2026-08-06 | A-1 完了（#1）。Next.js 16 / React 19 / TypeScript 5.9。Tailwind は SPEC 4.1 の推奨スタックに含まれるため導入 |
| 2026-08-06 | A-3 完了（#3）。**環境変数は「それを使うタスクで追加する」方針を採用。** LINE・AI・暗号化キーは SPEC に名前が定義されておらず、先に命名すると実装タスクで改名が発生するため。検証対象は `DATABASE_URL` `NODE_ENV` のみ |
| 2026-08-06 | A-4 完了（#4）。ロガーのマスク対象から `key` `auth` 単体を除外。`primary_keyword` `author` を巻き込むと事故調査に必要な情報が消えるため |
| 2026-08-06 | A-4 で `src/lib/entitlements.ts` を追加。将来のオプション課金に備えた権限判定の入口。Phase 0 は常に `true` |
| 2026-08-06 | A-4 で `docs/MODULE_RULES.md` を作成。整理中に `jobs → wordpress → jobs` と `approvals ↔ line` の循環が判明し、前者はハンドラ登録を `src/app/` 側へ、後者は受信をジョブ経由にすることで解消 |
| 2026-08-06 | `docs/DATA_MODEL.md` に10章（日時とタイムゾーン）と11章（記事本文の正本）を追加（#5）。記事の正本判定は生成時刻の比較ではなく content hash で行う。AIのリライトは常に後から実行されるため、時刻比較では利用者の修正が必ず失われる |
| 2026-08-06 | A-5 完了（#6）。CI を GitHub Actions に構築。**A-2 のスキーマが実PostgreSQLに適用できることを初めて実証**（26テーブル・30 enum） |
| 2026-08-06 | **Q-004 起票。** Prisma 7 が `datasource` の `url` を廃止しており、A-2 のスキーマが検証を通らない。A-5 では Prisma 6 系に固定して回避。移行判断は B-1 着手前 |
| 2026-08-06 | A-7「日時ヘルパー」を Phase A に追加（#7）。DATA_MODEL 10章で `src/lib/datetime.ts` への集約を定めたが、それを作るタスクが無かった。E-9 と G-2・G-6 の両方から使われるため単独タスクとした。タスク総数 52 → 53 |
| 2026-08-06 | A-6 完了。`docs/ARCHITECTURE.md` と `docs/README.md` を作成。README を Phase A の実態に更新 |
