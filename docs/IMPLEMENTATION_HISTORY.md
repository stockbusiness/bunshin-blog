# IMPLEMENTATION HISTORY

決定と変更の履歴。**何を作ったか**ではなく、**何をどう決めたか**を残す。作業内容は `docs/IMPLEMENTATION_STATUS.md` と各PRを参照。

## 書き方

- 判断が伴った変更のみ記録する。定型作業は書かない
- 決定は「何を選んだか」と「なぜ他を選ばなかったか」をセットで書く
- OPEN_QUESTIONS の解決は必ず記録する

---

| 日付 | 内容 |
|---|---|
| 2026-08-06 | 仕様書 v2.0 → v2.1 へ改訂（構成表ジェネレーター新設ほか8件） |
| 2026-08-06 | タスク分解表を作成（52タスク） |
| 2026-08-06 | `prisma/schema.prisma` を設計（A-2）。SPEC 5章の疑似記法から26テーブル・30 enum を確定 |
| 2026-08-06 | 構成表生成のAI仕様を作成 |
| 2026-08-06 | **Q-003 解決。** ソースの配置を `src/` 配下とする。SPEC 4.2 を唯一の正とし、TASKS.md の「主な変更先」欄の表記を修正した（#2）。`prisma/` は直下のまま |
| 2026-08-06 | A-1 完了（#1）。Next.js 16 / React 19 / TypeScript 5.9。Tailwind は SPEC 4.1 の推奨スタックに含まれるため導入 |
| 2026-08-06 | A-3 完了（#3）。**環境変数は「それを使うタスクで追加する」方針を採用。** LINE・AI・暗号化キーは SPEC に名前が定義されておらず、先に命名すると実装タスクで改名が発生するため。検証対象は `DATABASE_URL` `NODE_ENV` のみ |
| 2026-08-06 | A-4 完了（#4）。ロガーのマスク対象から `key` `auth` 単体を除外。`primary_keyword` `author` を巻き込むと事故調査に必要な情報が消えるため |
| 2026-08-06 | A-4 で `src/lib/entitlements.ts` を追加。将来のオプション課金に備えた権限判定の入口。Phase 0 は常に `true` |
| 2026-08-06 | A-4 で `docs/MODULE_RULES.md` を作成。整理中に `jobs → wordpress → jobs` と `approvals ↔ line` の循環が判明し、前者はハンドラ登録を `src/app/` 側へ、後者は受信をジョブ経由にすることで解消 |
| 2026-08-06 | `docs/DATA_MODEL.md` に10章（日時とタイムゾーン）と11章（記事本文の正本）を追加（#5）。記事の正本判定は生成時刻の比較ではなく content hash で行う。AIのリライトは常に後から実行されるため、時刻比較では利用者の修正が必ず失われる |
| 2026-08-06 | A-5 完了（#6）。CI を GitHub Actions に構築。**A-2 のスキーマが実PostgreSQLに適用できることを初めて実証**（26テーブル・30 enum） |
| 2026-08-06 | **Q-004 起票。** Prisma 7 が `datasource` の `url` を廃止しており、A-2 のスキーマが検証を通らない。A-5 では Prisma 6 系に固定して回避。移行判断は B-1 着手前 |
| 2026-08-06 | A-7「日時ヘルパー」を Phase A に追加（#7）。DATA_MODEL 10章で `src/lib/datetime.ts` への集約を定めたが、それを作るタスクが無かった。E-9 と G-2・G-6 の両方から使われるため単独タスクとした。タスク総数 52 → 53 |
| 2026-08-06 | A-6 完了。`docs/ARCHITECTURE.md` と `docs/README.md` を作成。README を Phase A の実態に更新 |
| 2026-08-06 | A-7 完了。`src/lib/datetime.ts` を追加。JSTは夏時間を持たないため固定オフセットで計算し、`Intl` に依存させない |
| 2026-08-06 | **Q-005 起票。** Search Console の日次データは時刻を持たない日付文字列で、UTCの1日はJSTの2日にまたがる。時間単位の内訳が無い以上JSTの暦日へ割り直せないため、どの暦日に載せるかの取り決めが要る。A-7 では実装せず G-2 の判断に委ねた |
| 2026-08-06 | **Q-004 解決。Phase 0 は Prisma 6 系で進める。** 7系で得られるもののうち Phase 0 で使う予定のものが無い一方、26テーブルのスキーマがCIで実DBへ適用できている現状を崩すリスクがある。移行の検討は Phase 0 終了後 |
| 2026-08-07 | A-8 完了。初期マイグレーションをコミットし、CIを `db push` から `migrate deploy` へ変更。適用後のDBと `schema.prisma` の乖離検出を追加した。DATA_MODEL 4章のCHECK制約2件は Prisma で表現できないため手で追記し、実際に効くことをCIで確認している |
| 2026-08-07 | **Q-006 起票・解決。** `blogs.targetReader` に `@map` が無く、DBのカラム名が snake_case にならなかった。実カラム26テーブル分でこの1件のみ。**初期マイグレーション適用前に `@map("target_reader")` を追加**して解消。適用後だと `RENAME COLUMN` の追加マイグレーションと B-3・B-5・G-7 への波及が必要だった |
| 2026-08-07 | A-8 で `prisma/schema.prisma` を変更した（Q-006 の1行のみ）。A-2 完了後のスキーマ変更にあたるが、初期マイグレーションと不可分であり、まだ適用済みDBが存在しないため同一PRで扱った |
| 2026-08-07 | B-1 完了。LIFF の IDトークン検証を実装。SPEC 13.1 に従いLINEの検証エンドポイントに委ねたうえで、**`iss` / `aud` / `exp` をこちら側でも再確認する**。検証結果の解釈を相手側の実装だけに預けないため |
| 2026-08-07 | B-1 で、認証の失敗理由をクライアントへ返さない方針を採用。署名不正・期限切れ・チャネル不一致を区別して返すと、攻撃者にトークンの改竄結果を教えることになる。理由はログにのみ残す |
| 2026-08-07 | B-1 で `LINE_LOGIN_CHANNEL_ID` を環境変数に追加（A-3 の「変数はそれを使うタスクで追加する」方針に従う） |
| 2026-08-07 | B-2 完了。セッションは署名付きCookieを採用。モニター10名の規模でセッションストアを持つ必要が無く、DBへの往復も増やしたくないため。**CookieにはユーザーIDと期限だけを入れ、ロールと同意状態は毎回DBを見る**（権限はく奪が効かなくなるのを防ぐ） |
| 2026-08-07 | B-2 で同意チェックを `requireConsentedUser` に集約。各Route Handlerが個別に確認すると必ず書き忘れが出るため。オンボーディング用に同意を見ない `requireUser` を分けた |
| 2026-08-07 | B-2 で同意時刻を上書きしない実装にした。「いつ同意したか」はデータ利用の根拠になるため、後の操作で書き換えない |
| 2026-08-07 | **`docs/MODULE_RULES.md` の依存の向きに誤りがあったため修正。** 図の矢印が「import する側 → される側」と説明されていながら、`users --> blogs` `blogs --> wordpress` など実際の依存と逆の記載が混在していた。B-2 で `auth → users` を実装する際に判明 |
| 2026-08-07 | A-9 完了。統合テスト基盤を追加し、B-3 の所有権検証を実DBで検証できるようにした。**fake DB では「SQLの条件そのもの」が他人の行を弾いている証明にならない**ため、土台を作る前に検証手段を用意した。越境テストの中身は C-6 に残す |
| 2026-08-07 | A-9 のリセット処理は、テーブルを列挙せず `pg_tables` から動的に集めて `TRUNCATE ... CASCADE` する。列挙するとテーブル追加のたびに更新漏れが起き、`deleteMany` を並べると外部キーの削除順に依存するため |
| 2026-08-07 | B-3 完了。所有権検証を `src/modules/blogs/ownership.ts` に集約。**所有していない資源は 403 ではなく 404 を返す**方針を採用した。403 だと「そのIDは存在するが他人のものだ」と伝わり、IDの総当たりで他ユーザーの資源の有無を調べられるため。存在しないIDとの応答が同一であることをテストで固定した |
| 2026-08-07 | B-3 で更新・削除に `updateMany` を所有権付きで使う。Prisma の `update` は主キーのみで対象を決めるため、`where` に所有権を含められない |
| 2026-08-07 | B-3 の削除は物理削除せず `CLOSED`（SPEC 13.2）。一覧では既定で除外する |
| 2026-08-07 | **Q-007 起票・解決。ブログのWordPress接続先を途中で別サイトに変更することを禁止。** 接続先が変わると `wordpress_posts`・`metrics_daily`・Search Console のデータが混ざり、実験データとして読めなくなるため。ただし**同一サイトのままの認証情報の入れ替えは許可する**。塞ぐとパスワード漏洩時や権限変更時に復旧できなくなる |
| 2026-08-07 | **Q-008 起票・解決。`CLOSED` にしたスロットの再利用を禁止。** 再利用を許すには `UNIQUE(user_id, slot_number)` を部分一意索引へ差し替えるマイグレーションが必要で、`(user_id, slot_number)` が期間を通じた識別子でなくなる。B-4 の完了条件に無い変更を入れるだけの価値が無いと判断した |
| 2026-08-07 | **設定ミスの救済を「作り直し」から「ADMIN の手動介入」へ変更。** Q-007（接続先変更の禁止）と Q-008（スロット再利用の禁止）が重なると、オンボーディングでの接続ミスが1/3の枠を恒久的に潰す。ADMIN が `site_url` の変更とスロットの解放を行えることとし、**記事を投稿していないブログに限る**（投稿済みを消すと実測データが失われる）。Phase 0 では専用UIを作らずSQLで行う |
| 2026-08-07 | B-4 完了。3件上限とスロット制御を実装。**スロット番号はサーバーが決める**（省略時は空いている最小の番号）。一覧APIが `CLOSED` を返さない以上、クライアントは空きスロットを計算できず、番号を選ばせると必ず埋まった番号を指定する経路が残るため |
| 2026-08-07 | B-4 の判定を `src/modules/blogs/slots.ts` にDBを触らない純粋関数として置いた。上限・重複・`CLOSED` の再利用という3つの分岐は、実DBを起こさずに固定できたほうが壊れたときに原因が分かる。実DBでは「使用状況の集計が `CLOSED` を拾えているか」を確認する |
| 2026-08-07 | B-4 で上限判定を重複判定より先に行う。3件埋まった状態で「スロット2は使用中」とだけ返すと、他の番号なら入ると読めてしまう。一方で**範囲外の指定は上限より先に返す**（入力の誤りを「上限到達」と伝えると原因が分からなくなる） |
| 2026-08-07 | B-4 で `UNIQUE(user_id, slot_number)` と `CHECK(slot_number BETWEEN 1 AND 3)` を残したまま、アプリ層に同じ判定を置いた。DB制約は同時実行に対する最後の砦で、アプリ層は理由の分かるエラーを返すためのもの。役割が違うので二重に持つ |
| 2026-08-07 | B-8「LIFFクライアント基盤」を Phase B に追加。B-1 が実装したのはサーバー側のIDトークン検証で、**ブラウザ側でLIFF SDKを初期化してIDトークンを取り出す部分がどのタスクにも無かった**。B-5・F-4・F-5・G-5・H-2 が同じものを使うため、最初のLIFF画面である B-5 に含めず単独タスクとした。タスク総数 55 → 56 |
| 2026-08-07 | **Q-009・Q-010・Q-011 起票。** B-5 の完了条件「全項目を編集できる」の「全項目」が確定しない。SPEC 6.1 が設定画面に挙げる**ジャンル**は E-4 の審査結果で自由入力ではなく、**通知設定**は `monitor_profiles`（ユーザー別）にあってブログ別の置き場所が無く、**新規記事・改善提案比率**は保存先そのものが無い。推測で作ると E-4・F-3・E-9 の実装時に作り直しになるため、B-5 は決定待ちとした |
| 2026-08-07 | **Q-009 解決。ジャンルは設定画面から変更できない（表示のみ）。** 変更は E-4 の審査を経由する。停止条件（YMYL該当・案件0件・上位10件の寡占）はジャンル選定の中核（SPEC 9.2.2）で、設定画面から素通りできる経路を作ると審査の意味が無くなる |
| 2026-08-07 | **Q-010 解決。通知設定はユーザー単位のまま**とし、ブログ設定画面から外した。1日1件はモニターに届く総量の話（F-3「3ブログ合計で制限される」）で、ブログごとに曜日と時刻を分けると3ブログで最大3倍の通知が出る。編集の入口は H-2。スキーマ変更なし |
| 2026-08-07 | **Q-011 解決。「新規記事・改善提案比率」は B-5 で扱わない。** 保存先を先に決めず、比率を実際に読む E-9 で判断する。あわせて **`article_ratio.revenue` / `traffic` を利用者に編集させない**ことを決めた。SPEC 9.2.4 の算出値であり、編集させると SPEC 9.2「判定は必ずコード側で行う」に反する。設定画面で編集できるのは `weeklyPublishCap` のみ |
| 2026-08-07 | B-8 完了。LIFFの初期化からセッション確立までを `src/app/liff/layout.tsx` の1か所に集約した。各画面が個別に `liff.init()` を呼ぶと、初期化の重複と失敗時の扱いのばらつきが必ず出るため |
| 2026-08-07 | B-8 で、手順を `src/lib/liff/` の純粋関数に置き、SDK と `fetch` を引数で受け取る形にした。**`@line/liff` はブラウザでしか動かず、実物に依存したままでは分岐を検証できない**（A-9 で fake DB を避けたのと逆向きの判断で、ここは外部SDKを差し替える側が正しい） |
| 2026-08-07 | **`NEXT_PUBLIC_LIFF_ID` を `src/lib/env.ts` の起動時検証に含めない。** ビルド時にバンドルへ焼き付く値であり、サーバー起動時に確認しても既に決まった値を見るだけで実際の失敗を防げない。設定漏れはブラウザ側で検出して画面に出す |
| 2026-08-07 | B-8 で `docs/MODULE_RULES.md` にルール4「ブラウザで動くコードからサーバー専用のコードを import しない」を追加。`src/lib/` にサーバー専用（`db.ts` `env.ts`）とブラウザ側（`liff/`）が混在するようになったため。**`process.env` をオブジェクトのまま渡してキーで引くとブラウザで `undefined` になる**点も明記した（Next.js が置き換えるのは `process.env.NEXT_PUBLIC_XXX` の形だけ） |
| 2026-08-07 | B-8 の完了条件を「LIFF外で開くと案内が出る」から「設定漏れ・初期化失敗・認証失敗のときに案内が出る」へ改めた。LIFF は外部ブラウザでも `init()` が成功し LINE Login へ遷移するため、当初の文言は実際の挙動と合っていなかった |
| 2026-08-07 | B-5 完了。ブログ設定画面を実装。編集できるのは名前・ペンネーム・想定読者・収益方針・投稿頻度・状態で、**ジャンルと算出値は「変更できない項目」として理由付きで表示する**。触れない項目を黙って並べると設定漏れだと思われて問い合わせになる |
| 2026-08-07 | B-5 で `article_ratio` の部分更新を実装。**jsonb は部分更新ができない**ため、`weeklyPublishCap` を変えるときは現在値を読んでから差し替える。上限だけを受け取って全体を組み立て直すと、SPEC 9.2.4 の算出値が既定値で上書きされる |
| 2026-08-07 | B-5 で `parseArticleRatio` を「壊れた値でも例外を投げない」実装にした。jsonb は型を保証せず、想定外の形が入っていても設定画面が開けなくなるのは避けたいため。欠けた項目だけを既定値で埋める |
| 2026-08-07 | B-5 で `AppBlog` に `articleRatio` と `genre` を追加。ジャンルは `blogs` モジュールの所有テーブル（MODULE_RULES）なので同モジュールから引く |
| 2026-08-07 | B-5 のブラウザ側は `AppBlog` を使わず `BlogJson` を別に定義した。**JSON を通ると `Date` は文字列になる**ため、サーバー側の型を借りると `launchDate.getTime()` のような実行時に落ちるコードが型検査を通る |
| 2026-08-07 | B-5 で `weeklyPublishCap` の検証より所有権の確認を先に行う。不正な上限に 422 を返すと「そのIDは存在する」と伝わり、B-3 で 404 に揃えた方針が崩れる |
| 2026-08-07 | B-9「LIFF画面のテスト基盤」を Phase B に追加し、完了。ロジックはユニットと実DBで検証していたが、**画面の描画・入力・保存は検証手段が無かった**。B-5 だけでなく F-4・F-5・G-5・H-2 も同じ状況になるため単独タスクとした。タスク総数 56 → 57 |
| 2026-08-07 | B-9 で `vitest.config.ts` を project に分けた。ロジックは `node`、画面は `jsdom`。**全体を `jsdom` にすると `Response` などの実装が差し替わり**、B-8 の LIFF テストのように Node の実装を前提にしたテストが影響を受ける |
| 2026-08-07 | B-9 の画面テストではAPIを差し替える。サーバー側の判定は統合テストの担当であり、二重に持つと同じ仕様が2か所に散る |
| 2026-08-07 | **B-9 の画面テストが B-5 の不具合を検出した。** 入力欄の補足文が `<label>` の中にあり、読み上げ時の項目名が「ペンネーム 記事の書き手として表示されます」になっていた。補足を `aria-describedby` で別に結びつけるよう直した |
| 2026-08-07 | B-6 完了。`requireAdmin` を追加し、`/admin` 配下をサーバー側で弾くようにした。**判定を `src/app/admin/layout.tsx` に置き、`middleware.ts` を使わない。** セッションの検証が `node:crypto` の `timingSafeEqual` に依存しており（B-2）、Middleware の既定の実行環境で動かないため |
| 2026-08-07 | B-6 で MONITOR に **403 を返す（404 ではない）。** B-3 で 404 に揃えたのはIDの総当たりで他人の資源の有無を調べられるのを防ぐためであり、`/admin` は固定パスで総当たりの対象にならない。隠しても得るものが無い |
| 2026-08-07 | B-6 の `requireAdmin` は**同意を見ない**。利用規約とデータ利用への同意は実験に参加するモニターに求めるもので、運営者を同意状態で締め出すと同意周りの不具合を管理画面から直せなくなる |
| 2026-08-07 | **Q-012 起票。** タスク名は「管理者認証（Supabase Auth）」だったが、SPEC 3.2 は Supabase Auth・メール＋ワンタイムリンク・ADMINロール制御の「**いずれか**」としており必須ではない。このリポジトリに Supabase は入っておらず、導入すると認証が2系統になる。B-6 は**認可**まで実装し、ログイン手段の決定は Q-012 に分けた。TASKS の B-6 の名称を「管理者の認可（ADMINロール制御）」に改めた |
| 2026-08-07 | **Q-012 解決。管理者のログインは「メール＋ワンタイムリンク」を自前で実装する**（SPEC 3.2 の2つ目）。Supabase は導入しない。モニター側の LINE Login は変更せず、認証が通ったあとは既存のセッションCookie（B-2）を共用するため `requireAdmin` は変更不要 |
| 2026-08-07 | Q-012 の決定に伴い B-10（トークンのテーブルとマイグレーション）と B-11（ログインの実装）を追加。**スキーマ変更を単独タスクにするのは DATA_MODEL 9章に従う。** タスク総数 57 → 59 |
| 2026-08-07 | **Q-013 起票。** メール送信基盤が未選定。Q-012 の決定で初めてメール送信が必要になった。SPEC・TASKS のどちらにも記載が無く、B-11 の前提になる |
| 2026-08-07 | B-7 完了。モニター一覧を実装。**`docs/MODULE_RULES.md` にルール5「全ユーザーを横断して読むのは `ForAdmin` の関数だけ」を追加した。** 管理画面はルール1（必ず `userId` で絞る）に当てはまらないため、名前（`...ForAdmin`）と置き場所（`admin-*.ts` へ隔離）で用途を固定し、モニター向けの経路から誤って呼ばれないようにする |
| 2026-08-07 | B-7 のブログ数は `groupBy` で1回にまとめた。行ごとに引くとモニターが増えるたびに管理画面が順に遅くなる |
| 2026-08-07 | B-7 の一覧は **`WITHDRAWN` を含める**。退会者を消すと「10名中何名が続いたか」が読めなくなる（SPEC 1.2）。状態は列で示す。一方 **ADMIN は含めない**。運営者を混ぜると参加者数が読めなくなる |
| 2026-08-07 | B-7 の実機確認で **B-6 の作りの不備が見つかった。** レイアウトが拒否画面を出しても**ページの関数は実行され例外を投げる**ため、拒否のたびに未処理例外がRSCのペイロードに載っていた。ページ側の `requireAdmin` を `catch` して何も描かずに戻す形へ変更した（判定自体は残す。レイアウトの判定だけに頼らない） |
| 2026-08-07 | B-10 完了。`admin_login_tokens` を追加し、**Phase 0 で初めての追加マイグレーション**を適用した。26 → 27テーブル。DATA_MODEL 9章のとおり単独PRで、機能実装（B-11）は混ぜていない |
| 2026-08-07 | B-10 で**トークンの原文を保存せずハッシュだけを持つ**設計にした。DBが漏れてもリンクが使えないようにするため。`used_at` を持たせるのは、署名だけの自己完結トークンでは期限内に何度でも使えてしまい、メールの転送や端末の紛失でそのまま入られるため（Q-012 の決定） |
| 2026-08-07 | B-10 でCHECK制約を2件追加（期限は発行より後、使用時刻は発行以降）。DATA_MODEL 4章の「取り違えると事故になるものはDB側にも入れる」に従う。期限切れのトークンを発行しても「なぜかログインできない」としか分からず、原因の切り分けが難しい |
| 2026-08-07 | `admin_login_tokens` の所有モジュールを `auth` とした。MODULE_RULES で `auth` は所有テーブル無しとしていたが、認証の状態そのものであり `users` へ置くと users モジュールが認証方式を知ることになる |
| 2026-08-07 | **Q-013 解決。メール送信は Resend。** SDKは入れず HTTP API を `fetch` で叩く（送るのは1種類のメールだけで、依存を1つ増やす価値が無い）。`RESEND_API_KEY` `MAIL_FROM` `APP_BASE_URL` の3つとも `src/lib/env.ts` の必須項目にしない。未設定でも LIFF 側は動く必要があり、メールの設定漏れでサービス全体を止めない |
| 2026-08-07 | B-11 完了。管理者のメール＋ワンタイムリンクを実装。**`APP_BASE_URL` を環境変数から取る。** リクエストの `Host` ヘッダーからリンクを組み立てると、偽の `Host` を送られたときに向き先を攻撃者のドメインへ差し替えられる |
| 2026-08-07 | **B-11 のリンクは GET では消費しない。** メールのリンクを開いただけで使用済みにすると、受信側のセキュリティ製品やクライアントの先読みで消費され、本人がクリックしたときには使えない。リンク先はボタンを出すだけの画面にし、押したときに POST する |
| 2026-08-07 | B-11 の「1回だけ」は **`WHERE used_at IS NULL` を条件に含めた1文の更新**で実現した。「引いてから書く」を分けると、同時に2回叩かれたときに両方が通る。実DBで3並列を投げて1つだけ通ることを確認している |
| 2026-08-07 | B-11 で応答を分岐させない。未登録・MONITOR・停止中・発行しすぎ・期限切れ・使用済みのいずれも同じ結果を返す。区別すると、どのアドレスが管理者かを外から調べられる。理由はログにのみ残す（B-1 と同じ方針） |
| 2026-08-07 | B-11 で同一管理者への発行を15分に3回までに制限した。攻撃者が繰り返し要求しても、管理者の受信箱がリンクで埋まらないようにする |
| 2026-08-07 | B-11 で `/admin` をルートグループ `(protected)` に分けた。**ログイン前の画面を認証の内側に置くと、ログインするためにログインが要る。** URLは変わらない |
| 2026-08-07 | **Phase A・B 完了**（20タスク）。ユニット313件・統合96件。未解決は Q-001（リダイレクト方式）・Q-002（費用負担）・Q-005（Search Console の暦日） |
| 2026-08-07 | Q-001 に選択肢と確認事項を整理した。**判断の中心は技術ではなくASPの規約**である。`schema.prisma` は既に自前リダイレクタを前提にしているが、ブログはモニター自身のドメインで動くため、別ドメイン経由の掲載を許さないASPがあると成果が無効になる。成果が無効になると実験そのものが成立しない（SPEC 1.2）ため、クリックデータの価値より優先される |
| 2026-08-07 | Q-001・Q-002 が止めるのは D-8・E-10・H-1 であり、**Phase C（C-1〜C-6）は依存しない**ことを TASKS.md に明記した。SPEC 20.1 の「Phase C に入る前」という書き方だけでは、C-1 が止まるかどうかが読めなかった |
| 2026-08-07 | **Q-001 の前提が変わった。** 「別ドメインのリダイレクタを許可するASPとしないASPが混在する」ことが確認されたため、方式を1つに決める前提を捨て、**案件ごとに `REDIRECT` / `DIRECT` を切り替える案**を整理した。既定は安全側の `DIRECT` とし、規約を確認できたASPだけ許可側へ上げる |
| 2026-08-07 | Q-001 の整理で、**切り替えの仕組みを最初から入れれば SPEC 20.1 が懸念した「公開済み記事の貼り替え」は起きない**ことを確認した。リンクの組み立てを1関数に集約すれば、後から規約が変わっても影響するのは以後に生成される記事だけ |
| 2026-08-07 | Q-001 で**サブIDを全案件に付ける**ことを提案した。ASPが用意した機能なので規約上の問題が無く、**クリック数より価値の高い「成果の記事単位の紐付け」**が得られる。Phase 0 の目的は収益の実測（SPEC 1.2）であり、リダイレクタが担うクリック計測とは役割が違う |
| 2026-08-07 | **Q-001 解決。リンクの出し方を案件ごとに切り替える。** 方式を1つに決める前提を捨てた。既定は安全側の `DIRECT` で、規約を確認できたASPだけ `REDIRECT` へ上げる。ASP単位の既定値は ADMIN が設定し、モニターに規約の判断をさせない |
| 2026-08-07 | Q-001 の決定に伴い **D-9（案件のリンク方式のテーブル追加）を追加**し、D-1 の依存に加えた。案件登録時にリンクの組み立て規則が決まるため、D-1 の前に列が要る。D-8 の完了条件も「`REDIRECT` の案件でクリックが記録される」へ改めた。タスク総数 59 → 60 |
| 2026-08-07 | `docs/DATA_MODEL.md` 6章の `affiliate_links` / `link_clicks` から「方式が確定するまで確定版ではない」の但し書きを外した。**この2テーブルは `REDIRECT` の案件でのみ使う** |
| 2026-08-08 | **Q-002 解決。ドメイン・サーバー費用はモニター負担。** 実装への影響は無い。H-1 の募集要項に明記し、**規約同意より前に提示する**（費用は参加可否そのものであり、同意の後に出すのは順序が逆）。参加者が「自費で用意できる人」に偏るため、SPEC 1.2 の結果を一般化する際はこの条件を併記する |
| 2026-08-08 | **SPEC 20.1 の未確定2件がどちらも解決した。** 残る未解決は Q-005（Search Console の暦日・G-2 の前提）のみ |
| 2026-08-08 | C-1 完了。WordPress接続情報の暗号化保存を実装。**AES-256-GCM のユーティリティは `src/lib/crypto/` に置いた。** DATA_MODEL 7章の暗号化対象は WordPress だけでなく `search_console_connections.refresh_token_encrypted`（G-1）も含むため、WordPress モジュールの中には置かない |
| 2026-08-08 | C-1 で**暗号文に AAD（追加認証データ）を付けた。** `wordpress_connection:<blogId>:<列名>` を結び付けており、DBへ書ける立場の攻撃者が他人の行の暗号文を自分の行へ複製しても復号できない。付けないと、暗号文としては正しいまま別のブログの認証情報が使える |
| 2026-08-08 | C-1 で保存形式にバージョン（`v1.`）を付けた。鍵の更新やアルゴリズム変更を後から入れるとき、**どの方式で入っている行かを判別できないと移行できない** |
| 2026-08-08 | C-1 で**復号値を `Secret` クラスに包んだ。** A-4 のロガーはフィールド名でマスクするが、名前が変われば素通りする。`JSON.stringify`・文字列連結・`console.log` のいずれでも `[REDACTED]` になる型にして、`expose()` の呼び出し箇所を grep できるようにした（SPEC 14.2） |
| 2026-08-08 | C-1 の `site_url` は **https のみ**。アプリケーションパスワードは Basic 認証で送るため、http では毎回平文で流れる。あわせて IPアドレス・`localhost`・`.local`・既定以外のポートを拒否した。到達確認そのものは C-2 |
| 2026-08-08 | C-1 で**アプリケーションパスワードの空白を除去してから保存する。** WordPress は4文字ずつ区切って表示し認証時に空白を無視するため、貼り付けたままだと人によって保存値が変わる |
| 2026-08-08 | C-1 の `disconnect` は**行を消さず、認証情報を空で上書きする**。`site_url` は残して Q-007 の再接続時の照合に使う。使えない資格情報を持ち続けないため、列（NOT NULL）には空文字を暗号化した値を入れる |
| 2026-08-08 | C-1 の再接続では**権限とテスト結果を初期化する。** 認証情報が変われば以前の接続テスト（SPEC 7.2）の結果は当てにならない |
| 2026-08-08 | C-1 の所有権検証は `blogs` モジュールの `requireBlogForUser` を経由する。`wordpress_connections` を `blog.userId` で join すれば1クエリで済むが、それは他モジュールのテーブルを条件に使うことになる（MODULE_RULES 1） |
| 2026-08-08 | C-1 で `vitest.integration.config.ts` にテスト用の環境変数の既定値を置いた。`getServerEnv()` は**全ての**必須変数を見るため、`DATABASE_URL` だけでは起動時検証で落ちる。`DATABASE_URL` にはダミーを置かない（未設定時は helpers/db.ts が設定方法を示す方が親切） |
| 2026-08-08 | **C-7 を追加**（タスク総数 60 → 61）。C-2 はモニターが入力したURLへ実際にリクエストを出すため、SPEC 14.3 の SSRF 対策が前提になる。D-2（LP自動評価）も同じものを要るため、**共通クライアントとして切り出した**。C-2 に混ぜると security boundary が接続テストの中に埋もれる |
| 2026-08-08 | C-7 完了。外向きHTTPの共通クライアントを実装。**`fetch` ではなく `node:http` / `node:https` を使う。** 接続先のIPを固定する必要があるため。`fetch` はホスト名で繋ぎ直すので、判定と接続の間にDNSの応答が変わると（DNSリバインディング）判定を通ったあとで内部アドレスへ繋がる |
| 2026-08-08 | C-7 の到達判定は**名前解決した結果のIP**で行う。ホスト名の見た目では判定できない。攻撃者は自分のドメインの A レコードを `127.0.0.1` へ向けられる |
| 2026-08-08 | C-7 で**解決結果が1件でも内部アドレスなら拒否する。** 先頭だけ見て通すと、公開IPと内部IPの両方を返すレコードで内部側へ繋がる |
| 2026-08-08 | C-7 で **IPv4射影（`::ffff:127.0.0.1`）と NAT64（`64:ff9b::`）を IPv4 として判定する。** 見落とすと、同じ宛先へ IPv6 表記で到達できる。**先頭ゼロつきの表記（`010.0.0.1`）は受け付けない**。8進数と解釈する実装があり、判定と接続で食い違う |
| 2026-08-08 | C-7 で**転送のたびに最初と同じ検証をやり直す。** 転送先を素通しすると、公開サイトから `http://169.254.169.254/` へ飛ばすだけで対策が無効になる |
| 2026-08-08 | C-7 の実装中に **Node 22 の `lookup` の呼び出し形が想定と違うことが分かった。** `node:net` は `all: true` で呼び、`[{address, family}]` の配列を期待する。単一の形で返していたため `Invalid IP address: undefined` で接続が失敗していた。**実サーバーへのテストを書いていなければ気づかなかった** |
| 2026-08-08 | C-7 のテストは3層に分けた。判定表（純粋）・組み立て（送信を差し替え）・送信層（実サーバー）。さらに**差し替えなしで `localhost` へ到達しないことを、実際にサーバーを立てて確かめている**（1度も到達しないことをカウンタで確認）。差し替えテストだけでは「判定表を正しく呼んでいるか」しか見ていない |
| 2026-08-08 | **TASKS.md の「タスク総数」が実際の表と合っていなかった。** 各Phaseの表を数えると71件だが、「59」から加算して書き継いでいた。数え直して内訳（Phase別の件数）を併記した。合計だけを持つと、ずれても気づけない |
| 2026-08-08 | D-9 完了。`affiliate_offers.link_mode` と `LinkMode` enum を追加。**既定は `DIRECT`（安全側）。** 規約を確認できたASPだけ `REDIRECT` へ上げる。判断がつかないものを許可側へ倒すと、成果が無効になったときに取り返しがつかない（Q-001）。30 → 31 enum |
| 2026-08-08 | D-9 は **DATA_MODEL 9章のとおり単独PR**。機能実装（D-1・D-8）は混ぜていない。実PostgreSQLで `migrate deploy` → 乖離なし → **既定値が実際に `DIRECT` で入ること**と、想定外の値が enum に拒否されることを確認した |
| 2026-08-08 | **Q-014 起票。** Q-001 で「サブIDは全案件に付ける」と決めたが、**付け方はASPごとに違う**（パラメータ名・文字種・長さの制限）。こちらでは調べられないため D-9 では実装せず、D-1 の着手前に決める。形が決まらないうちに列を足すと作り直しになる |
| 2026-08-08 | ARCHITECTURE.md のテーブル数・enum数が B-10 の時点から更新されていなかった（26テーブル・30 enum のまま）。D-9 で数え直して 27テーブル・31 enum に直した。**数を複数の文書に散らすと必ずずれる**ため、どこに書いてあるかを D-9 で洗い出した |
| 2026-08-08 | C-2 完了。SPEC 7.2 の7項目を実装。**項目ごとに別のエラーコードを返す**（完了条件）。「接続できません」だけではモニターが何を直せばよいか分からない |
| 2026-08-08 | C-2 は**最初の失敗で止めない。** 分かるところまで進めて全項目の結果を返す。ただし前提が崩れた項目（認証できていないのに権限を見る等）は `SKIPPED` にする。存在しない結果を「失敗」として出さないため |
| 2026-08-08 | C-2 の到達確認（項目2）は**認証を付けずに叩く。** 認証で落ちたのか到達できないのかを分けるため。実サーバーへのリクエストログで、`/wp-json/` だけ認証なしで飛んでいることを確認した |
| 2026-08-08 | C-2 で**到達禁止アドレスと通信エラーを区別しない**（どちらも `unreachable`）。「そのホストは内部アドレスへ解決された」と返すと、内部ネットワークの構成を外から調べられる（SPEC 14.3 の趣旨）。理由はログにのみ残す |
| 2026-08-08 | C-2 のテスト投稿は**必ず `status: draft` で作り、終わったら消す**（SPEC 7.2 の8番目・7.3）。毎回の接続テストで下書きが増えるとモニターの管理画面が埋まる。消せなければ投稿IDを残骸として返す。偽WordPress側で「残り0件」を確認した |
| 2026-08-08 | C-2 の**メディア権限は実際にアップロードして確かめない。** テストのたびにモニターのメディアライブラリへファイルが増えるのを避ける。WordPress が応答ごとに返す `Allow` ヘッダー（`rest_send_allow_header`）を主に使い、無い環境では `capabilities.upload_files` へ落とす |
| 2026-08-08 | C-2 の接続テストAPIは**失敗しても 200 を返す。** テストの実行そのものは成功しており、結果が「繋がらない」なだけ。HTTPのエラーにすると、実行できなかった場合と区別がつかない。所有権・未接続はこれまでどおり 404 |
| 2026-08-08 | C-2 のクライアントテストで、**`safeFetch` は `lookup` を差し替えても迂回できない**ことを確認した（判定は解決結果のIPに対して働く）。C-7 の設計として正しいため、クライアントのテストは送信層を直接使い、**`safeFetch` へ何を渡すか**を別に確かめる形にした |
| 2026-08-08 | **C-2 は実WordPressで確認していない。** サイトとアプリケーションパスワードが要る。偽WordPressサーバーを立てて、接続情報の保存 → 復号 → 7項目 → 保存まで通しで確認し、認証失敗・メディア権限なしの分岐も実際のHTTPで確かめた。B-11（Resend の実送信未確認）と同じ扱い |
| 2026-08-08 | **Q-005 解決。Search Console の日付は変換せずそのまま `metric_date` に入れる。** 返るのは時刻を持たない日付文字列で日単位に集計済みのため、**別のタイムゾーンの暦日へ割り直すことは原理的にできない**。どの案も「取り決め」にすぎず、根拠のある変換は存在しない |
| 2026-08-08 | Q-005 でずれを許容できるのは、**系統的なずれが全ブログに等しくかかり比較を歪めない**ため（SPEC 1.2 の目的は3ブログ×10名の比較）。タイムゾーン列を足す案はマイグレーションと分岐を増やすわりに分析上の利得が無い。代わりに **`metric_date` の意味を DATA_MODEL 10章に明記**し、G-5 の収益と最大1日ずれることを残した |
| 2026-08-08 | **Q-005 には未検証の前提がある。** DATA_MODEL が「Search Console は日付がUTC基準で返る」としていたが、これを確かめていない（太平洋時間だという説明も見られる）。**G-1 の実接続で確認する**。決定の中身は前提がどちらでも変わらないため、G-2 は止めない |
| 2026-08-08 | **Q-014 解決。`affiliate_offers.sub_id_param`（NULL可）にパラメータ名を持つ。** `NULL` ならサブIDを付けない。**これで D-1 のブロックが外れた** — ASPの情報がゼロでも案件は登録でき、分かったASPから順に SQL で埋めれば有効になる。「着手を止める問題」から「あとから入れる運用データ」へ変わった |
| 2026-08-08 | Q-014 で (b)（ASP名から実装側のテーブルで引く）を採らなかったのは、ASPが増えるたびにコード変更とデプロイが要るため。(c)（URLテンプレートを登録）は柔軟だが、手入力の誤りが増える |
| 2026-08-08 | Q-014 で**パス埋め込み型のASPを先回りして設計しない**と決めた。形が決まらないうちに作ると作り直しになる。出てきた時点で論点として起票する |
| 2026-08-08 | Q-014 の列追加を **D-10 として単独タスク**にした（DATA_MODEL 9章）。D-1 の依存に加え、タスク総数 71 → 72 |
| 2026-08-08 | **未解決の論点がゼロになった**（2026-08-08）。Q-001・Q-002・Q-005・Q-014 がすべて決着。新たに判断できない事項が出たら OPEN_QUESTIONS へ記録し、推測で実装しない方針は変えない |
| 2026-08-08 | D-10 完了。`affiliate_offers.sub_id_param`（NULL可）を追加。**既定を `NULL` にすることで、ASPの情報がゼロでも案件を登録できる**（Q-014 の決定どおり）。実PostgreSQLで、指定せずに登録すると `NULL` になること・後から埋められることを確認した |
| 2026-08-08 | D-10 は **DATA_MODEL 9章のとおり単独PR**。機能実装（D-1）は混ぜていない。enum の追加が無いため 31 enum のまま |
| 2026-08-08 | C-3 完了。下書き投稿を実装。**`status: draft` 以外で投稿しない**（完了条件）。作成時は必ず `status: draft` を送り、**応答が `draft` 以外なら失敗にする** |
| 2026-08-08 | C-3 の実装中に**自分で入れた欠陥をテストで見つけた。** 作成後の状態確認を enum へ変換した値で判定していたため、`future`（予約投稿）や `private` が「知らない値 → null」となり、下書き扱いで通っていた。**生の文字列が `draft` そのものでなければ全て異常**に変えた。知らない状態を安全側に倒してはならない |
| 2026-08-08 | C-3 の**更新では `status` を送らない。** Phase 0 の公開はモニターが WordPress 上で行う（SPEC 7.4）。`draft` を送ると公開済みの記事を下書きへ戻してしまう |
| 2026-08-08 | C-3 は**下書き以外の投稿を更新しない**（DATA_MODEL 11章「公開済み記事の更新は承認を必須とする」）。リクエストを1回も出さずに 409 で拒否する。承認を経る経路は C-5・F-6 で作る |
| 2026-08-08 | C-3 の content hash は**WordPress が保存した本文（`content.raw`）に対して計算する。** 送った文字列で計算すると、WordPress のサニタイズ（`wp_kses`）で値が変わったときに C-5 の同期が「利用者が編集した」と誤判定する |
| 2026-08-08 | C-3 は**接続テスト（C-2）を通っていない接続では投稿しない。** 権限を確かめずに投稿すると、権限不足のエラーが記事生成のたびに出る |
| 2026-08-08 | C-3 で**自分のブログIDと他ブログの `content_item_id` の組み合わせ**を弾く経路を入れた。`wordpress_posts.content_item_id` は unique なので、これが無いと他人の投稿行を自分のブログから更新できてしまう |
| 2026-08-08 | `docs/ARCHITECTURE.md` §8「現在の実装状況」が **B-3 の時点から更新されていなかった**（「Phase B は B-3 まで」）。実際のモジュール・Route Handler・画面を数え直して書き直した。**「現時点で実際に存在するもの」を書く**という同文書の趣旨に反していた |
| 2026-08-08 | ARCHITECTURE に「**まだ動かないもの**」の節を足した。実WordPress・実メールでの確認が未実施であることを、進捗と同じ場所に書いておかないと見落とされる |
| 2026-08-08 | **E-1 のジョブ基盤は外部サービスを使わず PostgreSQL をキューにする。** 決め手は**スキーマが既にそう設計されていた**こと — `jobs.idempotency_key` は unique、索引 `jobs(status, job_type)` の用途を DATA_MODEL が「ワーカーのポーリング」と明記している。10名×3ブログの規模で契約・APIキー・障害点を増やす理由が無い |
| 2026-08-08 | **デプロイ先が Vercel と判明し、設計を見直した。** 常駐ワーカーを持てないため、(1) 締め切りを持って抜ける (2) `RUNNING` のまま残った行を回収する (3) ジョブ単位でも時間を区切る、の3点を入れた。**(2) が無いと、関数が実行時間の上限で殺されたジョブは二度と動かない** |
| 2026-08-08 | E-1 で**再試行の待ち時間に専用の列を持たせなかった。** `updated_at`（失敗して QUEUED へ戻した時刻）と `attempt_count` から算出できる。スキーマ変更が不要になり、E-1 が1本のPRで収まった。**取得SQLと `backoff.ts` で同じ式を使う**ため、片方だけ変えてはいけない |
| 2026-08-08 | E-1 の取得は `SELECT ... FOR UPDATE SKIP LOCKED`。**Vercel の関数は並行して起動しうる**ため、同じ行を二重に取らないことが要る。実PostgreSQLで、別トランザクションがロックした行を**待たずに飛ばす**ことを確認した（`Promise.all` は接続プールの都合で本当に競合しないことがある） |
| 2026-08-08 | E-1 の回収では **`attempt_count` を戻さない。** 取得時に加算済みで、毎回タイムアウトするジョブが無限に再試行されるのを防ぐ |
| 2026-08-08 | E-1 のワーカー起動は `CRON_SECRET` で保護し、**未設定なら実行しない**（fail closed）。`src/lib/env.ts` の必須には入れていない。cron の設定漏れでアプリ全体を止めないため（B-11 と同じ方針）。**未設定と不一致を区別しない** — 区別すると設定状態を外から調べられる |
| 2026-08-08 | **`vercel.json` の毎分cronには Vercel Pro が要る**（Hobby は1日1回まで）。Hobby だと承認された記事が最大24時間投稿されない。ARCHITECTURE に明記した |
| 2026-08-08 | **記事生成（E-10）が関数の実行時間に収まるかは未確認。** AI呼び出しは分単位になり得る。E-1 は1件ごとに時間を区切って失敗として記録するところまでで、分割は E-10 の課題として残した |
| 2026-08-08 | **C-4 の冪等性キーは `<種類>:<対象>` を強制する。** 対象（`content_item_id`）だけをキーにすると、同じ記事の `WORDPRESS_POST` と `WORDPRESS_SYNC` が衝突し、**後から積んだほうが黙って既存扱いになって実行されない**。`enqueueJob` が積む前に形を確かめる |
| 2026-08-08 | **C-4 で「WordPress に投稿はしたが、記録の前に関数が殺された」場合を手当てした。** 冪等性キーが守るのは積むところまでで、この状態の再試行はDBに記録が無いため2本目を作る。**外部を呼ぶ直前に印を残し、再試行時に印があればやり直さずに失敗させる。** 統合テストでは、印なしで二重投稿になることを対照として先に示している |
| 2026-08-08 | C-4 の印は `jobs.output_json` に置いた。**専用の列を足さない**（スキーマ変更は単独PRの決まりで、C-4 の変更先は `src/modules/jobs/`）。`output_json` は成功時にしか書かれないため実行中は空いており、成功すれば `completeJob` が結果で上書きして印が消える |
| 2026-08-08 | C-4 は**中断からの復旧を自動でやらない。** 二重投稿は実験データを壊しモニターのブログに要らない記事を残すため、「2本目を作る」より「止まって人に見せる」を選んだ。試行の上限まで進んで `FAILED` で止まり、`error_code` が残る（運用は H-6） |
| 2026-08-08 | C-4 の `performOnce` は、失敗時に**既定では印を消さない。** 消す条件は呼び出し側が渡す述語で、**副作用が出ていないと確実に言える失敗**（接続拒否・名前解決の失敗・401/403）に限る。タイムアウトと 5xx は言えない。消さないと接続できなかっただけのジョブが再試行されなくなるため、両方向に倒せる必要がある |
| 2026-08-08 | **`user_edited_at` を C-5 本体から切り離し、C-5-schema として単独のPRにした。** DATA_MODEL 11章は「C-5 で追加する」と書いていたが、9章の「機能実装のPRにマイグレーションを混ぜない」を優先した（B-10・D-9・D-10 と同じ扱い）。TASKS に C-5-schema の行を足し、総数を 72 → 73 に直した |
| 2026-08-08 | **C-5 の同期では `last_content_hash` を書き換えない。** ハッシュが指すのは常に**前回こちらが書き込んだ本文**である。同期のたびに WordPress 側の本文で上書きすると、次の同期で「未編集」に戻り、**利用者の編集を見失う** |
| 2026-08-08 | C-5 の `user_edited_at` は**初めて検出した時刻**で固定する。同期のたびに現在時刻へ進めると「いつから WordPress 側が正なのか」が分からなくなる。承認を経て上書きし終えた時点で `null` へ戻す |
| 2026-08-08 | **C-5 で「content hash が同一なら更新しない」を、送る本文のハッシュと `last_content_hash` の比較で実装した。** ハッシュは WordPress が保存した本文に対して計算しているため、サニタイズが入ると一致せず**余分な更新が1回走る**。逆（更新すべきなのに飛ばす）は起きないため、安全側に倒れている |
| 2026-08-08 | C-5 の判定順は **公開済み → 内容が同じ → 利用者の編集**。内容が同じなら何も起きないので、編集の検出より先に抜ける。逆順にすると**同じ内容の再実行が失敗になり、C-4 の再試行と噛み合わない** |
| 2026-08-08 | C-5 の同期は `context=edit` の `content.raw` を使う。**`rendered` を比較に使わない** — ショートコードの展開や整形フィルタを通った後の文字列で、誰も編集していなくても変わる。`raw` が取れない（権限不足）場合は**本文なしで「未編集」と判定せず失敗させる** |
| 2026-08-08 | C-5 は WordPress が返す `future`（予約投稿）・`private` を**推測で埋めずに失敗させる**（C-3 の `notDraft` と同じ方針）。知らない状態を下書き扱いに倒すと、公開済みの記事を下書きへ戻す事故につながる |
| 2026-08-08 | **C-6 の越境テストで実際の穴が1件見つかった。** `wordpress_posts` は `content_item_id` と `blog_id` を別々の外部キーで持っており、両者が同じブログを指す保証が無かった。**他人の未投稿の `content_item_id` を自分のブログの投稿として登録でき、`content_item_id` は unique なので持ち主はその記事を二度と投稿できなくなる**（所有権の判定が「既存行の blog_id が違う」で404になる） |
| 2026-08-08 | 上記の穴を**アプリ層ではなく複合外部キーで塞いだ**（C-6-schema）。実装側で確かめると `wordpress` モジュールが `content_items` を直接読むことになり MODULE_RULES 1 に反する。**DBに置けばどのモジュールから書いても迂回できない。** 参照先に要る `content_items(id, blog_id)` の unique は制約としては冗長で、複合外部キーの要件として置いている |
| 2026-08-08 | **C-6 を単独タスクにした判断（TASKS の指示）が実際に効いた。** C-1〜C-5 の統合テストはいずれも越境を1〜2件ずつ確かめていたが、**「自分のブログID + 他人の記事ID」で、かつ相手がまだ投稿していない場合**という組み合わせはどれも踏んでいなかった |
| 2026-08-08 | C-6 で、越境が**拒否されること**だけでなく**攻撃のあとに相手のデータが1バイトも変わっていないこと**まで比較するようにした。「エラーは返ったが書き込みは起きていた」を見逃さないため。相手のブログ・接続・投稿をまとめて写し取り、前後で突き合わせる |
| 2026-08-08 | C-6 に**入口の網羅チェック**を入れた。`blogs` と `wordpress` の `...ForUser` を列挙し、テスト側の一覧と一致しなければ落ちる。**新しい入口が生えたときにここが落ちない限り、C-6 は放置される**（TASKS が警告している「ついでに書かせると省略される」と同じ力学） |
| 2026-08-08 | C-6 で、複合外部キーに弾かれた場合を **P2003 → 404** に写した。他の越境と同じ見え方に揃える。この時点で WordPress 側に下書きが1件できているが、**残るのは指定した本人のサイトで相手には影響しない**。事前に防ぐにはモジュール境界を越えるしかなく、割に合わない |
| 2026-08-08 | **D-1 で `link_mode` と `sub_id_param` を入力から受け取らない形にした。** どちらもASPの規約に関わる判断で、TASKS が「モニターに規約の判断をさせない」と定めている（Q-001）。型から外すだけでなく、渡されても保存しないことを統合テストで確かめている |
| 2026-08-08 | D-1 の `AppAffiliateOffer` に **`sub_id_param` を含めない。** ADMIN が運用で入れる値で、モニターの画面に出す理由が無い。リンクの組み立てに要るときだけ `readLinkableOfferForUser` で取る（`wordpress` の認証情報と同じ扱い） |
| 2026-08-08 | D-1 のリンク組み立ては **`href` と `destinationUrl` の2つを返す。** `REDIRECT` では前者が `/go/<code>`、後者がサブID付きのアフィリエイトURL（`affiliate_links.destination_url` に保存する）。**リダイレクタを経由してもサブIDを落とさない** — リダイレクタはクリック、サブIDは成果で役割が違う（Q-001） |
| 2026-08-08 | D-1 の URL 検証は **`http` / `https` 以外を拒否する。** 記事本文へ埋める値なので、`javascript:` を通すと読者のブラウザで任意のスクリプトが動く。到達確認はしない（それは D-2 が `safeFetch` で行う） |
| 2026-08-08 | D-1 の更新では**掲載期間を保存後の値どうしで確かめる。** 入力だけを見ると、片方だけ更新したときに既存のもう片方と逆転する。逆転した案件は一度も選ばれず、原因が「選定ロジックの不具合」に見える（F-2） |
| 2026-08-08 | **C-6 の入口網羅チェックが早速仕事をした。** D-1 で `affiliate` モジュールを足したので、C-6 の一覧にも追加した。設計どおり「新しい入口が生えたら C-6 も更新する」流れになっている |
| 2026-08-08 | **SPEC 9.2.3 が判定を求める3項目のうち、ページ長だけ保存先が無かった**（`lp_form_fields` と `lp_mobile_ready` はある）。スコアにも足切りにも使われないが、**判定した値が消えると「なぜこの案件が落ちたのか」を後から確かめられない**ため `lp_content_length` を足した（D-2-schema） |
| 2026-08-08 | **D-2 の `lp_form_fields` は「input要素数」ではなく「利用者が入力する項目の数」を入れる。** SPEC 9.2.3 の記述は前者だが、`hidden` はCSRFトークンなどで十数個入ることがあり、そのまま数えると**3項目のフォームが「11以上＝0点」になる**。同じ節のスコア区分が「フォーム項目」と書かれており、そちらに合わせた。生の `<input>` 数も併せて返している |
| 2026-08-08 | D-2 は**HTMLを構文解析しない。** 外部ライブラリを足さず正規表現で必要な2つだけ取る。求めているのは「項目が何個か」と「viewport があるか」だけで、DOMを組み立てる必要が無い。**壊れたHTMLでも数えられるほうがこの用途には向く**（LPは広告用に手書きされることが多い） |
| 2026-08-08 | **D-2 は失敗時に評価列を消さない。** `NULL` へ戻すと、一時的な障害で案件が足切り（SPEC 9.2.3「LPがスマートフォン非対応」）に引っかかる。到達できなかった案件を「フォーム項目0個＝満点」にもしない — **推測で埋めない** |
| 2026-08-08 | D-2 の SSRF対策は**実HTTPサーバーを立てて確かめた。** 偽の `safeFetch` は書いたとおりに動くだけで、「本当に内部アドレスへ行かないか」を証明できない（A-9 の考え方と同じ）。`127.0.0.1` `localhost` `10.0.0.1` `192.168.0.1` `169.254.169.254` `[::1]` のいずれも到達しない |
| 2026-08-08 | D-2 のテストで、判定側の確認に使う偽の解決先に **`203.0.113.10`（ドキュメント用）を置いたら C-7 に弾かれた。** 対策が効いている証拠なので、公開範囲のアドレスへ変えた |
| 2026-08-08 | **D-3 の画像URLは `https` を必須にした。** バナーはモニターのブログ（https）に `<img>` で埋まるため、`http` の画像は混在コンテンツとしてブラウザに遮断される。**表示されないまま気づかれない**のが最悪で、登録時に弾くほうがよい。遷移先URLは `http` も許す（ASP側のURLでこちらでは選べず、混在コンテンツにもならない） |
| 2026-08-08 | **D-3 では紐づける案件の所有権を `affiliate` の公開関数で確かめた。** `affiliate_offers.id` は全ブログで一意なので、素通しすると他ブログの案件をバナーに紐づけられる（C-6 で `wordpress_posts` に見つけたのと同じ形）。**C-6 のときと違い所有モジュールが既にあるので、DB制約ではなく実装側で塞げる**（MODULE_RULES 1 に反しない）。依存は `banners` → `affiliate` の一方向 |
| 2026-08-08 | D-3 のバナーは既定を `ACTIVE` にした（案件の既定 `DRAFT` と揃えない）。**バナーは出すために作る**もので、登録しただけで出ないと「なぜ出ないのか」を探すことになる。案件は選定を経るため下書きから始まるのが自然 |
| 2026-08-08 | D-3 の対象カテゴリは**空を「全ての記事が対象」**とし、未指定と区別しない。SPEC 5.9 は既定を定めていないが、絞り込みを指定しなければ全件、が素直な読み方。重複は落とす |
| 2026-08-08 | **D-4 では jsonb の形を保存直前に固定した。** `base_profile` `tone` `values` はDBが中身を保証しない。壊れた形のまま入ると、読むのは記事生成（E-8）で、そこまで気づけない。DATA_MODEL 3章の定義をそのまま検証に落とした |
| 2026-08-08 | **D-4 の編集は項目ごとに丸ごと差し替える。** `tone` を渡したら4項目すべてを指定する。入れ子の一部だけを更新できるようにすると「今どういう設定になっているか」が分からなくなる。**部分上書きは D-5 の `tone_override` が担う**設計になっており、両方に持たせる理由が無い |
| 2026-08-08 | D-4 は**未登録のユーザーを編集で作らない**（404）。一部だけ渡された入力から作ると、残りが空のまま保存される。作成は `saveUserPersonaForUser`（全項目必須）に分けた |
| 2026-08-08 | D-4 の保存は `upsert`。`user_id` が unique なので「引いてから入れる」を分けると同時実行で片方が落ちる（B-11・E-1 と同じ考え） |
| 2026-08-08 | **D-5 で重ね合わせ（`resolveEffectivePersona`）を1関数に集約した。** 呼び出し側（記事生成 E-8）に「共通とブログ別のどちらを見るか」を判断させると、**片方だけ見る経路がいずれ生まれる**。`tone_override` の未指定項目は共通人格を継承する（DATA_MODEL 3章） |
| 2026-08-08 | **D-5 では `allowed_experiences` を入力から受け取らない。** 参照先の `persona_facts` は D-6 で作るため、いま受け取ると**所有権を確かめられないIDを保存する**ことになる（C-6 で見つけたのと同じ形）。参照先が無い今は設定しても意味が無く、入口は D-6 で足す |
| 2026-08-08 | D-5 の `resolveEffectivePersona` は**ブログ別設定が無くても動く。** 設定前のブログでも記事は書けるべきで、ブログ固有の項目が `null`／空になるだけ。逆に**共通人格が無ければ404** — 書き手そのものが決まらない |
| 2026-08-08 | D-5 の `tone_override` は**指定された項目だけを保存する。** `undefined` の項目を入れて保存すると「上書きしない」と「空で上書きする」の区別が付かなくなる |
| 2026-08-08 | **D-6 の一人称利用は「書き込みのたびに機械的に落とす」形にした。** AIが推測しただけの体験を「私は使いました」と書くと**架空の口コミ**になる（SPEC 9.6 の禁止事項）。読者を欺くうえに景品表示法にも触れうるため、画面や呼び出し側の善意に任せず、保存の直前で必ず判定する |
| 2026-08-08 | **D-6 で `REJECTED` も一人称利用不可にした。** SPEC 5.7 が明記するのは `AI_INFERENCE`×`UNVERIFIED` だけだが、**裏取りで否定された事実を一人称で書くのは未確認より悪い**。`REJECTED` を許す読み方は成り立たない |
| 2026-08-08 | **D-6 の更新は `source` と `verification` を現在値と重ねてから判定する。** 入力だけを見ると、`VERIFIED` の事実を `UNVERIFIED` へ戻したときに一人称利用フラグが残る。片方だけの更新で禁じられる組み合わせに入るのを見落とさない |
| 2026-08-08 | **D-5 で保留した `allowed_experiences` の入口を D-6 で足した。** 参照先の `persona_facts` ができたので、渡されたIDが自分の事実かを確かめられる。1つでも他人のものが混ざれば丸ごと拒否する。事実を消したときは参照からも外す（引けない参照を記事生成が掴む） |
| 2026-08-08 | **D-8 のリダイレクタは認証が無い入口。** コードを22文字の base64url（約128ビット）にし、飛び先はDBに保存済みの値だけを使う。**クエリから飛び先を受け取らない** — 受け取ると誰でも任意のURLへ飛ばせる踏み台になる。見つからない理由も分けない（総当たりで有効なリンクの有無を調べられる） |
| 2026-08-08 | **D-8 はクリックの記録に失敗しても飛ばす。** クリック数は実験の参考値で、1件の欠落より「リンクを踏んだのに何も起きない」ほうが損害が大きい。失敗はログへ残す |
| 2026-08-08 | D-8 のリダイレクトは **302**。301 だとブラウザが覚えてしまい、**以後クリックを数えられない** |
| 2026-08-08 | D-8 は**生のURLとUAを保存しない。** 参照元はホスト名だけ（記事のURLにはクエリが付き、利用者の入力が混ざりうる）、UAはハッシュだけ（要るのは「同じ端末からの連打か」であってUAそのものではない）。**塩は使わない** — 同じUAが同じハッシュになること自体が目的で、塩を入れると再起動のたびに別のハッシュになる |
| 2026-08-08 | **D-8 で `is_ai_referral` は常に `false` で入れる。** 判別は G-4 の担当（対象ドメインを設定ファイルで追加できる形にする）。**`referrer_host` を残してあるので後から数え直せる** — 派生した値ではなく生の手がかりを保存する |
| 2026-08-08 | D-8 のリンクは**同じ案件×記事では作り直さない。** 記事を再生成するたびに新しいコードを発行すると、**公開済み記事に埋まった古いコードが宙に浮く**（消せば404、残せばクリック数が分散する） |
| 2026-08-08 | **C-6 の入口網羅チェックが自動で仕事をした。** D-8 で `ensureRedirectLinkForUser` を足したところ、追加し忘れたまま統合テストが落ちた。設計どおりの挙動で、**人が気づく前に落ちる**形になっている |
| 2026-08-08 | **D-8 の注記で参照するタスク番号を間違えていた**（`content-planning` ができるのは E-2 と書いたが、E-2 はプロンプト管理で、`content-planning` は E-4）。塞ぐ時期の判断が変わるため訂正した |
| 2026-08-08 | **E-2 の「有効な版は1つまで」を1文のUPDATEで保った**（`update prompt_versions set is_active = (version = $2) where key = $1`）。「他を無効にしてから有効にする」を2文に分けると、**同時に別々の版を有効化したときに2つ有効な状態**が残りうる。1文なら後から実行したほうが丸ごと勝つ。**部分ユニーク索引（スキーマ変更）を足さずに済んだ** |
| 2026-08-08 | **E-2 のロールバックに専用の入口を作らなかった。** 過去の版を `activatePromptVersionForAdmin` に渡せば戻る。どの版へ戻すかは一覧（`/admin/prompts`）を見て人が選ぶもので、「1つ前」を機械的に決める意味が無く、履歴を別に持つ理由も無い |
| 2026-08-08 | **E-2 は同じ版の上書きを拒否する**（409）。版は「どのプロンプトで生成したか」を後から辿る記録で（`article_versions.prompt_version` が参照）、中身が変わると**過去の記事の生成条件が分からなくなる** |
| 2026-08-08 | E-2 の `findActivePrompt` は **`is_active` で引く。「いちばん新しい版」ではない。** 新しい版を作っただけで生成の挙動が変わると、試すことができない。一覧の並びも `created_at` 順にした（版の文字列で並べると `v10` が `v9` より前に来る） |
| 2026-08-08 | **E-3 は用途 → 段 → モデル名の順で決める。** 呼び出し側が渡すのは `AiOperation`（`ARTICLE_BODY` など）だけで、モデル名もプロバイダーの作法も知らない（SPEC「モデル名をコードに直書きしない」）。段を挟むのは、乗り換えたときに**直す場所を1つにする**ため |
| 2026-08-08 | **E-3 の切替先に設定テーブルではなく環境変数を採った**（SPEC 9.8 はどちらでもよい）。テーブルを足すとスキーマ変更が要るうえ、モデルの乗り換えは**プロンプトの調整とセットで効果を見るデプロイを伴う判断**で、実行中に切り替えたい類のものではない |
| 2026-08-08 | **E-3 の単価は入力と出力の両方が揃わなければ費用を `null` にする。** 片方だけで計算すると費用が実際より小さく出て、**予算通知（E-15）が鳴らないまま使い切る**。0で埋めないのも同じ理由 |
| 2026-08-08 | E-3 は**プロバイダーのエラー応答本文をそのまま返さない。** 課金情報や組織の識別子が混ざりうる。HTTPの状態番号だけを伝える |
| 2026-08-08 | E-3 は `safeFetch`（C-7）を通さない。**宛先はこちらが決めた固定のURL**で、SSRF対策が守る「利用者が宛先を決められるリクエスト」に当たらない。代わりに**タイムアウトだけは自前で掛ける** — サーバーレスでは応答を待ち続けると関数ごと殺される（E-1） |
| 2026-08-08 | E-3 は **OpenAI を明示的に拒否する**（未対応）。作法が違うまま Anthropic の形で投げると、動かない理由が分かりにくい。対応するときはここに実装を足す |
| 2026-08-08 | **E-14 は単価が未設定でも記録を残す**（費用0で入る）。記録そのものを飛ばすと、後から単価を入れても**何回呼んだかすら分からなくなる**。代わりに集計が「費用を計算できなかった呼び出し数」を返し、**0円の合計を見て「安く済んでいる」と誤解しない**ようにした |
| 2026-08-08 | E-14 の費用は `decimal(10,6)` のまま丸めない。**1回あたり0.001ドルを下回ることがあり**、丸めると積み上げが合わなくなる。外へ出すときだけ `number` に直す |
| 2026-08-08 | **C-6 の網羅チェックが「一覧への追加漏れ」ではなく「命名の逸脱」を捕まえた。** E-14 で `totalCostForBlog` `totalCostForContentItem` という名前にしたところ、所有権を伴うのに `...ForUser` で終わらないため**チェックの対象から外れていた**。`totalBlogCostForUser` `totalContentItemCostForUser` へ改名した。**規約に合わせるほうが、チェックを緩めるより正しい** |
| 2026-08-08 | E-14 の期間指定は**終端を含めない**（`gte from` / `lt to`）。含めると月末の呼び出しが2つの月に数えられ、月間予算（SPEC 12.2）の判定がずれる |
| 2026-08-08 | **E-15 の通知は「境目を跨いだとき」だけ出す。** 80%を超えている間ずっと鳴らすと、AI呼び出しのたびにメールが飛ぶ。**記録の前後の合計を比べる**ことで、通知済みかどうかの状態を持たずに一度きりにできた（専用のテーブルを足さずに済んだ） |
| 2026-08-08 | **E-15 の停止・低価格モデルへの切替は仕組みだけ用意し、既定を無効にした**（SPEC 12.2 の指示どおり）。Phase 0 で止めると**検証データが欠落し、実験の目的そのものを損なう**。判定関数は用意してあるので、ADMIN が環境変数で有効にできる |
| 2026-08-08 | E-15 の通知文に**「停止しません」と明記した。** 予算超過のメールを受け取った側が「生成が止まった」と誤解して対応を急ぐのを防ぐ。SPEC 12.2 の方針が運用の現場に伝わらないと意味が無い |
| 2026-08-08 | **E-15 は宛先未設定でも送信失敗でも記録を残す。** 予算の通知は運用の助けであって、記事が出るかどうかとは関係が無い。失敗はログに残す |
| 2026-08-08 | E-15 の予算は環境変数に置いた。**Phase 0 は10名で上限も一律の想定**（SPEC 12.3）で、利用者ごとに変える段階ではない。個別の上限が要るようになったらテーブルへ移す |
| 2026-08-08 | **E-3〜E-15 まででAPIキー無しに作れる範囲を使い切った。** 残る E-4〜E-13（ジャンル審査・構成表・記事生成）は実際にAIを呼ぶため、`ANTHROPIC_API_KEY` が要る |
| 2026-08-08 | **一覧の並びに `id` を最後の決め手として足した。** E-2 の統合テストがCIで落ちた（`created_at` はミリ秒までしか持たず、続けて作った2件が同じ値になった）。**同じ時刻の行の前後は決められない**が、呼ぶたびに入れ替わるのは防げる。とくに `take` を伴う一覧（E-14）は、切れ目でどの行が落ちるかが毎回変わっていた。テスト側も「作る速さ」に頼らず `created_at` を明示して確かめる形にした |
| 2026-08-08 | ARCHITECTURE 8章の1行に**古い記述が残っていた**（「Phase E はジョブ基盤（E-1）だけ」）。文の前半だけを置き換える更新を重ねたため、末尾が取り残されていた。**同じ行を何度も書き換えるときは行ごと差し替える** |
| 2026-08-09 | **Vercel のプランは Pro（Q-016）。** 毎分 cron を前提にしてよい。Hobby だと承認された記事が最大24時間投稿されず、承認の意味が薄れる |
| 2026-08-09 | **APIキーを管理画面から設定できるようにする（Q-017）。** E-3 では「モデルの乗り換えはデプロイを伴う判断」として環境変数を採ったが、**運用の実態が違った** — ADMIN は鍵の発行・差し替え・疎通確認をデプロイと切り離して行いたい。解決順は DB → 環境変数 → コード既定 |
| 2026-08-09 | **設定のすべてを画面へ出すことはできない。** 設定はDBにあり、DBを読むには設定が要る。`DATABASE_URL` `ENCRYPTION_KEY` `SESSION_SECRET` `APP_BASE_URL` などは環境変数のまま残す。**`NEXT_PUBLIC_*` はビルド時にブラウザへ埋め込まれる**ため、DBに置いても効かない |
| 2026-08-09 | **保存済みの秘密を復号して返す入口を作らない。** 画面に出すのは末尾4文字と更新日時だけ。一度入れた鍵を読み返す必要は運用上無く、経路があるだけで漏洩面が増える。代わりに**保存前の値で接続テストができる**ようにして、誤った鍵を保存してから気づく順序を避ける |
| 2026-08-09 | **E-3 の `AiConfigSource` が辞書を受け取る形だったため、設定の画面化で `src/lib/ai/` を作り替えずに済んだ。** 「テストから任意の環境を渡せるように」と決めた形が、そのまま「DBの設定を渡す」に使えた |
| 2026-08-09 | **H-7-schema は「秘密が平文の列に入らない」ことをDBで保証する。** `is_secret` と値の入り先の食い違いを CHECK で禁じる。アプリ側の書き方に任せると、入口が増えたときに漏れて**APIキーが平文で保存されたまま誰も気づかない**。C-6 と同じ筋 — 迂回できない場所に置く |
| 2026-08-09 | **設定の解除は行ごと削除。** 「値を空にする」を許すと、`is_secret = true` なのに暗号文が無い行が生まれる。消せば解決順が環境変数・コード既定へ落ちるので、意味も自然に通る |
| 2026-08-09 | **H-7 は設定できる名前を一覧で固定した。** 任意の名前を受け取れるようにすると、管理画面が**アプリの環境変数を何でも書き換えられる入口**になる（`DATABASE_URL` を差し替えられたら終わり）。保存も削除も一覧に無い名前は 404 |
| 2026-08-09 | **H-7 の暗号文はAADに設定名を入れる。** 無いと、DBへ書ける立場の攻撃者が `MAIL_FROM` の暗号文を `ANTHROPIC_API_KEY` の行へ移し替えられる（暗号文としては正しく復号される）。実DBで移し替えを試し、`UNREADABLE` になることを確かめた |
| 2026-08-09 | **H-7 は設定をキャッシュしない。** 読むのはAI呼び出しやメール送信の直前で、索引の効いたSELECT 1回は元々のネットワーク待ちに沈む。持つと「画面で変えたのに効かない時間」が生まれ、**サーバーレスではインスタンスごとに切れる時期が違うため再現しにくい** |
| 2026-08-09 | **復号できない設定を「無い」ことにしない。** `ENCRYPTION_KEY` を変えると保存済みの秘密は読めなくなる。黙って環境変数へ落とすと**設定が効かない理由がどこにも出ない**ので、`UNREADABLE` として画面に出す |
| 2026-08-09 | **H-7 の一覧は「どこから来た値か」（DB / 環境変数 / 未設定）を必ず返す。** これが無いと、画面に出ていないのに動いている状態を説明できない |
| 2026-08-09 | **保存できるだけでは終わらない**ことを H-10 として立てた。読む側が `process.env` を直接見ていると「設定したのに効かない」が残る。H-7 の時点では `getMailer` も `createAiProvider` も付け替えていない |
| 2026-08-09 | **H-8 の接続テストは副作用のない呼び出しだけを使う。** Anthropic は `GET /v1/models`（トークンを消費しない）、Resend は `GET /domains`（メールを送らない）。確かめたいのは「鍵が通るか」であって、そのために課金や送信を起こす必要が無い |
| 2026-08-09 | **H-8 は鍵だけでなく「設定した名前が相手側に在るか」まで見る。** モデル名を打ち間違えても鍵は通り、**実際に記事を生成するまで分からない**。送信元ドメインが未認証なのも同じで、管理者ログインのリンクが届かなくなる（B-11 の残課題）。どちらもここで見えるようにした |
| 2026-08-09 | **一覧を取り切れていないときはモデル名を照合しない**（`has_more`）。全件見ていないのに「そのモデルは無い」と言うと、正しい設定を疑わせることになる |
| 2026-08-09 | **H-8 は例外を投げない。** 接続が通らないのは画面が伝えるべき情報であって、処理の異常ではない。投げるのは「設定できない名前を上書きに渡された」場合だけ — 重ねられると、設定できない環境変数を差し替えて外部を呼ばせられる |
| 2026-08-09 | **接続テストの上書きで空文字を無視する。** 画面は秘密を伏せ字で表示するため、そのまま送られたときに保存済みの値を上書きしてしまう |
| 2026-08-09 | **H-9 の入力欄は常に空から始まり、空のまま保存できない。** 秘密は伏せ字で表示するため、空の送信を通すと「見えている値を保存し直したつもり」で**鍵を消してしまう**。接続テストでも空の上書きは無視する |
| 2026-08-09 | **保存した値を返す `GET` を置かない。** 一覧が伏せ字で返すところまでで、平文を返す経路そのものを作らない（Q-017）。画面は `@/modules/settings` を import せず、型だけを写す（サーバー専用。MODULE_RULES 4） |
| 2026-08-09 | **画面に置けない設定とその理由も画面に出した。** `DATABASE_URL` や `ENCRYPTION_KEY` を設定画面で探して見つからないと、そこで止まる。「なぜ無いのか」を同じ画面に書いておく |
| 2026-08-09 | **接続テストの成功文に「何を確かめたか」を書く。** 「接続できました」だけだと、モデル名まで見たのかが分からず、結局あとで生成して確かめることになる |
| 2026-08-09 | 設定APIのCSRFはセッションCookieの `SameSite=Lax`（B-2）で防ぐ。`PUT` `DELETE` `POST` は他サイトからのフォーム送信で Cookie が付かない |
| 2026-08-09 | **H-10 で `getMailer` のキャッシュを外した。** 最初の1回を覚えていると、**管理画面で鍵を差し替えても古い設定のまま送り続ける**。インスタンスが入れ替わるまで直らず、サーバーレスでは入れ替わる時期が読めない。組み立ては設定を包むだけで費用が無いので、毎回作る |
| 2026-08-09 | **保存できるだけでは効かない、を実際に塞いだ（H-10）。** 環境変数に何も置かず、DBに保存した予算と宛先だけで通知が出ることを実DBで確かめた。H-7〜H-9 だけだと「画面はあるのに何も変わらない」状態だった |
| 2026-08-09 | **`createConfiguredAiProvider` を用意した。** `createAiProvider()` を引数なしで呼ぶと `process.env` を見るため、E-10 以降で**設定が効かない経路をうっかり作る**。呼ぶ側が毎回 `getRuntimeEnv()` を渡すのを覚えている前提にしない |
| 2026-08-09 | **予算の判定と通知の宛先で設定を2回読まない。** 解決した辞書をそのまま渡す。読み直すと、その間に設定が変わったときに判定と宛先がずれる |
| 2026-08-09 | `ADMIN_ALERT_EMAIL` を設定できる項目に足した（H-10）。E-15 が読んでいるのに、H-7 の一覧から漏れていた。**読む処理があるものは載せる**（A-3 の裏返し） |
| 2026-08-09 | **追記のアンカーに行末の改行を含めない。** `IMPLEMENTATION_STATUS.md` の H-7〜H-10 の4行が**1行に繋がっていた** — 締めの文の直前へ差し込むとき、アンカーが前の行を終える改行を巻き込み、新しい行が前の行の末尾に貼り付いていた。1回目だけは表と文の間の空行が緩衝になって成立し、**以降が静かに壊れた**。表へ追記するときは行の配列として扱う |
| 2026-08-09 | **E-4 の `judgeGenre` はAIの出力を受け取る引数を持たない。** 「AIに判定させない」（CONTENT_PLANNING 1.1）を、規約ではなく**関数の形**で守る。受け取れる形にしておくと、いつか渡される。実サーバーでAIに「問題ありません／decision: PASSED」と返させても BLOCKED のままであることを確かめた |
| 2026-08-09 | **E-4 は判定 → 記録 → 説明文の順で行う。** AIを呼べなくても可否は決まり、記録も残る。逆にすると**AIの不調中にジャンル審査が止まり、「止まったから通す」が始まる** |
| 2026-08-09 | **検索上位が空なら判定せずに落とす（E-4）。** 空を「該当なし」として通すと、大手が占めるジャンルが検索APIの不調のたびに通ってしまう。CONTENT_PLANNING 2.1 の「取得できないことを理由に停止条件をスキップしてはならない」をそのまま形にした |
| 2026-08-09 | **差し戻しの回数はDBの履歴から数える（E-4）。** 呼び出し側が申告した回数を信じると、2回に満たないまま「承知で進める」を通せてしまい、停止条件が実質的に無くなる |
| 2026-08-09 | **E-4 は終了した案件を数えない。** 数えると、実際には貼れない案件で「案件が0件」の停止条件を回避できる |
| 2026-08-09 | **続行を選んだ回（`OVERRIDDEN`）は差し戻しに数えない。** 数えると、一度承知で進めたあとに次のジャンルで即座に続行できてしまう |
| 2026-08-09 | **AIの応答は1回だけやり直す**（CONTENT_PLANNING 1.2）。壊れた出力を繰り返し引かせても直らず、費用だけが増える。**コードフェンス付きは剥がして読む** — 禁じてはあるが、指示に従わなかっただけで内容は正しいことが多く、1回の再試行を無駄にしない |
| 2026-08-09 | **候補の除外はコードで行う（E-4）。** プロンプトに「HIGH を除いてください」と書いて信じない。除外は判定であり、判定はコードの仕事（CONTENT_PLANNING 1.1） |
| 2026-08-09 | **SPEC 9.8 の場面の一覧に「ジャンル審査」が無かった。** CONTENT_PLANNING 1.3 は MODEL_HIGH を指定している。既存の場面へ寄せると `ai_usage_logs` の内訳が用途とずれるため、`GENRE_REVIEW`（HIGH）と `GENRE_SUGGESTION`（STANDARD）を足した。**SPEC.md は変更していない** |
| 2026-08-09 | **`audit_logs` を書くタスクが無かった**（Q-018）。SPEC は2か所で記録を求めているのに、`audit` モジュールを作るタスクが TASKS に存在しない。H-11 として登録した。E-4 は `planning_runs.overridden_at` に選択を残すので止めない |
| 2026-08-09 | **SPEC 9.2.3 の足切り「ブログ掲載禁止」を判定する列が無かった**（Q-019）。`deny_conditions` の文言から機械判定すると、**黙って通す**（規約違反）のも**黙って落とす**（機会損失）のも起きる。ADMIN が設定する列を足した（`link_mode`・`sub_id_param` と同じ形） |
| 2026-08-09 | **`blog_posting_prohibited` の既定は `false`。ここだけは安全側に倒さない。** `true` にすると、設定されるまで全案件が足切りされ、STEP 2 が常に0件になって STEP 1 へ差し戻され続ける |
| 2026-08-09 | **E-5 はAIに検索需要の3値だけを聞き、点数への写像をコード側の定数にした**（CONTENT_PLANNING 3.2）。実サーバーでAIに `score: 100` を返させても、合計が変わらないことを確かめた。返させると**プロンプト次第で合計が動く** |
| 2026-08-09 | **未評価の案件を0点として採点しない（E-5）。** 0点にすると、LPが良い案件が「LPの質0点」で沈み、**落選の理由が「まだ測っていない」から「質が低い」に化ける。** 足切り理由を分けて持ち、ADMINへ知らせる材料にする |
| 2026-08-09 | **足切りされた案件も点数の内訳を残す（E-5）。** 落ちた理由を後から確かめられないと「なぜこの案件が採用されなかったのか」に答えられない。D-2 で `lp_content_length` を残したのと同じ理由 |
| 2026-08-09 | **足切りされる案件にAIを聞かない（E-5）。** 点数に関わらず採用しないので、呼ぶだけ費用が増える |
| 2026-08-09 | **同点の案件はIDで並びを固定する（E-5）。** 呼ぶたびに採用が入れ替わると、再実行のたびに構成表が変わる（一覧の並びと同じ問題） |
| 2026-08-09 | **検索需要を聞けなかったときは `NONE`（0点）にする。** 「需要が無い」ではなく「聞けなかった」だが、**通るべき案件が落ちるほうが、通すべきでない案件を通すより取り返しがつく** |
| 2026-08-09 | **採点結果の保存は `blog_id` を条件に含める（E-5）。** `offerId` は呼び出し側から渡ってくるため、含めないと他人の案件の点数を書き換えられる（C-6 と同じ形） |
| 2026-08-09 | **E-6 はAIに枠を渡し、枠ごとに文言を付けさせる。** 件数と `slotId` を突き合わせてから保存する（CONTENT_PLANNING 4.2）。確かめずに保存すると、**AIが勝手に足した記事がそのまま構成表に入る**。実サーバーで枠を増やす／減らす応答を返させ、1件も保存されないことを確かめた |
| 2026-08-09 | **収益記事の上限に掛かったら、案件ごとの記事から削る（E-6）。** 比較記事は全体で1本しかなく、落とすと構成そのものが変わる |
| 2026-08-09 | **E-6 はAIを呼べなければ失敗させる。** STEP 1・2 と違い、タイトルと検索意図はAIが作る文言そのもので、コードで代わりを作れない。**空のタイトルで構成表を作らない** |
| 2026-08-09 | **構成表は実行のたびに新しい版を作る（E-6）。** 同じ版を書き換えると、既に投稿された記事が**どの構成表から生まれたのか分からなくなる**（`prompt_versions` を上書きしないのと同じ理由。E-2） |
| 2026-08-09 | **記事は1つのトランザクションでまとめて入れる（E-6）。** 途中で失敗すると半端な構成表（収益記事が3本だけ）が残り、次に走らせたときの重複を数えるのが難しくなる |
| 2026-08-09 | **採用案件のIDをそのまま使わない（E-6）。** 渡されたIDがこのブログの案件かを `listOffersForUser` 経由で確かめる。確かめないと**他人の案件で構成表を作れる**（C-6 と同じ形） |
| 2026-08-09 | **E-7 はリンク先が `AFFILIATE` だけであることを保存の直前に検査する**（完了条件）。組み立ての途中で確かめると、あとから足された分をすり抜ける。CONTENT_PLANNING 5.5 が「手作業での検証では30本中9本でこの誤りが発生した」と書いている箇所 |
| 2026-08-09 | **比較記事もリンク先にしない（E-7）。** 収益記事ではあるが種別が `COMPARISON` で、規則をそのまま適用する。**例外を作ると「どの種別なら許されるのか」を各所で判断することになる。** 被リンク3本以上の対象からも外れる |
| 2026-08-09 | **キーワードの重複はコードで正規化して突合する（E-7）。** NFKC＋空白畳み＋小文字（CONTENT_PLANNING 5.3 の実装そのまま）。通さないと「ＶＯＤ　比較」と「vod 比較」が別物として通る。**`existingKeywords` をAIに渡しても重複は出る** |
| 2026-08-09 | **差し替えても重複が残る候補は落とす（E-7）。** 通すと、ブログ内に同じキーワードの記事が2本できる。差し替えは重複した項目だけを対象にする — 全体を作り直させると、重複していなかった記事まで変わり、何が直ったのか分からなくなる |
| 2026-08-09 | **AIが返した収益記事IDをそのまま使わない（E-7）。** 構成表にあるIDだけを通す。使うと、他の記事や存在しないIDへリンクが向く |
| 2026-08-09 | **D-8 で残した穴は E-7 では塞げなかった**（Q-020）。`affiliate` から記事の持ち主を確かめると `affiliate → content-planning` になり、**依存が循環する**（向きは `content-planning → affiliate`）。C-6 と同じく**制約をDBへ置く**ことにし、`affiliate_links.blog_id` と複合外部キーを D-11 として登録した |
| 2026-08-09 | **D-8 の穴を C-6 と同じやり方で塞いだ（D-11）。** アプリ層では塞げない（`affiliate → content-planning` が循環する）ため、`affiliate_links.blog_id` を足して複合外部キーで強制する。**どのモジュールから書いても迂回できない** |
| 2026-08-09 | **D-11 はマイグレーションと実装を分けられなかった。** `blog_id` は NOT NULL で、埋める側が同じ変更に要る（分けると、間の状態でリンクが1本も作れない）。DATA_MODEL 9章の決まりは**混ぜる必然性が無い場合**のもの |
| 2026-08-09 | **複合外部キーの `ON DELETE SET NULL` は使えない。** 参照している列を全て NULL にするため、NOT NULL の列と両立しない。PostgreSQL 15 以降の列指定（`SET NULL (col)`）は Prisma が表現できず、スキーマとの乖離になる。**`CASCADE` にしても挙動は変わらない**（`content_items` が単独で消えることは無い） |
| 2026-08-09 | **E-8 は「だいたい通っている」を返さない。** 1つでも欠ければ不合格で、3回で収束しなければ例外にする。SPEC 9.2.6 が「**暫定的な構成表を承認依頼へ送ってはならない**」と定めている — 通らないものを返せるようにすると、いつか送られる |
| 2026-08-09 | **制約チェックは保存された値で行う（E-8）。** 組み立ての途中の値ではなく、DBに入ったものを引き直して判定する。途中の値で通しても、保存で落ちたぶんは見えない |
| 2026-08-09 | **やり直しは構成表の版を増やして作る（E-8）。** 前の版を書き換えない。4回試したなら4つの版が残り、**何が違って通らなかったのか**を後から比べられる |
| 2026-08-09 | **試行のたびに `planning_runs` へ残す（E-8）。** 通っても通らなくても書く。CONTENT_PLANNING 9章が「再生成が何回発生しているかが、プロンプト改善の主要な指標」と書いている |
| 2026-08-09 | **手がかりは不足している記事のIDだけ渡す（E-8 の `buildRepairHints`）。** 全体を作り直させると、通っていた記事まで変わる |
| 2026-08-09 | **公開週が未割り当てなら週の上限を判定しない（E-8）。** 割り当てるのは E-9 で、その前に判定すると必ず通ってしまうか、必ず落ちるかのどちらかになる |
| 2026-08-09 | **E-9 の週の上限はブログの設定を使う。** SPEC 2.2 の4本は絶対の上限で、`weeklyPublishCap`（1〜4本。B-5）はそれ以下。**上限が低いと収益記事が2週に収まらない**（案件3件なら7本）。SPEC 9.2.7 の「1〜2週目」は週4本を前提にした書き方で、**守るべきは「収益記事が先行する」ほう**（完了条件）。上限を破って詰め込むのは SPEC 2.2 に反する |
| 2026-08-09 | **集客記事はリンク先の収益記事が公開される順に並べる（E-9）。** 先に出る収益記事へ流す記事を先に出すほうが、流入が早く効く。同じリンク先なら構成表の順で固定する（呼ぶたびに入れ替わらない） |
| 2026-08-09 | **公開順序は制約チェックの前に付ける（E-9）。** 週の上限は制約チェックの対象で、未割り当てのまま判定すると必ず通ってしまう |
| 2026-08-09 | **`PLAN_GENERATION` ジョブを登録した（E-9）。** E-1 で基盤を作って以来、はじめて実際に動くハンドラ。**`user_id` と `blog_id` はジョブの列から取り、`input_json` からは取らない** — 入力を信じると、他人のブログの構成表を作れる |
| 2026-08-09 | **E-10 の入口は `contentItemId` だけ。** タイトルを渡して1本作る関数を置かない（完了条件「単体生成モードを作らない」）。置くと、**内部リンクも公開順序も持たない孤立した記事**が生まれる |
| 2026-08-09 | **本文のリンクは実際に抜き出して照合する（E-10）。** AIの「指定されたリンクだけ使いました」を信じない。CONTENT_PLANNING 7.2 が「プロンプトに明記し、**かつ受信後にコードで検査する**」と定めている。外部サイトへ自由にリンクさせると、意図しない誘導になりASPの規約にも触れうる |
| 2026-08-09 | **PR表記は記事の種別ではなく広告リンクの有無で要る（E-10）。** 景表法のステマ規制が求めるのは「広告であることを示す」ことで、収益記事かどうかではない |
| 2026-08-09 | **生成しただけでは `READY_FOR_REVIEW` にしない（E-10）。** 事実チェック（E-12）と禁止表現の検査（E-13）を通っていない。ここで状態を進めると、未検査の記事が承認依頼へ流れる |
| 2026-08-09 | **記事の版は上書きしない（E-10）。** 修正依頼で作り直したときに「何がどう変わったか」を承認画面（F-5）で見せるため |
| 2026-08-09 | **`REDIRECT` の案件は生成時にコードを発行する（E-10）。** ここで `affiliate_links` に行ができ、記事と案件が同じブログであることは D-11 の複合外部キーがDB側で確かめる |
| 2026-08-09 | **結論を置く位置をAIに任せない（E-11）。** WordPress は記事タイトルを H1 として描画するので、本文の先頭＝H1直後。カプセルをコードで本文の先頭へ挿し、本文の `<h1>` を禁じる。「H1直後に置いてください」と頼むだけでは、置かれたかどうかを後から確かめられない |
| 2026-08-09 | **文字数は `String.length` で数えない（E-11）。** UTF-16 のコード単位を数えるため、絵文字や一部の漢字が2文字に見え、**80字の結論が「120字を超えた」と落ちる**。`[...text].length` で数える |
| 2026-08-09 | **長さの違反は落とさず作り直す（E-11）。** CONTENT_PLANNING 7.2 の「範囲外なら再生成」。次の試行で直りうる違反と、作り直しても変わらない違反（構成表との突き合わせ）を、呼ぶ場所で分ける |
| 2026-08-09 | **失敗した試行の費用も記録する（E-11）。** CONTENT_PLANNING 9 の「再生成ループの各試行も個別に記録する」。E-10 は最終試行しか記録しておらず、**作り直しが何回起きているかというプロンプト改善の主要な指標が消えていた** |
| 2026-08-09 | **評点を作り出さない（E-11、Q-021）。** Google の `Review` は `reviewRating` を必須とするが、評点の出どころが無い。ここで数字を入れることは SPEC 9.6 が禁じる「根拠のないランキング」そのもの。**リッチリザルトの対象外という不利益を受け入れて、無いままにした** |
| 2026-08-09 | **AIに「その主張は正しいか」を訊かない（E-12）。** 訊けば「正しいです」と返ってくるだけで、**確かめたことにならない**。訊くのは「何を主張しているか」だけで、照合はコードで行う（CONTENT_PLANNING 8.1） |
| 2026-08-09 | **照合できなかったものを「確認済み」に倒さない（E-12）。** 文字列一致で日本語の言い換えは追えない。誤って未確認にした損失は「人が1つ確かめる」で、誤って確認済みにした損失は「嘘が公開される」 |
| 2026-08-09 | **`updatedAt` が無い facts は古い扱い（E-12、Q-022）。** 「いつ確かめたか分からない」を「新しい」に倒すと、**測っていないことが「問題なし」に化ける**。結果として全収益記事が `WARNING` になるが、鳴りっぱなしのほうが安全側 |
| 2026-08-09 | **事実チェックを飛ばす経路を作らない（E-12）。** 別の入口として置くと「呼び忘れた記事」が `NOT_CHECKED` のまま残り、それが承認画面で「問題なし」に見える。生成の中から必ず呼ぶ |
| 2026-08-09 | **「主張が0件」と「抽出できなかった」を分ける（E-12）。** 同じ扱いにすると、**壊れた応答が `PASSED` に化ける** |
| 2026-08-09 | **仕様が固定したキーを、自分の検証で弾いていた（E-2 の見落とし・E-12 で判明）。** `prompt_versions.key` の形式がアンダースコアを許さず、CONTENT_PLANNING 1.4 の9個中7個を管理画面から登録できなかった。**検証の形式を決めるときは、仕様に挙がっている実例を全部通してみる** |
| 2026-08-09 | **`warning` で承認を止めない（E-13）。** 「ランキング」の一語で記事が進まなくなる。止めるのは法令に触れるものと、利用者が明示的に禁じた語だけ。**判断は承認者に残す**（SPEC 1.1「LINE承認型」） |
| 2026-08-09 | **承認へ送ってよいかの判定を1つにした（E-13）。** 事実チェックとリスクフラグを別々に問える関数を残すと、**いつか片方だけで通す経路ができる**。E-12 の `isApprovable` は `factCheckAllowsApproval` へ改名し、「これは半分」と名前で示した |
| 2026-08-09 | **禁止表現の検査でタグの中を見ない（E-13）。** `<a href="...ranking...">` の属性が「ランキング」として拾われると、本文に無いものを指摘することになる |
| 2026-08-09 | **語彙で拾える範囲しか拾えないと認める（E-13）。** 言い換えは追えない。ここを通ったから安全なのではなく、**明らかなものを機械が先に落とす**ための仕組みで、最後に見るのは承認者 |
| 2026-08-10 | **「同一提案を連続通知しない」を通知の側で数えない（F-1-schema）。** SPEC 8.3 の要求だが、通知の直前で数えると入口が増えたときに漏れる。**`approvals.article_version_id` を一意にして作れないようにした**（C-6 と同じ筋） |
| 2026-08-10 | **公開順序は E-9 の判断をそのまま使う（F-1）。** 収益記事の先行も週の割り当ても `publish_priority` に入っている。提案の側で同じ判断をやり直すと、**2箇所で順序を決めることになり、いつか食い違う** |
| 2026-08-10 | **待たせているブログを上げる（F-1）。** 1日1件しか送れないため（SPEC 8.3）、公開順序だけで並べると**3ブログのうち1つが勝ち続け、残り2つは永久に提案が出ない** |
| 2026-08-10 | **提案理由をAIに書かせない（F-1）。** 理由は点の付け方と一致していなければならず、書かせると「点は低いのに理由は強い」記事が生まれる |
| 2026-08-10 | **「提案を作る」と「提案を送る」を分けた（F-1）。** 送信の中で作ると、送信に失敗したときに提案ごと消える。件数制御（F-3）も送信側の話 |
| 2026-08-10 | **送る前に `sent_at` を立てる（F-2）。** 送ってから立てると、送信の直後に落ちたときに二度届く。逆にすると「立てたが送れなかった」が起こりうるが、**提案は承認一覧に残る**ので消えはしない。SPEC 8.3 が禁じているのは重複通知のほう |
| 2026-08-10 | **宛先は押さえる前に確かめる（F-2）。** 押さえてから宛先が無いと分かると、**送っていない提案に `sent_at` が立ったまま残る** |
| 2026-08-10 | **`line_user_id` を `AppUser` に載せない（F-2）。** 身元そのもので、画面へ渡る型に混ぜると出力先が増える（SPEC 14.2）。通知を送るときだけ専用の経路から取る |
| 2026-08-10 | **通知の文面をAIに書かせない（F-2）。** SPEC 8.2 が形式を定めており、**毎回同じ形で届くこと自体が「確認時間：約3分」という前提を支えている** |
| 2026-08-10 | **「3ブログ合計で制限」は数える単位の話（F-3）。** ブログごとに1日1件にすると、3ブログ持つ人には1日3件届く。SPEC 8.3 が言っているのは利用者単位で数えるということ |
| 2026-08-10 | **通知の件数はJSTの暦日で数える（F-3）。** UTCで数えると日本の1日が2日にまたがり、**夜に届いた1件が翌日の枠を1つ消す** |
| 2026-08-10 | **上限は読むときに丸める（F-3）。** `max_daily_proposals` は整数の列で3以上も入る。設定画面の検証だけに頼ると、直接書き換えられた値がそのまま効く |
| 2026-08-10 | **緊急通知は行を作らないことで別枠になる（F-3）。** 提案と同じ数え方にすると「今日は1件送ったので接続切れを知らせない」が起きる。数えているのは `approvals.sent_at` なので、そこを通らない通知は数えようがない |
| 2026-08-10 | **「他人のを開けない」は指定させないことで満たす（F-4）。** 絞り込みのクエリを受け付けてから弾く形にすると、**弾き忘れが穴になる**。入口を利用者単位にすれば、弾く処理そのものが要らない |
| 2026-08-10 | **仕様の一覧に無い状態を捨てない（F-4）。** SPEC 6.1 のタブは4つで `EXPIRED` が無い。どこにも置かないと画面から消えるので「見送り」に入れ、**行の表示は分けた** — 「自分で見送った」と「期限が切れた」は別の記録 |
| 2026-08-10 | **開いただけで一覧から消さない（F-4）。** `VIEWED` を承認待ちに残す。消すと「あとで答えよう」と閉じた提案を見つけられなくなる |
| 2026-08-10 | **飛び先の無いリンクを先に置かない（F-4）。** 詳細画面は F-5。リンクだけ先に作ると、押せて何も出ない状態になる |
| 2026-08-10 | **画面の表示ロジックをモジュールに置かない（F-4）。** `approvals` の `index.ts` は `repository.ts` 経由で Prisma を引き込む。クライアントコンポーネントから読むと**ブラウザ向けの束に `node:dns` が混ざりビルドが落ちる**（MODULE_RULES 4）。表記が `_lib/labels.ts` にあるのと同じ理由で、並べ分けも `_lib/` へ置く |
| 2026-08-10 | **ビルドの確認を grep で済ませない（F-4 で自分が踏んだ）。** `npm run build` の出力から「Compiled successfully」を拾って通ったと判断したが、**その後の段階で落ちていた**。確かめるのは終了コード |
| 2026-08-10 | **AIが書いたHTMLをページのDOMへ入れない（F-5）。** E-13 の検査は**表現**を見るもので、スクリプトを落とさない。`<img onerror=...>` ひとつでセッションを触られる。`sandbox` を空にした `iframe` に入れ、**ブラウザに止めさせる**（自前のサニタイザを書くより保証が固い） |
| 2026-08-10 | **承認URLは文字として見せる（F-5）。** 承認の場でアフィリエイトリンクを踏ませると、実際のクリックが発生する。SPEC 6.1 が「アフィリエイトURL」を別項目として並べているのはそのため |
| 2026-08-10 | **`viewed_at` は最初に開いた時刻のまま（F-5）。**「いつ気づいたか」の記録で、開くたびに更新すると意味が変わる |
| 2026-08-10 | **`params` の Promise をクライアントで開かない（F-5）。** `use()` は未解決ならサスペンドし、境界の有無で描画が変わる。ページ（Server Component）で解いて文字列を渡せば、画面は素直に試験できる |
| 2026-08-10 | **同じ答えの二度押しは成功させる（F-6）。** LINEの通信は落ちるし、人は二度押す。**二度目にエラーを返すと「押せていない」と思って三度押す。** 冪等にするのは通信の都合ではなく、押す人の都合 |
| 2026-08-10 | **違う答えは受け付けない（F-6）。** 承認した提案を見送りへ変えられると、**何を承認したのかが分からなくなる**。冪等と「上書き可能」は別物 |
| 2026-08-10 | **読み取りで状態を変えない（F-6 で F-5 を訂正）。** SPEC 13.6 は `GET /:id` と `POST /:id/view` を別に定めている。GETで既読にすると、**一覧を先読みしただけで「開いた」ことになる** |
| 2026-08-10 | **「後で確認」にボタンを置かない（F-6）。** 何もせず閉じることがそれ。操作を足すと「押さないと保留にならない」と誤解させる |
| 2026-08-10 | **承認とジョブを同じトランザクションで決める（F-7）。** 承認したのにジョブが無いと記事は永久に投稿されず、承認が巻き戻ったのにジョブだけ残ると**承認していない記事が投稿される**。どちらも後から気づけない |
| 2026-08-10 | **どの版を投稿するかを入力から取る（F-7）。** 最新の版を勝手に拾うと、**承認後に作り直された版が承認なしで公開されうる** |
| 2026-08-10 | **投稿できてから状態を進める（F-7）。** 先に `POSTED` にすると、投稿に失敗した記事が「投稿済み」に見える |
| 2026-08-10 | **外部呼び出しだけを差し替えられる形にする（F-7）。** ジョブの種類と処理の対応は1箇所のまま（`createJobHandlers`）。試験のために登録そのものを差し替えられると、**本番と違う対応表で通ったテスト**が生まれる |
| 2026-08-10 | **参加は「登録できた」ではなく「認めた」で決まる（H-1）。** `users.status` の既定が `INVITED` で、そのままではアプリを使えない。実験の参加者数を運営が握れる形になっている |
| 2026-08-10 | **状態の遷移を表にして1箇所に置く（H-1）。** 条件分岐に散らすと「停止中を承認できてしまう」ような穴が後から入る |
| 2026-08-10 | **戻せない操作を戻せる操作と同じ並びに置かない（H-1）。** 退会（`WITHDRAWN`）のボタンを停止の隣に置くと、**停止のつもりで退会させる事故**が起きる |
| 2026-08-10 | **ADMIN を管理操作の対象にしない（H-1）。** ADMIN 同士で停止し合えると、**誰も管理画面に入れない状態**を作れる |
| 2026-08-10 | **監査ログに正常系を残さない（H-11）。** 全部残すと、後から見たときに**異常が埋もれる**。残すのは「承知で進める」とADMINの介入だけ |
| 2026-08-10 | **`action` を自由入力にしない（H-11）。** 列は `text` だが、書く側が好きな語を入れると集計できない。使える語を1箇所に並べた |
| 2026-08-10 | **記録の失敗で本体を止めない（H-11）。** 監査ログは後から辿るためのもので、**書けないことを理由に操作を失敗させると本末転倒**。ただしトランザクションから呼ぶ経路は別で、本体と一緒に巻き戻す — 起きなかったことを記録しない |
| 2026-08-10 | **行為者を必須の引数にした（H-11）。** `actorUserId` を省略できると「誰がやったか分からない記録」が静かに増える。型で書かせる |
| 2026-08-10 | **抜けた人の記録を消さない（H-4）。** Phase 0 は10名がどこまで続いたかを見る実験で、消すと**「10名中何名が続いたか」が数えられなくなる**。SPEC 13.2 の「物理削除せずCLOSED」はその実験設計から来ている |
| 2026-08-10 | **戻せない操作に別の入口を与える（H-4）。** 退会を `/status` の引数の一種にすると、値を間違えたときの結果が重すぎる。H-1 で画面のボタンを分けたのと同じ理由 |
| 2026-08-10 | **エクスポートに秘密を入れない（H-4）。** 暗号化して保存しているものを復号して出す先を作らない。`line_user_id` も入れない — 出力の行き先が増えるほど漏れる面が広がる |
| 2026-08-10 | **退会後のエクスポートで空にならないようにする（H-4）。** 閉じたブログを外すと、**持ち出すときには何も入っていない**という状態になる |
| 2026-08-10 | **バックアップ手順の先頭に「鍵が無いと戻らない」を置いた（H-5）。** DBだけ戻しても `ENCRYPTION_KEY` を失っていれば、**全モニターのWordPressへ投稿できなくなる**。手順の途中に書くと、障害の最中に読み飛ばされる |
| 2026-08-10 | **鍵をDBのバックアップと同じ場所に置かない（H-5）。** 同じ場所だと、その場所を失ったときに両方まとめて消える |
| 2026-08-10 | **一度も戻したことのないバックアップはバックアップではない（H-5）。** 月1回の復旧確認を手順に入れ、**結果を HISTORY に残す**ことまで含めた。「やったつもり」を防ぐ |
| 2026-08-10 | **ダンプを暗号化して保管する（H-5）。** 中身には `line_user_id` と記事の本文が入る（SPEC 14.2）。バックアップは「安全な場所にある」と暗黙に扱われやすい |
| 2026-08-10 | **同じことを毎日言わない（H-3）。** 接続が切れたままなら検出は毎回当たる。毎回送ると**通知が「読まなくてよいもの」になる**。冪等キーに日付を入れて1日1回にし、直っていなければ翌日また届くようにした |
| 2026-08-10 | **落ちたことと切れたことを分ける（H-3）。** 404/410 だけを「消えた」とする。401/403 は**ASPが機械的なアクセスを弾いているだけ**のことが多く、人が開けばリンクは生きている |
| 2026-08-10 | **確認では `affiliate_url` を踏まない（H-3）。** 踏むとクリックとして計上されうる。確かめるのは `landing_page_url` |
| 2026-08-10 | **準備中のブログに毎日エラーを送らない（H-3）。** 一度も接続を試していないブログは対象外にした |
| 2026-08-10 | **NULL を含む一意制約は効いていない（G-5-schema）。** PostgreSQL は NULL 同士を「違う値」として扱うため、`(blog_id, content_item_id, metric_date)` の一意は `content_item_id IS NULL` の行に効かない。**重複した行は集計で二重に数えられ、しかも気づけない。** `NULLS NOT DISTINCT` で塞いだ |
| 2026-08-10 | **「0件」と「未報告」を分ける（G-5）。** 0件の週に数字を入れるのは面倒で放っておかれる。すると記録に穴が空き、**あとから「成果が無かった」のか「報告されなかった」のかが分からない**。完了条件が「0件を1操作で」と書いている理由 |
| 2026-08-10 | **0件なのに報酬があれば落とし、成果があって0円は通す（G-5）。** 前者は打ち間違いだが、後者は承認待ちの案件でありうる。対称に扱わない |
| 2026-08-10 | **取れていない指標の枠を画面に並べない（G-5）。** 自動収集が未実装のまま枠だけ出すと、**「取れているのに0」と読める** |
| 2026-08-10 | **対象ドメインは足せるだけにする（G-4）。** 既定を環境変数で上書きできる形にすると、誤って空にしたときに判別が全て `false` になり、**「AI経由の流入が無い」という読めてしまう結果**が静かに出る。`AI_REFERRAL_EXTRA_DOMAINS` は既定に追加するだけ |
| 2026-08-10 | **末尾一致に区切りを入れる（G-4）。** 単純な `endsWith` だと `notchatgpt.com` が `chatgpt.com` に一致する。誰でも取れるドメインでAI流入を水増しできる |
| 2026-08-10 | **判別の結果と元の値を分けて持つ（G-4）。** `is_ai_referral` は集計を速くするための派生で、元は `referrer_host`。**ドメインを足したら過去のクリックを数え直せる**（`recountAiReferrals`）。数え直しで `referrer_host` は書き換えない |
| 2026-08-10 | **取れなかった `Referer` を「AI経由でない」に倒す（G-4）。** そのぶんこの数は必ず実際より少ない。SPEC 11.4 が「完全値として扱わない」と書いているとおり、表記も「判別可能なAIサービス経由流入数」にする |
| 2026-08-11 | **未審査のOAuthは3か月もたない（Q-030）。** `webmasters.readonly` は機密スコープで、審査を通さない公開状態は「テスト」。**そのリフレッシュトークンは7日で失効する。** 10名が毎週つなぎ直すことになり、日次の取得ジョブが週明けに落ちてデータに穴が空く |
| 2026-08-11 | **OAuthのスコープはプロパティ単位で絞れない（Q-030）。** モニター自身の同意だと、そのGoogleアカウントにある**全プロパティ**が読める。サービスアカウントなら、モニターが実験対象のブログだけを指定できる |
| 2026-08-11 | **使わない NOT NULL の列を残さない（G-1-schema）。** 埋める値が無いのに残すと、後から読む人に「入っているはず」と読ませる。落とすほうが正直 |
| 2026-08-11 | **仕様と食い違ったら、仕様を黙って直さない（Q-030）。** SPEC 11.3 は「ブログ単位でOAuth連携」と書いている。`docs/SPEC.md` は唯一の正なので書き換えず、食い違いを OPEN_QUESTIONS に残した |
| 2026-08-11 | **連携を「保存できた」で終わりにしない（G-1）。** モニターが Search Console 側でアドレスを追加していなければ、URLを保存しただけでは何も取れない。**保存と同時に問い合わせて結果を残す** — 取れないまま「連携済み」に見えると、G-2 が動き出すまで誰も気づけない |
| 2026-08-11 | **確かめられなくても行は残す（G-1）。** モニターが先にURLを入れ、あとから権限を渡す順序がありうる。消してしまうとやり直しに1手増える |
| 2026-08-11 | **プロパティのURLは2つの形を両方受ける（G-1）。** `sc-domain:` とURLプレフィックスがあり、**モニターがどちらを作ったかはこちらで決められない。** 片方しか受けないと、オンボーディングで「合っているのに弾かれる」が起きる |
| 2026-08-11 | **404 と 403 を同じに倒す（G-1）。** Google はどちらでも返しうるが、**モニターがすることは同じ**（アドレスを追加する／URLを直す）。ただし所有確認前（`siteUnverifiedUser`）は別扱いにした — 頼むことが違う |
| 2026-08-11 | **鍵は1つのJSONとして保存する（G-1）。** `client_email` と `private_key` を別の設定に分けると、**片方だけ差し替えられて食い違う**。Google が配るのもこのJSONそのもの |
| 2026-08-11 | **要求するのは読み取りスコープだけ（G-1）。** こちらがするのは取得だけで、万一鍵が漏れてもモニターのプロパティ設定を変えられないほうがよい |
| 2026-08-11 | **繋ぎ直しを誤りにしない（G-1）。** WordPress は `REVOKED` で `site_url` を残す（Q-007）が、Search Console は行ごと消す。**ドメインプロパティとURLプレフィックスの取り違えは起きる**し、残しておくべき秘密も無い |
| 2026-08-11 | **昨日ぶんだけ取らない（G-2）。** Search Console のデータは遅れて確定する。昨日ぶんを取って保存で終わりにすると、**取りこぼしたまま二度と取り直さない**。直近5日を毎回取り直して上書きする |
| 2026-08-11 | **ブログ全体をページの合計で代用しない（G-2）。** 1回の検索で複数ページが出れば表示は重複して数えられ、**平均掲載順位は加重平均なので足せない**。次元を減らしてGoogleに集計させ、その値をそのまま入れる |
| 2026-08-11 | **触るのは自分の列だけ（G-2）。** 同じ `metrics_daily` の行には手入力の成果（G-5）が入っている。行ごと置き換えると、モニターが入れた数字が消える |
| 2026-08-11 | **上限に満たなければそこで終わり（G-2）。** 空の応答を待つと1往復ぶん無駄になる。逆に無限に回さない歯止めも置き、**達したら黙って打ち切らず記録する** |
| 2026-08-11 | **やり直して直るかを持たせる（G-2）。** 429・5xx は時間をおけば直るが、403 は何度やっても同じ。**再試行はモニターに何も知らせないまま回数を消費する** |
| 2026-08-11 | **ジョブはブログごとに積む（G-2）。** 1件で3ブログ回すと、2つ目で落ちたときに1つ目までやり直すことになる |
| 2026-08-11 | **URLは末尾の `/` と大文字小文字を揃えて突き合わせる（G-2）。** WordPress のパーマリンクと Search Console が返すURLは食い違う。揃えないと**記事があるのに0件として並ぶ** |
| 2026-08-11 | **上限の枠が違うものを同じジョブに入れない（G-3）。** URL Inspection は記事の本数だけ呼ぶ（1プロパティ1日2,000回）。Search Analytics と同じジョブにすると、**上限に当たったときに取れていたはずの検索データまで巻き戻る** |
| 2026-08-11 | **「分からない」を `false` に倒さない（G-3）。** Google が判断を返さない日は書かない。倒すと「調べたが載っていない」と区別できず、**インデックス率が実際より低く出る** |
| 2026-08-11 | **`date` 型の列に「JSTの00:00のUTC瞬間」を渡すと1日前になる（Q-031）。** `startOfJstDay('2026-08-11')` は `2026-08-10T15:00Z` で、日付部分は前日。**実測で確かめた。** 書き手も読み手も同じ関数なので今は壊れないが、**外と突き合わせるとすべて1日ずれる** |
| 2026-08-11 | **`date` 型の列と `timestamptz` で渡す値を変える（Q-031）。** 「JSTの00:00を表すUTCの瞬間」は範囲検索では正しいが、`date` 型の列に渡すと**日付部分が前日になる**。関数を分け、doc にどちらをどこで使うかの表を置いた |
| 2026-08-11 | **DBが実際に何を持っているかを試験で固定する（Q-031）。** JSの `Date` を突き合わせるだけでは、書き手と読み手が同じ関数を使っている限り**ずれたまま通る**。`select metric_date::text` で確かめた |
| 2026-08-11 | **データが入る前に直す（Q-031）。** 本番にまだ1行も無い。実験が始まると古い行と新しい行で基準が変わり、行ごとにどちらの日付か判断することになる |
| 2026-08-11 | **条件を日ごとに写し取らない（G-6）。** ジャンル・競争レベル・戦略はブログの属性として既にある。日次表に複製すると、**変えたときにどちらが正か分からなくなる**。SPEC 10.3 が「条件の記録は行い、集計はSQLで実施する」と定めているのはこの形 |
| 2026-08-11 | **記事に紐づかないクリックも全体には数える（G-6）。** 落とすと「記事の合計＝ブログ全体」に見えて、**実際より少なく読める** |
| 2026-08-11 | **クリックが無いブログに空行を作らない（G-6）。** ただし既にある行は0で更新する。「数えた結果0」と「まだ数えていない」を分ける |
| 2026-08-11 | **測っていないものにそれらしい数字を入れない（Q-032）。** `banner_clicks` は0のままなら「測っていない」と読めるが、何か入れると「測ったが少ない」と読めてしまう |
| 2026-08-11 | **完了条件の項目を1つずつ当たる（G-6）。** SPEC 10.2 の19項目を数え上げて初めて、**2項目に測る手段が無い**ことが分かった。「集計を書いた」で終わらせると気づけない |
| 2026-08-11 | **モジュールをまたぐ集計は上位へ寄せる（G-7）。** 5つのテーブルを同時に結合するため、どれか1つのモジュールに置くと MODULE_RULES 1 に反する。MODULE_RULES 3 が示す「上位へ寄せる」に当たり、TASKS の主な変更先も `src/app/admin/` |
| 2026-08-11 | **記事ごとの行とブログ全体の行を一緒に足さない（G-7）。** 足すと同じクリックを二重に数える。`content_item_id IS NULL` で絞る |
| 2026-08-11 | **指標と費用を1つのクエリで join しない（G-7）。** 行数の掛け算で数が膨らむ。CTEで別々に集めてから結合する |
| 2026-08-11 | **未設定を「その他」に混ぜない（G-7）。** ジャンルや戦略が付いていないこと自体が、運営が直すべき状態である |
| 2026-08-11 | **測っていない列を画面に出さない（G-7・Q-032）。** 0として並べると「測ったが0だった」と読める。G-5 で「取れていない指標の枠を並べない」と決めたのと同じ |
| 2026-08-11 | **破壊的なスキーマ変更を「足す・移す・消す」に分ける（A-2-R-1、Q-033）。** 20ファイルから参照されているモデルを一度に置き換えると、スキーマだけのPRでもCIが赤になりマージできない。各段階でCIが緑に保たれる形にした |
| 2026-08-11 | **分身の設定を4つのjsonbに散らさない（A-2-R-1）。** 旧 `base_profile`/`tone`/`values`/`ng_expressions` を `identity` に統合した。分けて持つと、分身を1つ作るのに4つ埋めることになり、どれがどこに効くのか読めない |
| 2026-08-11 | **記憶と成長の専用テーブルを作らない（A-2-R-1）。** 記憶は `persona_facts` と記事履歴、成長は `metrics_daily` で代替する。**同じことを2か所に持つと、どちらが正か分からなくなる** |
| 2026-08-11 | **分身を消してもブログを道連れにしない（A-2-R-1）。** `blogs.persona_id` の `onDelete` は `Restrict`。媒体はモニターの資産で、人格の整理で消えてよいものではない |
| 2026-08-11 | **テーブル数の見張りに、次にどう変わるかを書く（A-2-R-1）。** 28 → 29 に更新し、A-2-R-3 で 28 に戻ることをコメントに残した。数字だけ直すと、次に落ちたとき理由が分からない |
| 2026-08-11 | **記憶は媒体ではなく人格に溜める（A-2-R-2-schema）。** 同じ分身が将来べつの媒体へ広がっても引き継げるようにする。`persona_facts` の所属を `personas` へ移した |
| 2026-08-11 | **`onDelete` を対象ごとに変える（A-2-R-2-schema）。** ブログは `Restrict`（モニターの資産で、人格の整理で消えてよいものではない）、記憶は `Cascade`（分身の一部）。揃えるほうが読みやすいが、性質が違う |
| 2026-08-11 | **一意でなくなったIDは、所有者と一緒に絞る（A-2-R-2a）。** 旧 `user_personas` は `user_id` が unique で、条件に入れるだけで越境の余地が無かった。1ユーザー複数件になった時点で、`id` だけで引く入口は他人のものを掴める |
| 2026-08-11 | **作ることと使い始めることを分ける（A-2-R-2a）。** 作った直後は `DRAFT`。`ACTIVE` の上限に当たっても下書きは作れるので、作業が止まらない |
| 2026-08-11 | **`activated_at` を再開で更新しない（A-2-R-2a）。** 段階解放の起点なので、止めて再開するたびに動くと日数の数え方が変わる |
| 2026-08-11 | **撤退条件を先に決めさせる（A-2-R-2a）。** 続けるかどうかを後から決めると、沈んだ費用に引きずられる。分身を作る時点で必須にした |
| 2026-08-11 | **起点が決まっていない計算を呼ばない（Q-034）。** 段階解放の日数は純粋関数として置き、参加開始日が決まるまで配線しない。仮の起点で動かすと、承認待ちの長い人ほど早く枠が開く |
| 2026-08-11 | **リダイレクタを各ブログのドメインへ移す（Q-001 の再決定）。** 30ブログが同一の外部ドメインへリンクすると、**運営者の同一性を示す痕跡になる**。計測の一元化より footprint の回避を優先した |
| 2026-08-11 | **完了したタスクの完了条件を後から変えない（Q-001 の再決定）。** D-8 は実装・マージ済み。置き換えは D-12 として別に起票した。書き換えると、何が済んでいて何が済んでいないのかが読めなくなる |
| 2026-08-11 | **テーブルは変えずに「誰が行を作るか」だけ変える（D-12）。** `link_clicks` と `affiliate_links` はそのまま使え、集計（G-4・G-6）も影響を受けない |
| 2026-08-11 | **参加開始日を登録時刻で代用しない（Q-034）。** 登録しただけでは `INVITED` でアプリを使えない。承認待ちが長い人ほど早く分身の枠が開いてしまう。`users.activated_at` を足した |
| 2026-08-11 | **起点は最初の1回だけ入れる（Q-034）。** 段階解放・90日検証の期間・8週間継続率の3つが同じ列を見る。停止・再開で動くと3つとも数え方が変わる |
| 2026-08-11 | **参加前は段階解放を効かせない（A-2-R-2b）。** ここで断ると「作れるのに使い始められない」状態になり、原因が画面から読めない。参加前に使えないことは `isActiveUser` が別に担保している |
| 2026-08-11 | **断る文言に経過日数を出す（A-2-R-2b）。** 「上限です」だけでは、いつ開くのかが分からない。待てば開くものと、待っても開かないものを区別できるようにする |
| 2026-08-11 | **後から変えると高くつく選択だけを先に決める（Q-028）。** プロバイダの乗り換えは `pg_dump` で済むが、**保管場所を国外から国内へ移すのは同意の取り直しになりうる**。不可逆なのはリージョンだけなので、そこを東京に固定した |
| 2026-08-11 | **PITR が標準のものを選ぶ（Q-028）。** 別料金のアドオンにすると、規模が変わるたびに費用の判断が要る。誤った `UPDATE` 1回でその日の承認が全部消えることを防ぐ |
| 2026-08-11 | **アプリとDBを同じリージョンに置く（Q-028）。** ジョブは1件ずつ順番にクエリを投げるため、離れていると1往復ぶんの遅延が何十回も積み上がる。「日本にあること」ではなく「同居していること」が速度の条件 |
| 2026-08-11 | **接続プールをいま用意しない（Q-028）。** 30ブログでは接続数を使い切らない。必要になったら PgBouncer を挟む — **「足す」変更であって「移す」変更ではない** |
| 2026-08-11 | **完了したタスクの完了条件を広げない（Q-025・Q-027・Q-029）。** 通知の時刻は F-3b、監査ログの残りは H-12、リンク切れの保存は H-3b として別に起票した。後から広げると、何が済んでいて何が済んでいないのかが読めなくなる |
| 2026-08-11 | **同じ経路に乗せられるものは同時にやる（Q-032）。** バナーのクリック計測は D-12（リダイレクタの移設）に含めた。後から単独でやると、公開済み記事のバナーを貼り替えることになる |
| 2026-08-11 | **測っていないものに0を入れない（Q-032）。** `page_views` は `NULL` のまま残す。0にすると「測ったが0だった」と読める |
| 2026-08-11 | **守るものが増えない仕組みを足さない（Q-026）。** 招待コードのテーブルを作っても、参加の可否は ADMIN の承認が既に握っている。URLを知られても `INVITED` では何もできない |
| 2026-08-11 | **承認1回あたりの負担を増やさない（Q-021）。** `Review` の評点をモニターに入力させる案は採らない。短期KPIが「承認率」と「1記事当たり確認時間」である以上、そこを重くしない |
| 2026-08-11 | **書き忘れを防げない規約にしない（Q-022）。** `facts.updatedAt` を jsonb の中に書く規約では制約を置けない。列にすれば、更新した経路だけが触る |
| 2026-08-11 | **まだ誰も読んでいないうちに正しいほうへ揃える（Q-023）。** `unverified_claims` は仕様の形が情報を持たなかった。読み手が現れる前なら、直す費用はゼロ |
| 2026-08-11 | **根拠の無い定数は固定しない（Q-024）。** 提案の優先度の式は名前付き定数のまま実験を始め、10名の反応を見て差し替える。管理画面から変えられるようにはしない — 実験中に条件が揃わなくなる |
| 2026-08-12 | **確認を呼び出し側の作法に頼らない（A-2-R-2c-schema）。** 依存の向きが `personas → blogs` なので、`blogs` から分身の持ち主を確かめると循環する。「上位へ寄せる」で `src/app/` に置くこともできるが、経路が増えたときに抜けてもレビューでしか気づけない。C-6・D-11 と同じく**制約をDBに置いた** |
| 2026-08-12 | **NULL のあいだは検査されないことを、逃げ道にしない（A-2-R-2c-schema）。** 複合外部キーは MATCH SIMPLE で `persona_id` が NULL なら通る。段階移行のあいだ既存行が落ちないのはこの性質のおかげだが、**片方だけ NULL にしてすり抜けられない**ことを確かめたうえで採った（`user_id` は NOT NULL） |
| 2026-08-12 | **越境の防止と運用方針を、同じ場所に置かない（A-2-R-2c）。** 「他人の分身か」はDBの複合外部キー、「使い始めた分身か」は Route Handler。前者はどの経路からも迂回できない必要があり、後者は抜けても他人のデータには届かない。**要求が違うものを同じ層にまとめると、弱いほうに揃う** |
| 2026-08-12 | **任意で入れて後から必須にしない（A-2-R-2c）。** `personaId` を optional にすると、A-2-R-3 の `NOT NULL` に進めず、テスト25か所の手直しがマイグレーションのPRへ混ざる。**先送りしても総量は減らず、混ざる場所が悪くなるだけ** |
| 2026-08-12 | **下書きのまま媒体を持たせない（A-2-R-2c）。** 段階解放は `ACTIVE` の数を制限するもので、`DRAFT` でブログを作れると初日に3ブログを立てられて制限が意味を失う |
| 2026-08-12 | **閉じても分身の枠は空けない（A-2-R-2c）。** 作り直せるようにすると、同じ分身の媒体が実験期間中に2本に分かれ、一次データが繋がらない。`CLOSED` がスロットを保持し続ける（Q-008）のと同じ扱いにした |
| 2026-08-12 | **分身を付け替えられなくする（A-2-R-2c）。** 付け替えると、それまでに書いた記事の書き手が後から変わる。同じブログの記事が途中で別人になったのか、同じ人が変わったのかを区別できなくなる |
| 2026-08-12 | **読者像を2か所に置かない（A-2-R-2d）。** `blog_persona_settings.target_reader` の参照を外し、`personas.audience` に一本化した。媒体側から上書きできると、どちらが正か分からなくなる。読者を変えたいなら別の分身を立てる |
| 2026-08-12 | **書き手を推測で当てない（A-2-R-2d）。** 分身の割り当てが無いブログは404にした。既定の分身を当てると、誰が書いた記事なのかが後から分からなくなる |
| 2026-08-12 | **消す列は、消す前に読むのをやめる（A-2-R-2d）。** `target_reader` はまだ NOT NULL なので空で埋める。列の削除（A-2-R-3）を待たずに参照を切ると、スキーマのPRが純粋な削除だけで済む |
| 2026-08-12 | **外す前に、読む側が無いことを確かめる（A-2-R-2e）。** `allowed_experiences` は D-6 で入口だけを足し、記事生成は `listPersonaFactsForUser` を通していて一度も読んでいなかった。**繋がっていない配線を、機能として残さない** |
| 2026-08-12 | **選ぶ対象が1組しか無いなら、選ばせない（A-2-R-2e）。** 記憶は分身に溜まり、その分身の媒体は1件。媒体ごとに「使ってよい体験」を選び直す意味が消えた |
| 2026-08-12 | **テーブルを落とす前にコードを外す（A-2-R-2f）。** 逆順にすると Prisma クライアントから `userPersona` が消えて typecheck と build が落ち、スキーマのPRが赤のままマージできない。足す・移す・消すの「消す」を、さらにコードとスキーマに分けた |
| 2026-08-12 | **やめた分身も持ち出せるようにする（A-2-R-2f）。** データ持ち出し（H-4）は `ARCHIVED` を含めて全件返す。抜くと「途中でやめた」と「最初から作らなかった」が区別できない |
| 2026-08-12 | **落ちるべきところで落とす（A-2-R-3）。** `blogs.persona_id` の NOT NULL 化は、NULL の行があれば失敗する。既定の分身を当てて通すこともできるが、**誰が書いた記事なのかが分からなくなる。** 手で割り当ててから再実行する手順をマイグレーションに書いた |
| 2026-08-12 | **DBが保証するようになった判定を、アプリ層に残さない（A-2-R-3）。** `personaId` が NOT NULL になったので、`AppBlog.personaId` を `string` に締め、404 の分岐を消した。二重に置くと、どちらが効いているのか読めなくなる |
| 2026-08-12 | **一度に全部を消さない（A-2-R-3）。** `persona_facts` の `user_id` `blog_id` は読むコードが残っているので落とさなかった。A-2-R-2f → A-2-R-3 と同じ順序で、コード（A-2-R-4）→ スキーマ（A-2-R-4-schema）に分けた |
| 2026-08-12 | **アプリを通さない経路の確認を、CIに残す（A-2-R-3）。** 生SQLの煙テストが `persona_id` 未指定で落ちたのを直すついでに、他人の分身IDでブログを作れないことの確認を足した。**複合外部キーはアプリを迂回しても効くことが要点**なので、確かめる場所も迂回した経路であるべき |
| 2026-08-12 | **「共通」と「固有」を分けるのをやめた（A-2-R-4）。** `persona_facts.blog_id` が `null` の事実を全ブログ共通として扱っていたが、記憶が分身に溜まり、その分身の媒体は1件になったので分ける意味が消えた |
| 2026-08-12 | **落とす予定の列で絞らない（A-2-R-4）。** 所有の確認は `persona: { userId }` を辿る形にした。`persona_facts.user_id` はまだ NOT NULL なので書き込むが、読む側はもう使わない |
| 2026-08-12 | **使わなくなった索引は残さない（A-2-R-4-schema）。** `persona_facts` の `user_id` と `blog_id` の索引を落とした。絞りに使わない索引は、読みを速くせず書き込みだけを遅くする |
| 2026-08-12 | **人格中心モデルへの移行が完了した（A-2-R-1 〜 A-2-R-4-schema、Q-033）。** 20ファイルから参照されるモデルの置き換えを9つのPRに割り、各段階でCIを緑に保った。順序は「足す → 移す → コードを消す → スキーマを消す」 |
| 2026-08-12 | **移行が終わってから、使えるかを確かめた（Q-035）。** 分身のCRUDは揃ったが**HTTPの入口も画面も無く、いまはブログを1件も作れない**。D-14 として起票し、H-2 の依存に足した |
| 2026-08-12 | **仕様と実装の食い違いは、仕様を黙って直さない（Q-035）。** SPEC 6.1 のオンボーディング10段に分身を作る段が無い。`docs/SPEC.md` は実装タスクで書き換えない決まりなので、OPEN_QUESTIONS へ選択肢とともに記録した |
| 2026-08-12 | **同じことを2度聞かない（Q-035）。** オンボーディングの段4「目標登録」を「分身を作る」に置き換えた。目標（収益方針・KPI・撤退条件）は分身の `business` そのもので、分けて聞くと2回同じ話をさせることになる |
| 2026-08-12 | **「上限です」だけを画面に出さない（D-14）。** 待てば開くのか、止めれば開くのか、そもそも開かないのかで、モニターが次に取る行動が変わる。`getPersonaLimitsForUser` が経過日数と次に開くまでの日数を返し、画面が文を書き分ける |
| 2026-08-12 | **状態ごとにURLを分けない（D-14）。** `/activate` と `/pause` にすると状態が増えるたびURLが増える。`POST /status` の本文で何をするかを受ける |
| 2026-08-12 | **画面に検証の規則を書かない（D-14）。** 入力の規則は `normalizeCreatePersona` が持つ。2か所に置くと、いずれ食い違う |
| 2026-08-12 | **配列の入力欄は、打った文字をそのまま持つ（D-14）。** 親の配列から毎回組み立て直すと、空行を落とす処理が効いて**改行を打った瞬間に消え、2行目を打てない**。コンポーネントテストで実際に見つかった |
| 2026-08-12 | **トークンでブログを決める（D-12-schema）。** `blogs.link_event_token_hash` を `UNIQUE` にし、受信APIはこの1列でブログを引く。ブログIDを本文で受けてからトークンを照合する形にすると、**照合を1か所忘れただけで他ブログのイベントを名乗れる** |
| 2026-08-12 | **クリックの数え方を2つ作らない（D-12-schema・Q-032）。** バナーも案件と同じ `/go/{code}` と `link_clicks` を通す。別表に分けると、どちらが正しいのかを後から確かめられない |
| 2026-08-12 | **「どちらか片方」をDBに書く（D-12-schema）。** `link_clicks` の案件リンクとバナーは排他。両方 NULL は何のクリックか分からず、両方入ると二重に数えられる。受信APIの経路が増えても迂回できないよう CHECK 制約に置いた |
| 2026-08-12 | **マイグレーションで拡張に依存しない（D-12-schema）。** `gen_random_bytes` は pgcrypto が要り、実際にローカルで落ちた。`sha256(gen_random_uuid()…)` に替えた |
| 2026-08-12 | **再送を前提に鍵を用意する（D-12-schema-2）。** 受信APIは「失敗したら次回まとめて送る」形なので、応答だけが失われると同じクリックが2回届く。**送信元は保存できたかを知れないので、回数を送信元に任せられない** |
| 2026-08-12 | **「調べてから入れる」で重複を防がない（D-12-schema-2）。** 同じ電文が同時に2回届くと、両方とも「まだ無い」と判定して通る。外から叩かれる入口では同時到着が普通に起きる |
| 2026-08-12 | **壊れた1件で全部を落とさない（D-12）。** 送信元は失敗した分を再送するので、1件のせいで 400 を返すと同じ電文が延々と送られ続ける。通るものだけ通し、落とした数を応答で返す |
| 2026-08-12 | **未来の時刻を受けない（D-12）。** 送信元の時計は当てにできない。受け入れると、集計が存在しない日に数を積む |
| 2026-08-12 | **生の値は形で弾く（D-12）。** UAのsha256とホスト名の形を確かめ、違えば `null` にする。**落とさないのは、クリックそのものは実際に起きているから** |
| 2026-08-12 | **AI検索経由の判別を送信元に任せない（D-12）。** 対象ドメインの一覧が30ブログへ散らばると、後から足せなくなる |
| 2026-08-12 | **飛び先の解決にも認証を要る（D-12）。** 外すと、コードを総当たりして他ブログの案件構成を外から調べられる |
| 2026-08-12 | **置き換えても古い入口を消さない（D-12）。** 配信済みのページに残った旧 `/go/{code}` が 404 になると、読者が広告主へ行けない。数が0で安定してから畳む |
| 2026-08-12 | **オンボーディングの現在地を保存しない（H-2a）。** 段の番号を列に持つと、別の画面で作業したときに食い違う。データから毎回導けば、どこで作業しても一致する |
| 2026-08-12 | **飛ばした段を埋めさせない（H-2a）。** 先の段が済んでいても、抜けた段が現在地になる。順に埋めさせるより、抜けを見せるほうが直しやすい |
| 2026-08-12 | **押せないボタンを置かない（H-2a）。** 受け付ける画面がまだ無い段（同意・通知）はリンクを出さず、「まだありません」と書く |
| 2026-08-12 | **同意の前でも現在地を見せる（H-2a）。** 同意そのものが段2・3なので、`requireConsentedUser` で守ると詰む |
| 2026-08-12 | **同意した時刻を上書きしない（H-2b）。** いつ同意したかは実験の記録で、二度目の操作で動くと「いつから使ってよかったか」が分からなくなる。二度目は何もせず成功させる |
| 2026-08-12 | **同意を取り消す入口を作らない（H-2b）。** データを残したまま同意だけ外れた状態を作らない。やめるときは退会（H-4） |
| 2026-08-12 | **通知の時刻をJSTの壁掛け時計として持つ（H-2b）。** `time` 列へUTCで入れると、読む側が9時間ずらす処理を持つことになり、ずらし忘れが必ず起きる（Q-031 と同じ考え方） |
| 2026-08-12 | **曜日ゼロで保存させない（H-2b）。** 通知が一度も飛ばない「設定済み」は、一番分かりにくい壊れ方になる |
| 2026-08-12 | **空の `monitor_profiles` を作らない（H-2b）。** 登録した全員に行を作ると、通知が「未設定」なのか「行だけある」のか区別できない |
| 2026-08-12 | **通知の時刻ちょうどで判定しない（F-3b）。** ジョブが分単位でその時刻に走る保証は無く、少し遅れただけでその日は一度も届かなくなる。3時間の幅を持たせ、過ぎたら翌日へ回す |
| 2026-08-12 | **未設定を「送らない」にしない（F-3b）。** 設定していないだけの人へ通知が一切届かなくなる。F-3 までの挙動を、設定の有無で黙って変えない |
| 2026-08-12 | **押さえる前に時間帯を見る（F-3b）。** `sent_at` を立ててから弾くと、送っていない提案が送信済みとして残る |
| 2026-08-12 | **溜まった提案を消さずに期限切れにする（F-3b・Q-025）。** 7日を過ぎた未送信は待っても送られない（1日1〜2件）。「出したが送れなかった」と「そもそも出なかった」は別の事実なので、行は残す |
| 2026-08-12 | **同意の文言を2か所に別々に置かない（H-6）。** 画面の文言を切り出し、`docs/CONSENT.md` が同じ行を持つことをテストが見張る。片方だけ直すと落ちる（C-6 の自己維持ガードと同じ趣旨） |
| 2026-08-12 | **同意文に「どこで守っているか」を残す（H-6）。** 画面には出さないが、`line_user_id` を出さない・IPを保存しない・UAはハッシュ、をコードのどこが担保しているかを対応表にした。約束と実装が離れると、どちらかが黙って変わる |
| 2026-08-12 | **散らすものに既定値を置かない（C-9-schema）。** `publish_time` に共通の既定値を置くと、割り当てを忘れた行が「9時のブログ」として紛れる。nullable で足し、埋まったあとに NOT NULL にする |
| 2026-08-12 | **パーマリンクを自由文字列にしない（C-9-schema）。** `%postname%` の綴り違いが、実際のブログの設定と食い違ったまま残る。4つの列挙型にした |
| 2026-08-12 | **移行の埋め値をランダムにしない（C-9-schema）。** `id` から決まる値にする。ランダムだと、同じ移行を2回流したときに別の値になる |
| 2026-08-12 | **指示書どおりに列を足さない判断（C-9-schema・Q-036）。** W-8 の `weekly_post_cap` は `article_ratio.weeklyPublishCap` と同じもの。足すと週の上限が2か所になる。範囲（3〜5）への変更は SPEC 2.2 の書き換えを伴うので、実装タスクでは決めずに記録した |
| 2026-08-12 | **空きを探して割り当てない（C-9）。** 全ユーザーのブログを横断して読むことになるうえ、同時に2件作られると両方が同じ「空き」を選ぶ。`(userId, slotNumber)` から決まる値にした |
| 2026-08-12 | **公開時刻をランダムにしない（C-9）。** 作り直すたびに変わると、読者から見て更新のリズムが崩れ、こちらからは「なぜ変わったか」を追えない |
| 2026-08-12 | **単純なハッシュで散らさない（C-9）。** 足し算のハッシュだと似た種（`u:1` と `u:2`）が近い値になり、曜日も時刻も揃ってしまう。sha256 を使う |
| 2026-08-12 | **振り分けにAIを使わない（D-7a）。** 記事の内容を決める判断ではなく「どこへ入れるか」なので、外部呼び出しの費用と失敗を持ち込む理由が無い |
| 2026-08-12 | **語が複数当たったら、取り違えの害が大きいほうを先に見る（D-7a）。** 修正希望 → 助言 → 感想。修正希望を感想にすると「直して」が記事の素材になり、助言を感想にすると使ってもいない商品の体験談が記事に出る。逆向きは `OPINION` に入るだけで嘘にならない |
| 2026-08-12 | **名詞で経験を判定しない（D-7a）。** 「解約条件に注意」のような助言にも「解約」は出る。済んだことを示す形（`解約した`）だけを見る。**試験を書いて実際に取り違えた** |
| 2026-08-12 | **宛先の決まらない修正依頼を保存しない（D-7a）。** テキスト返信はどの記事への返信かを示さない。直近の承認に紐づけると、違う記事が書き換わる。分類だけ返して承認画面へ案内する |
| 2026-08-12 | **必須にするときも既定値を付けない（C-9-schema-2）。** 既定値があると、割り当てを通さない経路が黙って通る。落とすなら保存の時点で落とす |
| 2026-08-12 | **「拒否されるはず」の検査から必須列を省かない（C-9-schema-2）。** 狙った制約ではなく NOT NULL で落ち、検査が素通りする（#120 と同じ falseなpass） |


| 2026-08-12 | **署名の比較を `===` でしない（D-7b）。** 先頭から何文字一致したかが応答時間に出る。`timingSafeEqual` を使う |
| 2026-08-12 | **設定が無いときに素通りさせない（D-7b）。** チャネルシークレット未設定で 200 を返すと、入れ忘れたまま誰でも分身の記憶を書き込める状態になる |
| 2026-08-12 | **読めない電文に 4xx を返さない（D-7b）。** LINEは 200 以外だと同じ電文を再送する。壊れた1件のために、その電文が延々と届く |
| 2026-08-12 | **Webhookでその場で保存しない（D-7b）。** 応答が遅いとLINEが時間切れと見なして再送する。受けたら積んで、すぐ 200 を返す |
| 2026-08-12 | **長すぎる返信を切って保存しない（D-7b）。** 途中で切った文を事実にすると、書き手の言っていないことが記憶に残る。落とす |
| 2026-08-12 | **分身が2体以上なら記憶を保存しない（D-7b・Q-037）。** テキスト返信はどの分身への返信かを示さない。取り違えると、その分身が持っていない経験になり、嘘が一人称で本文に出る |
| 2026-08-12 | **迷った分類を一人称で使わせない（D-7b）。** `FREE_ANSWER` は「決められなかった」の受け皿で、体験談として書かせると嘘になる |
| 2026-08-12 | **配列の「空」を NOT NULL で塞がない（C-9-schema-3）。** PostgreSQL では `NULL` と「要素が0個」は別物。塞ぎたいのは後者なので CHECK を使う |
| 2026-08-12 | **必須にした列の隣が空でないかを見る（C-9-schema-3）。** 時刻を必須にしても曜日が空なら一度も公開されない。ブログは `ACTIVE` のまま記事だけが溜まり、止まっていることが誰にも見えない |
| 2026-08-12 | **「確かめた時刻」を行の `updated_at` で代用しない（D-13-schema）。** どの列を変えても更新されるので、状態を変えただけで「事実を確かめ直した」ことになる |
| 2026-08-12 | **列を足すときに既定値で埋めない（D-13-schema）。** `now()` を入れると、一度も確かめていない案件が「今日確かめた」ことになり、古い価格が「確認済み」として公開される |
| 2026-08-12 | **「確かめた」を「変わった」で判定しない（D-13）。** 確かめ直して内容が変わっていなかったのがいちばん多く、そこで時刻が動かないと警告が永久に鳴り続ける |
| 2026-08-12 | **省略時の既定値を「入力された」と見なさない（D-13）。** `facts` を省くと `{}` が入るので、書き込む値だけを見ると中身の無い事実が「確認済み」になる。入力に有ったかを見る |
| 2026-08-12 | **書く経路を作ったら読む側も切り替える（D-13）。** 列に書いても読み手が jsonb のままなら、全記事が WARNING のまま。両方に持たせるのは Q-022 が却下した形 |
| 2026-08-12 | **「確かめた」と「壊れ始めた」を1列にしない（H-3b-schema）。** まとめると、継続している切断と直った切断を区別できない。切れ始めた時刻は切れている間は動かさない（動かすと「いつからか」が毎回今日になる） |
| 2026-08-12 | **「分からなかった」を「確認した」にしない（H-3b）。** 届かなかった回に確認時刻を入れると、画面に「今日確認済み」と出る。1週間届かないままなら確認時刻は1週間前のまま出す |
| 2026-08-12 | **「問題ありません」と書かない（H-3b）。** 確認の結果が無いことと、確認して問題が無かったことは別。切れていないときは何も出さない |
| 2026-08-12 | **日付ではなく「何日前から」を出す（H-3b）。** 「8月3日から」だと、今日が何日かを数え直すことになる |
| 2026-08-12 | **目的を果たさない申告を出さない（E-16・Q-021）。** 評点なしの `Review` はリッチリザルトの対象にならず、根拠のない申告の形だけが残る。評点を作り出すことは SPEC 9.6 の禁止事項そのもの |
| 2026-08-12 | **使わなくなった引数を受け取り続けない（E-16）。** 残すと「渡せば何かに使われる」と読める |
| 2026-08-12 | **監査ログを本体と同じトランザクションで書く（H-12）。** 承認が巻き戻ったのに「承認した」記録だけが残ると、実験の記録が実際と食い違う |
| 2026-08-12 | **正常系を全部は残さない（H-12）。** SPEC 14.4 が挙げるものだけを残す。見送りや修正依頼まで残すと、後から見たときに異常が埋もれる |
| 2026-08-12 | **ジョブがした操作の行為者を人にしない（H-12）。** 送るのはジョブで、人が押した瞬間とは別の時刻に動く。誰が通したかは直前の承認の記録にある |
| 2026-08-12 | **何も起きていないなら記録もしない（H-12）。** 内容が同じで送り直したときは投稿していないので、監査ログにも残さない |
| 2026-08-12 | **無い操作は記録できない（H-13）。** ジョブ再実行の監査ログを足そうとして、再実行の機能そのものが無いことが分かった。記録だけ先に作らず、機能を作るタスク（H-14）で一緒に足す |
| 2026-08-12 | **失敗した操作を「した」ことにしない（H-13）。** 使用済みリンクでのログイン失敗は「ログイン」ではない |
| 2026-08-12 | **変化の記録は、変わったときだけ残す（H-13）。** 保存のたびに1行増えると、「いつ変わったか」を探すのに全部読むことになる |
| 2026-08-12 | **作成と有効化を別に残す（H-13）。** プロンプトは版を作っただけでは生成が変わらない。実際に効き始めたのは有効化のとき |
| 2026-08-12 | **嘘の行為者を入れない（H-13）。** `...ForAdmin` は誰が呼んだかを引数に持たない。分からないなら `null` にする |
| 2026-08-12 | **既定値を上限と同じにしない（G-8a）。** 上限を5へ広げたのは実測で上げるためで、最初から5本で始めるためではない |
| 2026-08-12 | **異常時の値を設定の選択肢に混ぜない（G-8a）。** 0本を選べるようにすると、止まっているのが異常なのか設定なのか区別できなくなる |
| 2026-08-12 | **範囲の外の設定を黙って丸めない（G-8a）。** 保存した覚えのない値になる。画面にいまの値を書く |
| 2026-08-12 | **「分からない」を母数から外す（G-8b）。** `indexed` が `NULL` の記事を「載っていない」に倒すと、取得に失敗しただけのブログが停止される |
| 2026-08-12 | **測れていないときは動かさない（G-8b）。** 1本で100%や0%になると上限が毎回振れる。率も返さない（出すと「0%だった」と読める） |
| 2026-08-12 | **記事ごとに最も新しい判定だけを使う（G-8b）。** 古い日の判定まで数えると、長く載っている記事ほど重く数えられる |
| 2026-08-12 | **変わらなかった回を記録しない（G-8b）。** 2週間ごとに同じ値を書くと、変わった回が埋もれる |
| 2026-08-12 | **間隔を冪等キーに持たせる（G-8b）。** cron を増やさず、毎分呼んでも2週間に1回しか積まれない。呼ぶ側が前回の時刻を覚えなくてよい |
| 2026-08-12 | **止めたことを黙らない（G-8b）。** 止まったブログは記事が出ないだけで、画面には何も起きていないように見える |
| 2026-08-12 | **再実行で新しい行を作らない（H-14）。** 同じ冪等キーでは積めず、キーに通番を足すと「この対象の処理」から「何回目の処理」に意味が変わり、二重実行を止める働きが弱くなる。元の行を戻す |
| 2026-08-12 | **状態を戻すだけでは足りない（H-14）。** `claimNextJob` は `attempt_count < MAX_ATTEMPTS` で拾うので、試行回数を0に戻さないと二度と拾われない |
| 2026-08-12 | **中断の印を消したことを黙らない（H-14）。** 消して積み直すと同じ投稿・送信がもう一度起きうる。監査ログと画面の両方に出す |
| 2026-08-12 | **バックオフのある処理を、失敗の繰り返しで再現しない（H-14の試験）。** `claimNextJob` は待ち時間を見るので、ループで回しても2回目は拾われない |
| 2026-08-12 | **「積む関数」を作っただけで繋がったことにしない（I-1）。** 各タスクの完了条件が「保存される」までで、誰が呼ぶかを含んでいなかった。部品は揃っているのに、本番へ出しても何も動き出さない状態だった |
| 2026-08-12 | **配るところ自体をジョブにする（I-1）。** cron から直接30ブログぶんを積むと、毎分120件の挿入を試みてほとんどが重複で捨てられる。1日1件の積み込みジョブに畳む |
| 2026-08-12 | **0件成功を「対象がいなかった」と読み違えさせない（I-1）。** 失敗した利用者の数を別に返す |

| 2026-08-12 | **モジュールが在ることと、画面から呼べることは別（I-3）。** D-1・D-3・C-5 の完了条件が「ブログ別に分離される」までで、入口を含んでいなかった。オンボーディング STEP 8 が画面から完了できない状態だった |
| 2026-08-12 | **受け取らない項目は、受け皿ごと作らない（I-3）。** `linkMode`・`subIdParam`・`blogPostingProhibited` は ASP の規約に関わる判断で、誤ると成果が無効になる。検証で弾くのではなくスキーマに置かない |
| 2026-08-12 | **IDが全体で一意なら、経路を持ち主の下に置く（I-3）。** `affiliate_offers.id` だけで引く入口は、他ブログの案件に届く。ブログ配下にすれば所有権の確認が経路そのものに現れる |
| 2026-08-12 | **状態の更新から終了させない（I-3）。** 終了は `DELETE` の仕事。両方から `ENDED` にできると「消したつもりが残る」逆も起きる |
| 2026-08-12 | **一度のリクエストに30回の外部呼び出しを入れない（I-3）。** 関数の実行時間の上限を超え、途中で切れたことに気づけない。まとめる仕事はジョブに渡す |
| 2026-08-12 | **選ぶことと送ることを、別の間隔のジョブに分ける（I-2）。** 送ってよい時間帯はモニターごとに違い、積み込みが走る深夜とは限らない。1つにすると、深夜に選定した提案がその日は一度も送られない |
| 2026-08-12 | **全員を1件のジョブで見る（I-2）。** 1時間おきに利用者ごとの行を作ると1日240件になり、そのほとんどは「時間帯の外」で何もせず終わる |
| 2026-08-12 | **判定を呼ぶ側に持たせない（I-2）。** 時間帯も件数の上限も、既にモジュールが持っている。呼ぶ側にも置くと、どちらが効いているのか読めない |
| 2026-08-12 | **日付と時は同じ暦で取る（I-2）。** 日付だけJSTにして時をUTCで取ると、日付が変わる時刻に同じ組が2回現れる |
| 2026-08-12 | **ジョブの結果に中身を残さない（I-2）。** 記事の題名が `output_json` に入ると、管理画面のジョブ一覧から他人の記事が読める |
| 2026-08-12 | **上限は詰めた場所で効かせる（I-4）。** 週あたりの本数は構成表を週へ詰める時点で決まっている。積む側で数え直すと、どちらが効いているのか読めなくなる |
| 2026-08-12 | **溜まっていてもまとめて積まない（I-4）。** AIの呼び出しが同時に走ると、費用の上限に一度で当たる。遅れは次の公開日に取り戻せる |
| 2026-08-12 | **起点が無いものは動かさない（I-4）。** `launch_date` が無いブログの「いま何週目か」を推測で決めると、構成表の順序が壊れる |
| 2026-08-12 | **入力が揃っていない仕事をスケジューラに持たせない（I-4・Q-039）。** `PLAN_GENERATION` はジャンルと採用案件を要る。日次の積み込みが推測で埋めてよい値ではない |
| 2026-08-12 | **鍵は作る前に保管先を決める（I-7）。** Vercel の環境変数だけに置くと、プロジェクトを消したときに一緒に消える |
| 2026-08-12 | **手順書は順序そのものが中身（I-7）。** マイグレーションより先にデプロイすると最初のリクエストで落ち、管理者を先に作ろうとするとテーブルがまだ無い |
| 2026-08-12 | **正常に見えるのに動いていない箇所を確認手順に立てる（I-7）。** `CRON_SECRET` が未設定でもアプリは正常に見え、ジョブだけが動かない |
| 2026-08-12 | **手順書に載せるSQLは実際に流して確かめる（I-7）。** `updated_at` は Prisma の `@updatedAt` でDBに既定値が無く、省くと NOT NULL 違反になる |
| 2026-08-12 | **戻り先が1本しか無い経路には、署名した依頼を持たせる（I-8）。** どの依頼に対する戻りなのかを、戻り自体からは区別できない |
| 2026-08-12 | **署名だけでは足りない（I-8）。** 署名は「自分が出した依頼」であることしか示さない。利用者・ブログ・サイトURLの3つを照合して初めて、差し替えを止められる |
| 2026-08-12 | **秘密がクエリで来る経路は、受け取ったら即座に落とす（I-8）。** 履歴・`Referer`・アクセスログに残り続ける。失敗のログにも「何が来たか」を添えない |
| 2026-08-12 | **ブラウザの遷移先でJSONを返さない（I-8）。** モニターが生のJSONを見ることになる。結果は問い合わせ文字列で画面へ渡す |
| 2026-08-12 | **新しい経路を足すとき、保存は既存の入口を通す（I-8）。** 接続先の変更の拒否も監査ログも、そこが持っている |
| 2026-08-12 | **入力が揃う場所で積む（I-10・Q-039）。** `genreName` と `adoptedOfferIds` はモニターがオンボーディングで決めるもので、日次の積み込みが推測で埋めてよい値ではない |
| 2026-08-12 | **「全体が終わった」と「一つ一つが整っている」は別（I-10）。** オンボーディングの完了は1ブログでも満たせば済みなので、完了した時点でまだ整っていないブログがありうる |
| 2026-08-12 | **要らない条件を足さない（I-10）。** 構成表を作るのに WordPress は要らない。条件にすると、書くものが決まっているのに繋ぐまで何も始まらない |
| 2026-08-12 | **既定のままの状態を「未表明」と読まない（I-10）。** 案件の `DRAFT` は登録した直後の状態で、オンボーディングには採用を別に示す手段が無い。外すと段8 が何のための作業か分からなくなる |
| 2026-08-12 | **繋いだことは、通しで動かすまで確かめられない（I-5）。** 各タスクの統合テストは自分の担当だけを見ており、誰も呼んでいない箇所を5つ見逃していた |
| 2026-08-12 | **通しの試験を途中で分けない（I-5）。** 分けると「ここまでは動く」が積み重なり、全体が繋がっていないことを見逃す |
| 2026-08-12 | **外部だけを差し替える（I-5）。** ジョブの積み込みも消化も本番と同じ経路を通す。差し替えると、繋がっていることを確かめられない |
| 2026-08-12 | **CI で秒数を判定しない（I-6）。** 実行機が本番と違うので、混んだ日に落ちる試験になり、落ちても誰も原因を見なくなる。件数と壊れないことを見る |
| 2026-08-12 | **上限の数字が別の上限と噛み合っているか確かめる（I-6）。** SPEC 15.4 の「1日60件通知」は、SPEC 8.3 の「提案は1人1日2件まで」では届かない。残りは提案の枠を消費しない緊急通知 |
| 2026-08-12 | **先に状態を進めると対象から外れる（I-6）。** `READY_FOR_REVIEW` にするのは提案ができてから。試験で先回りすると、選定が何も見つけられない |
| 2026-08-12 | **貼る作業を残さない（I-9）。** 貼り間違いは、リンクが404になって初めて気づく。トークンは一度しか出せないので、確かめる手段も無い |
| 2026-08-12 | **有効化の合図が無い場所では、版を持たせる（I-9）。** `mu-plugins` は置いた瞬間から動くので、書き換え規則を流す機会が無い。版が違うときだけ自分で流す |
| 2026-08-12 | **発行の前に組み立てられるか確かめる（I-9）。** 発行してから失敗すると、古いトークンが無効になったのに新しいファイルが手に入らない |
| 2026-08-12 | **手引きに生成物の写しを置かない（I-9）。** 生成する側と食い違ったときに、どちらが本当なのか分からなくなる |
| 2026-08-12 | **自分が作っているものから直す（J-1）。** 見た目はモニターのテーマにあるが、本文HTMLは Bunshin が作っている。ここなら何も頼まずに、どのテーマでも効く |
| 2026-08-12 | **IDを中身から作らない（J-1）。** 同じ見出しが2つあると重複し、目次の片方がもう片方へ飛ぶ。通し番号にする |
| 2026-08-12 | **分からないものを埋めない（J-1）。** 画像が何を写しているかは書いた本人しか知らない。それらしい `alt` を作ると読み上げに嘘が混ざる。空の `alt` は「読み飛ばしてよい」という正しい意味を持つ |
| 2026-08-12 | **自分が足したものを二度足さない（J-1）。** 修正依頼の再生成ではAIに前の本文を渡すので、足したものが返ってくる |
| 2026-08-12 | **嘘と禁句は止めていたが、薄さは素通りだった（J-4）。** 300〜600記事の全部が薄ければ成果はゼロ。読まれる形に整えても、中身が無ければ読まれない |
| 2026-08-12 | **保存しているのに読んでいないデータを探す（J-4）。** `used_fact_ids` は E-11 から保存されていたが、誰も見ていなかった。新しいデータを取らずに測れた |
| 2026-08-12 | **直す手段が無いものを止めない（J-4）。** 作り直す経路が無いまま `error` にすると、記事は二度と出られない。黙って出力が減るほうが、薄い記事が出るより悪い |
| 2026-08-12 | **閾値が推定であることを書き残す（J-4）。** 1200字も見出し3つも実測ではない。止める判断は、どれだけ引っかかるかを見てから |
